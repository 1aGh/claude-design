// Isolated Remotion entry for the Maude Native Launch social clip.
// Render: node_modules/.bin/remotion render src/launch/index.ts MaudeNativeLaunch <out> --scale=2
import { registerRoot } from 'remotion';
import { LaunchRoot } from './MaudeNativeLaunch';

registerRoot(LaunchRoot);
