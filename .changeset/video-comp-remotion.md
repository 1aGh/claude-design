---
"@1agh/maude": minor
---

Design videos and animations right in the canvas. Maude now ships in-house Remotion video compositions — a `<Player>` with a scrubbable, keyframe-aware Timeline (transport, volume, loop) and capture-first MP4 / GIF export (optionally muxing the comp's audio). Generate a motion piece with the Assistant, then watch, scrub, and export it without leaving the canvas.
