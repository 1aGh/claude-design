{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "PKG=\"${CLAUDE_PLUGIN_ROOT}/../..\"; if [ -f \"$PKG/cli/lib/preflight.mjs\" ]; then (cd \"$PKG\" && node cli/lib/preflight.mjs --plugin design --warn-only); elif command -v maude >/dev/null 2>&1; then maude preflight --plugin design --warn-only; fi",
            "timeout": 8
          }
        ]
      }
    ]
  }
}
