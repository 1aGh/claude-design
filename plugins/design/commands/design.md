---
name: design
category: daily
description: "Renamed to /design:edit. This stub will be removed in the next minor version. Forwarding arguments…"
argument-hint: "\"<feedback>\" [...]"
---

# /design — compat stub (renamed to /design:edit)

This command was renamed to **`/design:edit`** to remove the naming collision with the `design` skill.

Print:

```
→ /design has been renamed to /design:edit (verb-as-action). Forwarding…
  Compat stub removed in the next minor version — switch to /design:edit.
```

Then **invoke `/design:edit`** with the same arguments (`$ARGUMENTS` verbatim).
