# Workspace metadata never ships in the published tarball.
# The root @1agh/md-claude package owns publishing; this nested
# package.json exists only so pnpm treats this dir as a workspace.
package.json
node_modules/
