---
title: md-claude, how it works mostly
description: A Claude Code marketplace. Two plugins, one CLI, some vibes. No telemetry, no signup.
---

Two plugins, one CLI, and the `.ai/` workspace they both pretend not to need. You're probably here for `design` or `flow`. `mdcc` is the plumbing. Cmd-K skips ahead.

- **`design`**. Canvas-first iteration on HTML / JSX mocks under `.design/`. Zero-dep dev server. Cmd+Click inspector overlay so Claude knows which element the feedback is about.
- **`flow`**. The agentic loop that ships things eventually. Second-brain `.ai/` workspace, project-agnostic via per-repo `workflows.config.json`. Same plugin runs in Next.js, Expo, Go, anything.
- **`mdcc`**. Small CLI. Scaffolds the `.ai/` workspace, reads and writes config, boots the design dev server.

Open-source. MIT. No telemetry, no signup, no _book a demo_ button. If it crashes, `git pull` and try again. That's the entire support contract.

## Pick a direction

<Cards>
  <Card
    title="Getting Started"
    href="/docs/getting-started"
    description="Three commands and you're in. (Maybe four. Depends on your shell.)"
  />
  <Card
    title="`mdcc` CLI"
    href="/docs/cli"
    description="Subcommands: init, config, design serve, design init."
  />
  <Card
    title="Flow commands"
    href="/docs/flow"
    description="The 29 slash commands behind plan, execute, done, validate, release."
  />
  <Card
    title="Design commands"
    href="/docs/design"
    description="`/design:new`, `/design:edit`, `/design:critic`, and the rest of the canvas loop."
  />
  <Card
    title="Config reference"
    href="/docs/config"
    description="Every key in `.ai/workflows.config.json` with examples."
  />
  <Card
    title="Recipes"
    href="/docs/recipes/nextjs"
    description="Drop-in setups for Next.js, Expo, and pnpm monorepos."
  />
</Cards>

## Why both plugins together?

The two plugins share a worldview. **Claude Code is the IDE. The repo is the source of truth.** Decisions get written to `.ai/decisions/`. Plans get archived to `.ai/plans/archive/`. Design mocks live in `.design/` next to the production code they precede. Nothing lives only in a chat scroll.

Install one, install both. The marketplace doesn't care which.

## License and source

MIT. Source at [github.com/1aGh/md-claude](https://github.com/1aGh/md-claude). Open issues if it breaks. Send PRs if you have a better idea. I read both.
