name: Quality

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    name: Quality (lint+test+links)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0    # build-stats.mjs reads git tags + shortlog + per-file log

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '22'    # pnpm 11 requires Node >=22.13 (engines.node ">=20" is the CLI runtime constraint, not the build constraint)
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Lint (biome)
        run: pnpm lint

      - name: Test (node:test)
        run: pnpm test

      - name: Tarball shape
        run: bash scripts/check-tarball-shape.sh

      - name: Version parity
        run: bash scripts/check-version-parity.sh

      - name: MDCC tokens drift (.design ↔ site/app)
        run: pnpm --filter @md-claude/site sync:tokens:check

      - name: Site generated content drift (reference + stats)
        run: |
          pnpm --filter @md-claude/site gen:reference
          pnpm --filter @md-claude/site gen:stats
          if ! git diff --quiet site/content/docs/reference site/lib/stats.json; then
            echo "::error::Generated site content is stale. Run \`pnpm --filter @md-claude/site gen:reference && pnpm --filter @md-claude/site gen:stats\` locally and commit the diff."
            git --no-pager diff --stat site/content/docs/reference site/lib/stats.json
            exit 1
          fi

      - name: Site build (validates fumadocs MDX + Next compile)
        run: pnpm --filter @md-claude/site build
