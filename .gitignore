node_modules/
*.log
.DS_Store

# dev-server runtime artifacts (anything the server writes outside the plugin)
dev-server/.cache/

# .design/ runtime — server pid/port, active-tab state, snapshot history
# (source-of-truth files like config.json / README.md / INDEX.md / system/ ARE committed)
.design/_server.json
.design/_active.json
.design/_history/
.design/_comments/
.design/_canvas-state/

# AI workflow runtime — committed templates live in .ai/templates/
.ai/state/HANDOFF.md
.ai/device/
.ai/browser/
.ai/scenarios/**/baseline-staging/
.ai/logs/
.claude/settings.local.json

# Phase 3.4 — dev-server build output (binaries ship via npm sub-packages)
plugins/design/dev-server/dist/
plugins/design/dev-server/bun.lock

# Stub .keep written by dev-server before the inspect.ts fix landed (Phase 3.4)
.design/.keep
