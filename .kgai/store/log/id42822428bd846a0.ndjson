{"hash":"sha256:763a37495091458e2ce6dc6ed52ffa1b6da7be40ea0ad79f034f1c25469b0df4","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":690,"recorded_at":"2026-08-05T00:00:00Z","parents":null,"decision":{"id":"d_02462e5a94854eea85bd2e7d","title":"kgai engine pin bumped v1.0.0 -\u003e v1.4.0; session-sync now uses the engine's own kg sync --auto","rationale":"Upstream shipped v1.0.0 -\u003e v1.4.0 in ~1 week: automatic background sync (--auto, v1.2.0), stricter kg ingest field validation (v1.3.0), and correctness fixes (kg context --paths nested-glob matching, kg as-of end-of-day parsing, a false-conflict bug on bare re-upserts - all v1.1.0). Verified before bumping (DDR-054/056 discipline): kg --help diffed 1.0.0 vs 1.4.0 (purely additive - remote/status/rotate added, nothing removed), our documented kg ingest envelope shape validated with --dry-run against the live 1.4.0 engine, and scripts/kgai-smoke ran clean against this repo's real 212-decision DDR corpus (fixed a stale .ai/decisions/ path in ddr2kgai.py along the way - it silently measured an empty store on every prior run). session-sync (SessionStart pull) switched from a plain blocking kg sync to kg sync --auto, the engine's fire-and-forget mode with a 60s cooldown and non-blocking store-lock skip; the close-time push at /flow:done + /flow:pause stays a deliberate, uncooled kg sync. Also surfaced an unrelated operational footgun: kg's manual per-asset install (docs/kgai-onboarding.md) has no self-update, so a stale local engine can silently shadow a bumped pin on PATH forever - kg status now prints a version field to catch this.","author":"1aGh","summary":"element kgai-engine-pin-v1.4.0; element maude; element dev; link decision:kgai-engine-pin-v1.4.0-IN_REPO-\u003erepo:maude; link decision:kgai-engine-pin-v1.4.0-IN_DEPT-\u003edept:dev; element kgai engine; link decision:kgai-engine-pin-v1.4.0-ABOUT-\u003econcept:kgai engine","supersedes":["d_6be35520b658f88c2c359778"],"shapes":["el_0d3884a2c4157cfa4c0a","el_42b070d9886acea59c36","el_78218f1c592720bd85f8","el_ec5ef965b4979595c248"],"targets":["el_0d3884a2c4157cfa4c0a","el_78218f1c592720bd85f8"],"mutations":[{"op":"upsert_element","element_id":"el_0d3884a2c4157cfa4c0a","kind":"decision","name":"kgai-engine-pin-v1.4.0"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_0d3884a2c4157cfa4c0a","from_id":"el_0d3884a2c4157cfa4c0a","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_0d3884a2c4157cfa4c0a","from_id":"el_0d3884a2c4157cfa4c0a","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_78218f1c592720bd85f8","kind":"concept","name":"kgai engine","props":{"lastVerified":"2026-08-05","pin":"v1.4.0"}},{"op":"add_link","element_id":"el_0d3884a2c4157cfa4c0a","from_id":"el_0d3884a2c4157cfa4c0a","to_id":"el_78218f1c592720bd85f8","link_kind":"ABOUT"}]}}
{"hash":"sha256:8d7e2027cb0c936dbbdba6c6fa24935853d1a29edf24b2a4c802e320b747cd6d","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":692,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:763a37495091458e2ce6dc6ed52ffa1b6da7be40ea0ad79f034f1c25469b0df4"],"decision":{"id":"d_e1bda8cc7a55548fefcf560c","title":"Cloud attach starts syncing in-process — the linkedHub boot-pin is lifted for the consented path only","rationale":"Pressing Connect in the cloud panel used to do nothing observable: the attach lane stored the hub credential and wrote linkedHub into .design/config.json, but the sync runtime captures cfg.linkedHub once at boot and reloadConfig deliberately refuses to hot-swap it (a committed, repo-travelling config must never re-point the sync socket — DDR-149 fan-out review). The UI paid for that invariant with the sentence 'Linked to \u003cX\u003e — restart the studio server to start syncing': a task with no button behind it, naming a component that does not exist for a desktop user. Reported 2026-08-05 with a screenshot.\n\nDecision: split the two paths the boot-pin was conflating.\n\n1. A linkedHub arriving FROM DISK stays boot-pinned and refused — reloadConfig is unchanged. The threat model (a poisoned/attacker-authored config.json in a cloned repo silently re-pointing sync) is untouched.\n2. A linkedHub the person JUST AUTHORIZED in trusted app chrome — exchanged against the configured cloud address, with the matching hub credential stored by this same process one line earlier — is adopted in memory and applied by a real stop()+start() cycle.\n\nMechanism: a new module apps/studio/sync/supervisor.ts (createSyncSupervisor) becomes the sole owner of the live SyncRuntime lifecycle; server.ts no longer holds the runtime directly and publishes the supervisor on ctx.syncControl. context.ts gains adoptLinkedHub(ctx, linkedHub) as the ONE way cfg.linkedHub may change on a running server; it takes the value BY VALUE from the attach lane and never re-reads .design/config.json, so the file-watcher's refused capability cannot return through a side door. cloud/endpoints.ts linkToWorkspace calls ctx.syncControl.restart(linkedHub) and returns a typed sync outcome {syncing, canvases, reason, detail}; the panel reports an outcome ('Syncing with X — N canvases') or the server's own refusal reason, never a restart instruction. Refusals that createSyncRuntime only logged (unlinked / no-credential / insecure-url / nothing-syncable / error) are re-derived by the supervisor so a dialog can say something true; the zero-syncable arm repeats the runtime's own _sync.json reason rather than inventing a second explanation that could drift.\n\nAdjacent fix forced by the change: linkToWorkspace overwrote linkedHub wholesale, dropping a project-wide syncTsx:false opt-out (DDR-072/DDR-079). Harmless while nothing synced until a restart; a real disclosure now that Connect syncs immediately — so the restrictive direction (false only, never true) is carried across a re-link.\n\nAlternatives rejected: (a) a 'Restart now' button — the desktop shell has no restart-sidecar command, and the honest answer to 'why doesn't it do it itself' is that it should; (b) making reloadConfig hot-swap linkedHub for everyone — that is exactly the capability DDR-149 refused, and the file is not where consent lives; (c) leaving the copy and only rewording it — the person would still be doing the product's job.\n\nVerified live, not just in unit tests: a dev-server booted against a scratch project with a stubbed control plane + cell answered POST /_api/cloud/attach with {sync:{syncing:true,canvases:1}}, /_sync-status reported the live runtime immediately, a second Connect cleanly cycled it, and the config watcher emitted no 'not hot-reloadable' warning (the adopted value equals the written one). Tests: apps/studio/test/sync-supervisor.test.ts (incl. one end-to-end case through the real createSyncRuntime) + cloud-endpoints coverage of the outcome and the syncTsx carryover.","author":"1aGh","summary":"element cloud-attach-starts-syncing-in-process-linkedhub-boot-pin-consented-path; element maude; element dev; link decision:cloud-attach-starts-syncing-in-process-linkedhub-boot-pin-consented-path-IN_REPO-\u003erepo:maude; link decision:cloud-attach-starts-syncing-in-process-linkedhub-boot-pin-consented-path-IN_DEPT-\u003edept:dev","shapes":["el_42b070d9886acea59c36","el_a3b2a444b2a33b6af49e","el_ec5ef965b4979595c248"],"targets":["el_a3b2a444b2a33b6af49e"],"mutations":[{"op":"upsert_element","element_id":"el_a3b2a444b2a33b6af49e","kind":"decision","name":"cloud-attach-starts-syncing-in-process-linkedhub-boot-pin-consented-path","props":{"adjacent_fix":"syncTsx:false survives a re-link (restrictive direction only)","boot_pin":"still refused from disk; lifted only for a user-authorized attach","outcome_contract":"{syncing, canvases, reason, detail} — the panel reports a result, never a restart instruction","surface":"apps/studio/sync/supervisor.ts + context.adoptLinkedHub + cloud/endpoints.linkToWorkspace + CloudBar","value_flow":"linkedHub passed BY VALUE from the attach lane, never re-read from config.json"}},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_a3b2a444b2a33b6af49e","from_id":"el_a3b2a444b2a33b6af49e","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_a3b2a444b2a33b6af49e","from_id":"el_a3b2a444b2a33b6af49e","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"}]}}
{"hash":"sha256:4e81461a2f120931b1ca85a2cc1676b3f415375d6d1f00a4819452a9782966ca","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":693,"recorded_at":"2026-08-05T10:53:54Z","parents":["sha256:8d7e2027cb0c936dbbdba6c6fa24935853d1a29edf24b2a4c802e320b747cd6d"],"decision":{"id":"d_3e469929acbed6e0ca1cf4ec","title":"smart-frames gemma tier runs on two interchangeable scout runtimes — mlx-vlm (preferred) or Ollama","rationale":"User feedback: the Gemma model install story was too complicated (mlx-vlm is a manual Python install, and a bare `pip install mlx-vlm` fails on PEP 668 externally-managed Pythons). Two changes: (1) the mlx-vlm install command shown in Settings is now a copy/paste one-liner that creates a Maude-managed venv (~/.maude/mlx-venv, XDG-aware) which resolveMlxPython probes automatically — the command is computed server-side from the same path the probe checks so they can never drift; the install stays USER-RUN per DDR-183. (2) Ollama is a new alternative scout runtime: `ollama pull gemma3:4b` (vision-capable tags only — gemma3:1b and gemma3n are text-only and excluded) unlocks the gemma tier with no Python at all. Ollama has no video input, so the scout ffmpeg-samples ≤16 evenly-spaced timestamped frames and sends them as images to /api/chat; coarser than mlx's native video path, acceptable because scout beats only ADD candidates — ffmpeg cuts stay the precise backbone (DDR-183). mlx-vlm is preferred when both are present (the benchmarked path). Settings card shows both copy/paste commands and re-probes every 10 s so it unlocks itself after the user runs one. Manifest gains a `scout: mlx|ollama` field. Env knobs: OLLAMA_HOST, MAUDE_OLLAMA_MODEL.","author":"Claude (with Michal)","summary":"element smart-frames; element Ollama; element mlx-vlm; link topic:smart-frames-DEPENDS_ON-\u003eservice:mlx-vlm; link topic:smart-frames-DEPENDS_ON-\u003eservice:Ollama; set topic:smart-frames.scout_runtimes","shapes":["el_30bae9065f7a77ccaadf","el_90f03108790aa1fb17f2","el_9811a36114a6c58110b3"],"targets":["el_30bae9065f7a77ccaadf","el_90f03108790aa1fb17f2","el_9811a36114a6c58110b3"],"mutations":[{"op":"upsert_element","element_id":"el_30bae9065f7a77ccaadf","kind":"topic","name":"smart-frames","props":{"paths":"apps/studio/bin/_smart-frames.mjs, apps/studio/generation/gemma-models.ts"}},{"op":"upsert_element","element_id":"el_9811a36114a6c58110b3","kind":"service","name":"Ollama","props":{"role":"alternative local scout runtime (gemma3 vision via /api/chat)"}},{"op":"upsert_element","element_id":"el_90f03108790aa1fb17f2","kind":"service","name":"mlx-vlm","props":{"install":"Maude-managed venv ~/.maude/mlx-venv — copy/paste one-liner in Settings, auto-probed","role":"preferred scout runtime (native video input, Apple Silicon)"}},{"op":"add_link","element_id":"el_30bae9065f7a77ccaadf","from_id":"el_30bae9065f7a77ccaadf","to_id":"el_90f03108790aa1fb17f2","link_kind":"DEPENDS_ON"},{"op":"add_link","element_id":"el_30bae9065f7a77ccaadf","from_id":"el_30bae9065f7a77ccaadf","to_id":"el_9811a36114a6c58110b3","link_kind":"DEPENDS_ON"},{"op":"set_prop","element_id":"el_30bae9065f7a77ccaadf","key":"scout_runtimes","value":"mlx-vlm preferred, Ollama fallback; either unlocks the gemma tier"}]}}
{"hash":"sha256:31830ae4538659a1d5c90c137975765b356501000ab41e583864925d6010daf3","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":694,"recorded_at":"2026-08-05T11:13:02Z","parents":["sha256:4e81461a2f120931b1ca85a2cc1676b3f415375d6d1f00a4819452a9782966ca"],"decision":{"id":"d_0330bf77e856ae756fb8801a","title":"Ollama scout endpoint is loopback-pinned — the footage pipeline stays egress-free","rationale":"Both security reviewers (defender + attacker pass, .ai/logs/security-reviews/gemma-ollama-scout-runtime*.md) independently flagged that an unvalidated $OLLAMA_HOST would silently upload the user's video frames to a remote/LAN host and turn the TTL-cached server probe (GET /_api/generate/keyframe-model → ollamaStatus) into a blind SSRF emitter — contradicting the DDR-183 posture that the footage pipeline is egress-free by design. Resolution, mirroring the DDR-185 curl-local precedent: ollamaHost() (both copies — generation/gemma-models.ts and bin/_smart-frames.mjs) accepts only literal loopback hosts (127.x.x.x, ::1, localhost; no DNS names — a resolvable name could point anywhere or rebind), returns null otherwise, and both fetches use redirect:'manual' plus content-length caps; the scout temp dir is mkdtempSync and model-authored beat labels are control-stripped + capped at 120 chars before entering the manifest. Consequence: a remote Ollama is deliberately NOT supported — supporting one would need an explicit consent surface, not an env var.","author":"Claude (with Michal)","summary":"set service:Ollama.reachability; set topic:smart-frames.egress_posture","supersedes":["d_3e469929acbed6e0ca1cf4ec"],"shapes":["el_30bae9065f7a77ccaadf","el_9811a36114a6c58110b3"],"targets":["el_30bae9065f7a77ccaadf","el_9811a36114a6c58110b3"],"mutations":[{"op":"upsert_element","element_id":"el_9811a36114a6c58110b3","kind":"service","name":"Ollama"},{"op":"set_prop","element_id":"el_9811a36114a6c58110b3","key":"reachability","value":"loopback-only ($OLLAMA_HOST pinned to 127.x/::1/localhost; non-loopback refused, redirect:'manual')"},{"op":"upsert_element","element_id":"el_30bae9065f7a77ccaadf","kind":"topic","name":"smart-frames"},{"op":"set_prop","element_id":"el_30bae9065f7a77ccaadf","key":"egress_posture","value":"egress-free (DDR-183): scout frames never leave the machine; remote Ollama unsupported by design"}]}}
{"hash":"sha256:2318aa7c9cf499b550d68947744fcfda1f4dfc3d2baa313a3b5952ee9f7d87f8","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":695,"recorded_at":"2026-08-05T17:00:32Z","parents":["sha256:31830ae4538659a1d5c90c137975765b356501000ab41e583864925d6010daf3"],"decision":{"id":"d_6c94ec17191daee83f64e5eb","title":"Scout setup routes are server-probed capabilities, not hardcoded client copy — script over brew, link over button","rationale":"The Settings card hardcoded `brew install ollama` and a python3 venv one-liner, both dead ends on a machine without Homebrew or python3 (user-reported). Ownership of 'how do I install this' moves from the client to the server: gemma-models.ts now probes platform/arch/curl/brew/python3 and the ollama binary (TTL-cached, same DoS reasoning as the other probes) and exports ollamaSetupOptions()/mlxSetup(), returning an ORDERED list of routes that work on THIS machine; the client renders that list verbatim and owns no install copy. Three sub-decisions: (1) Ollama's official install.sh is the PRIMARY route ahead of brew on macOS and Linux — verified from the script source that Darwin is handled (fetches Ollama-darwin.zip into /Applications) and it needs only curl, so it works where brew doesn't; brew is offered only when brew exists. (2) The ollama.com download route renders as a COPYABLE LINK, never a button: DDR-054 gives the desktop shell no general URL opener (each opener is host-locked in Rust), so a button would silently no-op in the packaged .app — adding an ollama.com opener would widen that surface and need a Rust command + capabilities + build.rs. (3) installed-but-not-running is a distinct state carrying the opposite advice (`ollama serve`), and mlxSetup() returns a stated REASON instead of a command when the machine can't run it. Known gap left open: the whisper.cpp subtitles card has the identical brew-only hardcoding and would be the natural second consumer of this probe layer — Ollama cannot serve it (official docs: text/chat/embeddings/vision/tools/thinking/web-search, no audio or ASR capability), so only the setup UX unifies, never the runtime.","author":"Claude (with Michal)","summary":"element scout-setup-options; link component:scout-setup-options-PART_OF-\u003etopic:smart-frames; link component:scout-setup-options-DEPENDS_ON-\u003eservice:Ollama; link component:scout-setup-options-DEPENDS_ON-\u003eservice:mlx-vlm; set service:Ollama.install_route; set service:Ollama.capabilities","supersedes":["d_0330bf77e856ae756fb8801a"],"shapes":["el_30bae9065f7a77ccaadf","el_90f03108790aa1fb17f2","el_9811a36114a6c58110b3","el_ef1c604b7f53539bd072"],"targets":["el_9811a36114a6c58110b3","el_ef1c604b7f53539bd072"],"mutations":[{"op":"upsert_element","element_id":"el_ef1c604b7f53539bd072","kind":"component","name":"scout-setup-options","props":{"contract":"server probes capability -\u003e ordered SetupOption[]; client renders, owns no install copy","paths":"apps/studio/generation/gemma-models.ts, apps/studio/client/panels/SettingsPanel.jsx"}},{"op":"upsert_element","element_id":"el_30bae9065f7a77ccaadf","kind":"topic","name":"smart-frames"},{"op":"add_link","element_id":"el_ef1c604b7f53539bd072","from_id":"el_ef1c604b7f53539bd072","to_id":"el_30bae9065f7a77ccaadf","link_kind":"PART_OF"},{"op":"upsert_element","element_id":"el_9811a36114a6c58110b3","kind":"service","name":"Ollama"},{"op":"add_link","element_id":"el_ef1c604b7f53539bd072","from_id":"el_ef1c604b7f53539bd072","to_id":"el_9811a36114a6c58110b3","link_kind":"DEPENDS_ON"},{"op":"upsert_element","element_id":"el_90f03108790aa1fb17f2","kind":"service","name":"mlx-vlm"},{"op":"add_link","element_id":"el_ef1c604b7f53539bd072","from_id":"el_ef1c604b7f53539bd072","to_id":"el_90f03108790aa1fb17f2","link_kind":"DEPENDS_ON"},{"op":"set_prop","element_id":"el_9811a36114a6c58110b3","key":"install_route","value":"official install.sh (macOS+Linux, curl-only) primary; brew only when present; ollama.com download link as floor (copy, not button — DDR-054 has no general URL opener)"},{"op":"set_prop","element_id":"el_9811a36114a6c58110b3","key":"capabilities","value":"text/chat/embeddings/vision/tools/thinking/web-search — NO audio or ASR, so whisper subtitles can never move onto it"}]}}
{"hash":"sha256:6de327787b0d56dd97b0500a9a8dcd09afbfcce80f906bbfe1979a10df3f0609","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":696,"recorded_at":"2026-08-05T17:26:23Z","parents":["sha256:2318aa7c9cf499b550d68947744fcfda1f4dfc3d2baa313a3b5952ee9f7d87f8"],"decision":{"id":"d_898bb85eb948f95c4de82bb7","title":"Scout model downloads are runtime-aware — an Ollama box pulls through /api/pull, not a dead mlx button","rationale":"User feedback on the rendered card: it was a wall of seven options, the disabled button used its LABEL as the error message ('Needs mlx-vlm'), and — the real defect — the Download buttons only ever spoke for the mlx runtime, so a machine running Ollama saw dead buttons on models it could never use. Three changes. (1) The Settings card presents the shortest path as two numbered steps (install/start → pull), tick-marked as each completes, with every alternative route (brew, download link, the whole mlx-vlm option) folded behind one disclosure — default view is two commands. (2) A disabled control keeps its action label and carries the reason in small type beneath it. (3) The model list is now RUNTIME-TAGGED: listScoutModels() merges the mlx HF snapshots with a frozen OLLAMA_MODELS allowlist, marks each row with the runtime that can fetch it (ollama rows' `downloaded` is an EXACT tag match against /api/tags — gemma3:4b is not gemma3:4b-it-q4_K_M), sorts the actionable runtime first, and each row's button gates on its own runtime. The POST route dispatches on an `ollama:`-namespaced id to pullOllamaModel(), which drives the local server's /api/pull NDJSON progress stream into the SAME in-flight download state the HuggingFace path uses. The pull inherits the egress rules already recorded: loopback-pinned host, redirect:'manual', tag from the frozen allowlist and never from the request body. Verified end-to-end against a live Ollama (real pull reported 116 MB / 3.34 GB before cancellation).","author":"Claude (with Michal)","summary":"element scout-model-list; link component:scout-model-list-PART_OF-\u003etopic:smart-frames; link component:scout-model-list-DEPENDS_ON-\u003eservice:Ollama; set component:scout-setup-options.presentation","supersedes":["d_6c94ec17191daee83f64e5eb"],"shapes":["el_30bae9065f7a77ccaadf","el_9811a36114a6c58110b3","el_e898d2b435af4af5aaa1","el_ef1c604b7f53539bd072"],"targets":["el_e898d2b435af4af5aaa1","el_ef1c604b7f53539bd072"],"mutations":[{"op":"upsert_element","element_id":"el_e898d2b435af4af5aaa1","kind":"component","name":"scout-model-list","props":{"contract":"runtime-tagged rows; each button gates on its own runtime; ollama ids pull via /api/pull into the shared download state","paths":"apps/studio/generation/gemma-models.ts, apps/studio/http.ts, apps/studio/client/panels/SettingsPanel.jsx"}},{"op":"upsert_element","element_id":"el_30bae9065f7a77ccaadf","kind":"topic","name":"smart-frames"},{"op":"add_link","element_id":"el_e898d2b435af4af5aaa1","from_id":"el_e898d2b435af4af5aaa1","to_id":"el_30bae9065f7a77ccaadf","link_kind":"PART_OF"},{"op":"upsert_element","element_id":"el_9811a36114a6c58110b3","kind":"service","name":"Ollama"},{"op":"add_link","element_id":"el_e898d2b435af4af5aaa1","from_id":"el_e898d2b435af4af5aaa1","to_id":"el_9811a36114a6c58110b3","link_kind":"DEPENDS_ON"},{"op":"upsert_element","element_id":"el_ef1c604b7f53539bd072","kind":"component","name":"scout-setup-options"},{"op":"set_prop","element_id":"el_ef1c604b7f53539bd072","key":"presentation","value":"two numbered steps (install/start -\u003e pull); alternatives folded behind one disclosure; disabled controls keep their action label, reason in small type"}]}}
{"hash":"sha256:dbd760bfe788a0f4f4bb139332e5f48dd0259344cca77db70ac7edc968685eb5","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":697,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:6de327787b0d56dd97b0500a9a8dcd09afbfcce80f906bbfe1979a10df3f0609"],"decision":{"id":"d_72501773bc726bfd4ea1ad9b","title":"Canvas-lane Request rebuilds live in cell-config.mjs and must ride the original request as init source","rationale":"A Worker Request rebuilt as new Request(url, { ...request, headers }) copies NOTHING from the source request — method and body are prototype getters, invisible to object spread — so every canvas-origin write (annotation strokes, artboard layout, media) crossed the data plane as a bodyless GET. The studio answered writes from its GET branches with a 200 the client read as saved; changes crossed as cursors and died as documents THROUGH the v0.55.0 hub-door fix (canvas-origin-http-writes-at-capability-role), because the method never survived long enough to reach the door that fix repaired. Decision: (1) the two rebuild sites are extracted from worker.mjs into cell-config.mjs as exported pure functions canvasInnerRequest / stripCanvasOriginMarker — cell-config is the dependency-free module the node test suite can import (worker.mjs pulls @cloudflare/containers, unresolvable under plain node), so testability drives the ownership boundary; (2) the invariant is behavioral and pinned by canvas-request.test.mjs: a PUT crosses the rebuild as a PUT, body intact, and the canvas-origin marker is asserted by the Worker alone; (3) the two canvas-origin marker guards in canvas-origin.test.mjs moved from source-regex over worker.mjs to behavioral assertions against the same functions — a source regex pins an inline shape and cannot see a lossy rebuild, which is exactly how this bug shipped. Rule for future code: rebuild a Request only via new Request(url, request) / new Request(request) and mutate headers on the fresh copy; never spread a Request into an init object. Fixed in commit 7a59f9e4, deployed same day via wrangler deploy (Worker-only change — no image rebuild, no cell restart needed).","author":"1aGh","summary":"element canvas-lane-request-rebuild-preserves-method-and-body; element maude; element dev; link decision:canvas-lane-request-rebuild-preserves-method-and-body-IN_REPO-\u003erepo:maude; link decision:canvas-lane-request-rebuild-preserves-method-and-body-IN_DEPT-\u003edept:dev; link decision:canvas-lane-request-rebuild-preserves-method-and-body-REFERENCES-\u003edecision:canvas-origin-http-writes-at-capability-role","shapes":["el_0c351962ac34a5cd9c82","el_254ca6330926170c6309","el_42b070d9886acea59c36","el_ec5ef965b4979595c248"],"targets":["el_254ca6330926170c6309"],"mutations":[{"op":"upsert_element","element_id":"el_254ca6330926170c6309","kind":"decision","name":"canvas-lane-request-rebuild-preserves-method-and-body"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_254ca6330926170c6309","from_id":"el_254ca6330926170c6309","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_254ca6330926170c6309","from_id":"el_254ca6330926170c6309","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_0c351962ac34a5cd9c82","kind":"decision","name":"canvas-origin-http-writes-at-capability-role"},{"op":"add_link","element_id":"el_254ca6330926170c6309","from_id":"el_254ca6330926170c6309","to_id":"el_0c351962ac34a5cd9c82","link_kind":"REFERENCES"}]}}
{"hash":"sha256:479ded3c4ca3467aa25eb7377034605409b13506e2693df31856898d9fc06a37","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":698,"recorded_at":"2026-08-05T17:46:35Z","parents":["sha256:dbd760bfe788a0f4f4bb139332e5f48dd0259344cca77db70ac7edc968685eb5"],"decision":{"id":"d_d63583556c28b8111fca8963","title":"Transcription gains an `auto` engine — DDR-164's no-silent-switch rule kept by making auto a SELECTED, self-declaring mode","rationale":"User asked for the subtitle engine to switch to ElevenLabs Scribe automatically when a key is present. DDR-164 (Task 2.6) had deliberately made the engine an EXPLICIT choice so Maude would never silently reach for a paid, off-machine engine — and that rationale is load-bearing, not bureaucratic: one ElevenLabs key covers audio GENERATION and Scribe ('one key for the whole stack'), so a key added for music/TTS would otherwise be enough to start uploading the user's audio and billing their account for transcription they never chose. Resolution keeps the rule instead of reversing it: `auto` joins whisper|elevenlabs|groq as a mode the user SELECTS, and it always declares its resolution — the Settings card renders 'Right now Auto uses \u003cengine\u003e — \u003creason\u003e' plus an explicit 'audio is uploaded to that provider and billed to your account' when cloud, and the CLI prints 'auto → …' before transcribing. Precedence: elevenlabs (accuracy beats a local base model) \u003e groq \u003e local whisper; an UNRELATED provider key (e.g. Google for images) never moves audio off-machine, which is a test. Config-schema enum, prefs vocabulary and the CLI's KNOWN_PROVIDERS all gain `auto`; absent config still means whisper, so no existing install changes behavior without the user picking auto. Second half of the same change: the subtitle card now PROBES whisper.cpp (mirroring _transcribe.mjs's resolution exactly, including its refusal to probe a bare `main` — a prior security finding) and shows machine-viable setup routes in the two-step shape, since whisper.cpp ships prebuilt binaries for Linux/Windows but NOT macOS, making 'a cloud engine' the honest brew-less answer. Shared TTL-cached PATH/spawn probing extracted to generation/runtime-probe.ts, now used by both the scout and subtitle stacks. NOT unified: the RUNTIME — Ollama has no ASR capability (official docs: text/chat/embeddings/vision/tools/thinking/web-search), so subtitles can never move onto it.","author":"Claude (with Michal)","summary":"element runtime-probe; element transcription-engine-choice; link component:scout-setup-options-DEPENDS_ON-\u003ecomponent:runtime-probe; link feature:transcription-engine-choice-DEPENDS_ON-\u003ecomponent:runtime-probe; set feature:transcription-engine-choice.cloud_key_coupling","supersedes":["d_898bb85eb948f95c4de82bb7"],"shapes":["el_0f9d09f6eaaa86d944cb","el_dae3bf7906726ac2e95e","el_ef1c604b7f53539bd072"],"targets":["el_0f9d09f6eaaa86d944cb","el_dae3bf7906726ac2e95e","el_ef1c604b7f53539bd072"],"mutations":[{"op":"upsert_element","element_id":"el_0f9d09f6eaaa86d944cb","kind":"component","name":"runtime-probe","props":{"paths":"apps/studio/generation/runtime-probe.ts","role":"shared TTL-cached capability probing + SetupOption vocabulary for optional local runtimes"}},{"op":"upsert_element","element_id":"el_dae3bf7906726ac2e95e","kind":"feature","name":"transcription-engine-choice","props":{"invariant":"no engine switch without the user selecting it; auto always states its resolution and flags cloud egress + billing","paths":"apps/studio/generation/whisper-models.ts, apps/studio/generation/prefs.ts, apps/studio/bin/_transcribe.mjs","vocabulary":"auto|whisper|elevenlabs|groq"}},{"op":"upsert_element","element_id":"el_ef1c604b7f53539bd072","kind":"component","name":"scout-setup-options"},{"op":"add_link","element_id":"el_ef1c604b7f53539bd072","from_id":"el_ef1c604b7f53539bd072","to_id":"el_0f9d09f6eaaa86d944cb","link_kind":"DEPENDS_ON"},{"op":"add_link","element_id":"el_dae3bf7906726ac2e95e","from_id":"el_dae3bf7906726ac2e95e","to_id":"el_0f9d09f6eaaa86d944cb","link_kind":"DEPENDS_ON"},{"op":"set_prop","element_id":"el_dae3bf7906726ac2e95e","key":"cloud_key_coupling","value":"one ElevenLabs key serves audio generation AND Scribe — a key added for music/TTS makes auto route transcription to the cloud; card states this, pinning whisper stays one click"}]}}
{"hash":"sha256:570509dbfb9d1143607390e8d3dde92a0e220f92502dd967e9223a9391cd6fca","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":699,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:479ded3c4ca3467aa25eb7377034605409b13506e2693df31856898d9fc06a37"],"decision":{"id":"d_4c1b482985b2b0c6765018c6","title":"Studio route DNS-rebind guard is mode-aware: loopback Host locally, hub x-maude-role vouch in a cell","rationale":"The studio gated ~97 route handlers on isLoopbackHost(req.headers.get(host)) as a DNS-rebinding defence. In a cell that check can never pass — the hub proxy rewrites Host to the project public name (D4, upstreamHeaders) — so every browser-shell project write (Inspector CSS, artboard resize/style/kind, insert/delete/reorder, edit-attr/text) 403d with local request required, while annotations/comments worked only because their handlers carry no such guard. Decision: introduce one helper isTrustedRequestHost(req) in apps/studio/http.ts and route all 98 call sites through it. It returns true for a loopback Host (verbatim local behaviour) OR, when isWorkspaceMode(), when the request carries an injected x-maude-role header. That header is unforgeable proof of hub provenance because the proxy strips every inbound x-maude-* before injecting its own — the same WORKSPACE ? x-maude-role vouch the collab-WS upgrade in server.ts already trusts. Safe against secret leakage by construction: a cell PRUNES every secret-bearing route family from its table (pruneForWorkspace + the containment boot-assert), so the vouched path can only reach the project-editing surfaces that survive. Second, established invariant: a write-classified manifest entry must never be a GET-only handler — /_api/edit-scope was filed {safe:null,unsafe:edit} but is a GET-only read (Local/Shared badge), so the hub answered the shell GET 405 for every role. Fixed to {safe:read,unsafe:null} and pinned by a new source-scrape guard in studio-manifest.test.mjs that fails the build on any future inversion. This is the third distinct layer of the cloud editor write path repaired in one day, after canvas-lane-request-rebuild-preserves-method-and-body (worker method-drop) and the /auth/login role storage. Fix committed + validated (hub 474/474, studio 3528/0); reaches a live cell only via a cell-IMAGE rebuild, not wrangler deploy, because http.ts + the manifest run inside the container.","author":"1aGh","summary":"element studio-route-guard-mode-aware-hub-vouch; element maude; element dev; link decision:studio-route-guard-mode-aware-hub-vouch-IN_REPO-\u003erepo:maude; link decision:studio-route-guard-mode-aware-hub-vouch-IN_DEPT-\u003edept:dev; link decision:studio-route-guard-mode-aware-hub-vouch-REFERENCES-\u003edecision:canvas-lane-request-rebuild-preserves-method-and-body","shapes":["el_254ca6330926170c6309","el_42b070d9886acea59c36","el_e8eea1fb6c41593ce440","el_ec5ef965b4979595c248"],"targets":["el_e8eea1fb6c41593ce440"],"mutations":[{"op":"upsert_element","element_id":"el_e8eea1fb6c41593ce440","kind":"decision","name":"studio-route-guard-mode-aware-hub-vouch"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_e8eea1fb6c41593ce440","from_id":"el_e8eea1fb6c41593ce440","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_e8eea1fb6c41593ce440","from_id":"el_e8eea1fb6c41593ce440","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_254ca6330926170c6309","kind":"decision","name":"canvas-lane-request-rebuild-preserves-method-and-body"},{"op":"add_link","element_id":"el_e8eea1fb6c41593ce440","from_id":"el_e8eea1fb6c41593ce440","to_id":"el_254ca6330926170c6309","link_kind":"REFERENCES"}]}}
{"hash":"sha256:732818e4e88c7538dc67d3d2982a35a23b04fabb14d7d6a80044f58d60189423","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":700,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:570509dbfb9d1143607390e8d3dde92a0e220f92502dd967e9223a9391cd6fca"],"decision":{"id":"d_c13ffa3b9a66a12fea49d173","title":"sameOriginWrite compares host+port, not full origin — TLS terminates at the cell proxy","rationale":"The studio CSRF guard sameOriginWrite (apps/studio/http.ts, 78 write routes) compared the browser Origin header against new URL(req.url).origin — a full-origin equality including scheme. In a cell TLS terminates at the hub proxy: the browser sends Origin https://\u003chost\u003e but the studio serves plaintext and computes req.url as http://\u003chost\u003e, so the scheme differed and every legitimate shell write was refused with 403 cross-origin write rejected. This was the SECOND cell-incompatible guard on the shell write path, uncovered only after the loopback fix (decision:studio-route-guard-mode-aware-hub-vouch) let requests reach it; the two together are why the cloud Inspector could annotate/comment but not edit. Decision: compare host+port only (new URL(origin).host === new URL(req.url).host), the same host-only reasoning isOwnShellOrigin in studio-proxy.mjs already uses for the collab WS upgrade for the identical TLS-termination reason. Safe: scheme was never the CSRF discriminator (locally everything is http, and a real cross-site page has a different host), and the port still pins the origin. The change is unconditional (not workspace-gated) because host-only is strictly correct in both modes. sameOriginRead is untouched — it keys on Sec-Fetch-Site, already scheme-agnostic. Verified live on alligators: edit-css with no/http Origin reached the handler (422 element-not-found) before this change; this closes the https-Origin case a real browser sends. Committed 21a7b7a2.","author":"1aGh","summary":"element sameoriginwrite-host-not-scheme-behind-tls-proxy; element maude; element dev; link decision:sameoriginwrite-host-not-scheme-behind-tls-proxy-IN_REPO-\u003erepo:maude; link decision:sameoriginwrite-host-not-scheme-behind-tls-proxy-IN_DEPT-\u003edept:dev; link decision:sameoriginwrite-host-not-scheme-behind-tls-proxy-EXTENDS-\u003edecision:studio-route-guard-mode-aware-hub-vouch","shapes":["el_3a91f9aef04603d1f3bf","el_42b070d9886acea59c36","el_e8eea1fb6c41593ce440","el_ec5ef965b4979595c248"],"targets":["el_3a91f9aef04603d1f3bf"],"mutations":[{"op":"upsert_element","element_id":"el_3a91f9aef04603d1f3bf","kind":"decision","name":"sameoriginwrite-host-not-scheme-behind-tls-proxy"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_3a91f9aef04603d1f3bf","from_id":"el_3a91f9aef04603d1f3bf","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_3a91f9aef04603d1f3bf","from_id":"el_3a91f9aef04603d1f3bf","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_e8eea1fb6c41593ce440","kind":"decision","name":"studio-route-guard-mode-aware-hub-vouch"},{"op":"add_link","element_id":"el_3a91f9aef04603d1f3bf","from_id":"el_3a91f9aef04603d1f3bf","to_id":"el_e8eea1fb6c41593ce440","link_kind":"EXTENDS"}]}}
{"hash":"sha256:24aab95f51e71b2145d34633114b0ea78016836a37ac3c67a759c489b89fbc10","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":701,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:732818e4e88c7538dc67d3d2982a35a23b04fabb14d7d6a80044f58d60189423"],"decision":{"id":"d_a67b354973d7eea745134bc1","title":"The fleet nav has exactly one door, and it is the dashboard","rationale":"Cloud Phase 26 gated the operator surface twice on purpose: at the route (404 for anyone outside OPERATOR_ACCOUNT_IDS) and again at the shell, because a nav entry a customer can see but not open is an invitation to try, and the section's existence is itself information they have no reason to hold. appShell therefore renders the Fleet section only when passed isOperator: true.\n\nWhat shipped left that flag with one caller. operator-pages.mjs set it; the other five appShell callers — dashboard.mjs, project-admin.mjs, people-page.mjs, checkout-pages.mjs, device-auth.mjs — did not, and none of them computed the predicate at all. The observable consequence: the Fleet nav appeared only once you were ALREADY inside /operator, so the surface had no way in except typing the URL. The allowlist was doing its job; nothing linked to what it unlocked.\n\nDECIDED: the predicate is evaluated for exactly one customer-facing page. auth-routes.mjs (which has env in scope at the signed-in GET / branch) calls isOperator(env, account) and threads it into dashboardPage, which forwards it to the shell. The dashboard is where a signed-in person starts, so that is where the door belongs.\n\nDECIDED AGAINST threading it into the remaining four. project-admin, people-page, checkout and device-auth stay on the isOperator = false default. Two reasons. A payment or device-pairing flow is not a place to grow navigation — the nav would be noise at the moment the page is asking for one specific thing. And every additional caller that computes the predicate is another site where the gate can be got wrong; one door is one place to audit. The default-false parameter is what keeps those four pages byte-identical rather than merely equivalent.\n\nThe shell gate itself is UNCHANGED, and so is the rule behind it — a customer still renders no /operator string and no Fleet heading anywhere, which operator-pages.test.mjs already asserted and dashboard.test.mjs now asserts again at the page level. What changed is only that the predicate is finally evaluated outside /operator.\n\nTested at two levels on purpose, because they catch different regressions. dashboard.test.mjs covers the render contract (flag on renders the door, default renders nothing, a customer holding a project renders nothing). auth-routes.test.mjs covers the wiring end to end through a real worker.fetch with a session cookie and the account id in OPERATOR_ACCOUNT_IDS — that one fails if a future edit drops the env threading, which the render-level test alone could not see.","author":"1aGh","summary":"element cloud-fleet-nav-one-door-on-the-dashboard; element cloud-operator-surface; element maude; element dev; link decision:cloud-fleet-nav-one-door-on-the-dashboard-REFERENCES-\u003efeature:cloud-operator-surface; link decision:cloud-fleet-nav-one-door-on-the-dashboard-IN_REPO-\u003erepo:maude; link decision:cloud-fleet-nav-one-door-on-the-dashboard-IN_DEPT-\u003edept:dev","shapes":["el_42b070d9886acea59c36","el_5ca5dbae4ccd950abea1","el_a3a1e0406e09d5996716","el_ec5ef965b4979595c248"],"targets":["el_5ca5dbae4ccd950abea1"],"mutations":[{"op":"upsert_element","element_id":"el_5ca5dbae4ccd950abea1","kind":"decision","name":"cloud-fleet-nav-one-door-on-the-dashboard"},{"op":"upsert_element","element_id":"el_a3a1e0406e09d5996716","kind":"feature","name":"cloud-operator-surface"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_5ca5dbae4ccd950abea1","from_id":"el_5ca5dbae4ccd950abea1","to_id":"el_a3a1e0406e09d5996716","link_kind":"REFERENCES"},{"op":"add_link","element_id":"el_5ca5dbae4ccd950abea1","from_id":"el_5ca5dbae4ccd950abea1","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_5ca5dbae4ccd950abea1","from_id":"el_5ca5dbae4ccd950abea1","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"}]}}
{"hash":"sha256:db81e1f6fc2d51d51bb4e4b2e75d06eafd6d6708ba3efb8a512b40eb9102b0da","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":702,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:24aab95f51e71b2145d34633114b0ea78016836a37ac3c67a759c489b89fbc10"],"decision":{"id":"d_ce8c5949f8493984338bbc36","title":"Debate: production-grade stack direction — no wholesale Vite/Tailwind/Turborepo swap","rationale":"Divergent /flow:plan debate converged 3-0 swap-no (BUILDER, SHIPPER, BREAKER, each confidence 4). Chosen direction: keep the DDR-009 Bun.build/compiled-binary pipeline and the DDR-043 token contract; make the studio client observable first (biome un-exclude, per-package tsc gate, check-client-boots + bundle-parity per-PR, characterization tests), then decompose app.jsx in place along the existing client/panels/ seam, then dedupe the REAL duplication (packages/tokens, -maude CSS theme forks, runtime-state list generator, portable crypto primitives). Rejected: Turborepo (redundant over pnpm -r; caching is the v0.22.0 failure class); Tailwind in the studio client or canvas contract (public token API; already correctly placed in site/); Vite as shipping bundler (cannot compile into the sidecar; dev server IS a bundler — canvases compile at request time inside the bun --compile binary). Open doors recorded: Vite as dev-only inner loop after the split (byte-compat condition), Tailwind shell-only after Phase 1 (BREAKER concession). Evidence: client is 33,891 lines JSX + 10,554 CSS with zero tests, excluded from lint, untypechecked; all six shipped release breaks were build/packaging seams; cloud-hub route duplication is 12-14 identical lines per 400-1100 (share primitives, not routes).","author":"1aGh","summary":"element debate-production-grade-stack-direction","shapes":["el_872b2548f910094ecb4d"],"targets":["el_872b2548f910094ecb4d"],"mutations":[{"op":"upsert_element","element_id":"el_872b2548f910094ecb4d","kind":"decision","name":"debate-production-grade-stack-direction","props":{"confidence":"4","dissent":"BUILDER top_risk: decomposing app.jsx is exactly the change class that shipped v0.51.1 blank window unless check-client-boots runs per-PR before the first move. SHIPPER top_risk: a pure code-move refactor silently changes committed dist/client.bundle.js because check-client-boots is hand-run, not per-PR. BREAKER top_risk: gates-first degenerates into gates-only (better lint, same monolith); enabling biome across 33,891 virgin lines is itself a large risky diff on a Syncthing tree.","seats":"BUILDER,SHIPPER,BREAKER","shape":"divergent"}}]}}
{"hash":"sha256:78d29034fcd30065192de8d27dd6cf552267df245be6fb8fa9e1a761a7d5f427","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":703,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:db81e1f6fc2d51d51bb4e4b2e75d06eafd6d6708ba3efb8a512b40eb9102b0da"],"decision":{"id":"d_97f18132c1736916b20f8877","title":"Plan: production-grade hardening \u0026 decomposition (stack stays)","rationale":"Gates-first hardening instead of a Vite/Tailwind/Turborepo rewrite: Phase 1 make the studio client observable (lint+typecheck+boot/parity gates), Phase 2 decompose app.jsx in place along client/panels/, Phase 3 dedupe real duplication into packages/tokens, packages/protocol, packages/crypto-portable. Direction fixed by debate-production-grade-stack-direction.","author":"1aGh","summary":"element production-grade-hardening","shapes":["el_1bd9ffcf9992a7b3535f"],"targets":["el_1bd9ffcf9992a7b3535f"],"mutations":[{"op":"upsert_element","element_id":"el_1bd9ffcf9992a7b3535f","kind":"plan","name":"production-grade-hardening","props":{"path":".ai/plans/feature-production-grade-hardening.md","status":"active"}}]}}
{"hash":"sha256:325e9c83ce67affcbe362c25a25acc6e6f76b793122ac4f77aa67f478846d7f8","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":704,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:78d29034fcd30065192de8d27dd6cf552267df245be6fb8fa9e1a761a7d5f427"],"decision":{"id":"d_8001e81d18a4ad6cfeb256d9","title":"Container write bridge synthesises fs:any in workspace mode — canvas-edit relies on an unreliable inotify recursive watch","rationale":"In a cell, source edits (Inspector CSS, artboard, insert/reorder) reached disk but never live-re-rendered for a peer browser — the peer sat on the pre-edit module until a manual reload, while annotations crossed live. Root cause (verified live: a 200 edit-css delivered nothing to a connected canvas-hmr socket): canvas-edit.ts writes every edit atomically via Bun.write(tmp)+rename into a designRoot subdir (25 sites) and emits NO bus event — it depends on fs-watch.ts (fs.watch(designRoot,{recursive:true})) to notice and fire fs:any, which drives the HMR broadcaster and the peer canvas-hmr socket. macOS FSEvents catches the rename; the Linux container inotify emulated recursive watch MISSES the atomic rename into a subdir, so fs:any never fires. Decision: add createContainerWriteBridge(ctx) in hmr-broadcast.ts, wired WORKSPACE-MODE-ONLY in ws.ts. Every write path already arms activity:suppress(rel) immediately before writing (to mute the agent-activity rim) — a reliable, complete write-imminent signal. The bridge treats it as the fs:any the watcher owes and synthesises one 250ms later (the write is awaited inside the op so it has settled; 250ms \u003c\u003c activity SUPPRESS_TTL_MS 2500ms so the rim stays muted). A no-op/failed edit disarms via activity:unsuppress, which cancels the pending emit. Workspace-gated because locally fs.watch fires and a second source would double-load every canvas (the HMR 50ms per-path debounce cannot coalesce events ~250ms apart). This covers all 24 write-path ops at one point with zero churn to the 25 writers, and is the pragmatic alternative to a 25-site emit refactor or replacing the emulated recursive inotify watch. The synthetic fs:any also correctly drives the git-status broadcast; canvas-list-watch ignores plain edits. Follow-up on file: a durable fix replaces the container recursive watch with per-subdir watches or a poll fallback. Committed 1a8c4ab9; part of the same-day cloud-editor write-path repair series (worker method-drop, /auth/login role, DNS-rebind mode-aware guard, sameOriginWrite host-only, and now the live-render bridge).","author":"1aGh","summary":"element container-write-bridge-synthesises-fs-any-workspace-mode; element maude; element dev; link decision:container-write-bridge-synthesises-fs-any-workspace-mode-IN_REPO-\u003erepo:maude; link decision:container-write-bridge-synthesises-fs-any-workspace-mode-IN_DEPT-\u003edept:dev; link decision:container-write-bridge-synthesises-fs-any-workspace-mode-REFERENCES-\u003edecision:studio-route-guard-mode-aware-hub-vouch","shapes":["el_42b070d9886acea59c36","el_af9988d70027f1ebcd44","el_e8eea1fb6c41593ce440","el_ec5ef965b4979595c248"],"targets":["el_af9988d70027f1ebcd44"],"mutations":[{"op":"upsert_element","element_id":"el_af9988d70027f1ebcd44","kind":"decision","name":"container-write-bridge-synthesises-fs-any-workspace-mode"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_af9988d70027f1ebcd44","from_id":"el_af9988d70027f1ebcd44","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_af9988d70027f1ebcd44","from_id":"el_af9988d70027f1ebcd44","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_e8eea1fb6c41593ce440","kind":"decision","name":"studio-route-guard-mode-aware-hub-vouch"},{"op":"add_link","element_id":"el_af9988d70027f1ebcd44","from_id":"el_af9988d70027f1ebcd44","to_id":"el_e8eea1fb6c41593ce440","link_kind":"REFERENCES"}]}}
{"hash":"sha256:bf5e7ff1ad45ee71c69df81586a67efb41729cba4f6ede80c4879c7bb72aa49a","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":705,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:325e9c83ce67affcbe362c25a25acc6e6f76b793122ac4f77aa67f478846d7f8"],"decision":{"id":"d_fbaa01c69b2c777e939ad81e","title":"Debate round 2: shared-layer architecture — import spine, vendor edge","rationale":"Divergent debate (BUILDER, SHIPPER, BREAKER, USER-ADVOCATE — all confidence 4) converged 4-0: import (workspace packages) is the spine for internal apps, vendor (design:handoff shadcn registry) stays the edge protocol for downstream users and everything crossing the DDR-054 trust boundary. No-build consumers (cloud, cells, hub) receive generated committed source with CI dep-surface freeze — never workspace imports. Scope: packages/tokens (one source, ~7 emit targets, byte-identical-on-first-run gate, NAME deprecation window), packages/protocol (+ slug conformance test exposing the locator.ts canvasSlug mismatch), packages/crypto-portable narrowed to token grammar + timing-safe compare (base64url stays duplicated on purpose). Unified UI: shared CSS extracted from the APP (canvas becomes a view of real code — the mock is never the source), served to canvases via a @maude/ds-css virtual specifier (DDR-025 precedent) with a compiled-binary kill-switch; handoff gains --check freshness + a self-containment contract test; DiffView pair inversion is the measured pilot for any future packages/ui. Evidence corrections: base64url x5 dissolved (one 4-line Workers shim + Node builtins); -maude CSS pairs are coexisting layers (zero shared selectors), not forks; the REAL duplication is the canvas-app vocabulary authored twice (Studio.css hand-ported and drifted 667 to 2887 lines; five canvas-panel pairs with zero .design imports under apps/; token declarations x5; 12 registry sidecars freshness-unchecked). Gates before content: five-environment reachability canary, explicit workspace membership (never packages/* glob — 7 binary staging packages live there), dep freeze, import-coherence extension. TypeScript policy: checker-first ratchet — checking everywhere now, new packages TS-authored emitting committed .mjs, conversions only runtime-native (hub under Bun, client during decomposition), cli stays JSDoc .mjs (Node type stripping disabled in node_modules), cloud/cells evidence-gated.","author":"1aGh","summary":"element debate-shared-layer-architecture; element debate-production-grade-stack-direction","shapes":["el_234b3ea6f0f9ba14938f","el_872b2548f910094ecb4d"],"targets":["el_234b3ea6f0f9ba14938f"],"mutations":[{"op":"upsert_element","element_id":"el_234b3ea6f0f9ba14938f","kind":"decision","name":"debate-shared-layer-architecture","props":{"confidence":"4","dissent":"BREAKER top_risk: the dual-install seam — every packages/* must resolve under root pnpm AND apps/studio bun.lock AND bun build --compile AND the staged .app AND the npm tarball (the DDR-176/177 class that shipped v0.44, v0.45, v0.51.1). ADVOCATE top_risk: import gets executed as packages/ui with one real consumer, paying the DDR-177 tax while the .design-client drift the maintainer actually feels goes untouched. SHIPPER top_risk: the ds-css virtual specifier is a fourth instance of the DDR-045 bug class unless routed through paths.ts. BUILDER top_risk: generator sprawl — one source with ~7 emit targets is a new artifact seam.","seats":"BUILDER,SHIPPER,BREAKER,USER-ADVOCATE","shape":"divergent"}},{"op":"upsert_element","element_id":"el_872b2548f910094ecb4d","kind":"decision","name":"debate-production-grade-stack-direction"}]}}
{"hash":"sha256:471439d17a99938d0ebfc8b9d7a7ed08a8d449ae278dfa0c2b499e23e2087394","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":706,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:bf5e7ff1ad45ee71c69df81586a67efb41729cba4f6ede80c4879c7bb72aa49a"],"decision":{"id":"d_e2da885b8cebefa3113b1a17","title":"Debate round 3: maturity escalation — verification reach, release integrity, DTCG; language holds with named triggers","rationale":"Maturity-first round (effort/cost objections ruled invalid; online research guest verified industry practice) converged 4-0 escalate — on the VERIFICATION axis, not the language axis. Escalated: (1) verification reach — ~319 existing test files (studio 236, hub 42, cloud 41) never ran pre-merge; all three suites enter quality.yml non-blocking-then-ratchet; (2) gate repairs — studio tsconfig blind to 39/43 root .tsx incl. canvas-lib.tsx, check-import-coherence 43 percent blind to parent-relative imports (v0.54.0 hub break = seventh shipped artifact break), bun version pinned end-to-end, dead pre-DDR-009 server.mjs removed with loud no-bun refusal; (3) atomic release gate — publish-main gains needs: desktop smoke gates (npm was publishing while blank-window gates still ran; three same-day hotfix chains in three weeks); (4) release soak resolved via cross-challenge from a 2:2 split: GitHub-prerelease-by-default (site updater route reads releases/latest which excludes prereleases — stable isolation with zero client change), written promotion rule in the runbook (min soak 48h or one nightly pass, gates green, explicit promote, promote-now break-glass), maintainer install on canary feed, upgrade-from-N-1 nightly leg exercising the never-tested auto-updater path; populated channel deferred to \u003e5 external installs; (5) nightly ecosystem-verify as the repo's first scheduled workflow; (6) DTCG 2025.10 as packages/tokens SOURCE schema (3:1) with hand-written zero-dep emitters — Style Dictionary rejected in the contract path to protect byte-identity; DTCG export + round-trip via the existing DDR-172 import parser; (7) typecheck MATRIX incl. cloud/cells first type gate (wrangler types + tsc --noEmit; wrangler transpiles but never type-checks); (8) apps/cells converts to TS now (838 lines, tenant isolation — small and security-critical); (9) npm trusted publishing OIDC, NPM_TOKEN deleted; (10) scoped fast-check property tests (slug grammar, credential grammar, protocol schemas, token flattener — NOT the draw engine, render ladder is the better oracle); (11) local diagnostics with zero egress. Held: cli stays JSDoc checkJs .mjs (3:1 — only surface with commit==ship AND zero shipped breaks; isPkgRoot literal path in three languages) with three NAMED triggers (checkJs friction \u003e=3/30 files -\u003e Prettier shape; compiled-only distribution; first runtime dep); apps/cloud file renames behind the same trial; packages/ui + registry-internal held again with trust-boundary review added to the T19 go/no-go. Rejected 4-0: opt-out telemetry and auto crash-submit — privacy.mdx is a published promise, and the two flagship failures (v0.51.1 blank window, v0.38.0 hang) did not crash.","author":"1aGh","summary":"element debate-maturity-escalation; element debate-shared-layer-architecture","shapes":["el_234b3ea6f0f9ba14938f","el_7ca9429ad769eeff5204"],"targets":["el_7ca9429ad769eeff5204"],"mutations":[{"op":"upsert_element","element_id":"el_7ca9429ad769eeff5204","kind":"decision","name":"debate-maturity-escalation","props":{"confidence":"4","dissent":"All four seats, same risk in four voices: the detection layer must not displace the decomposition (round-1 gates-only degeneration, now with more gates). ADVOCATE: a permanently-red gate is worse protection than no gate — T0a suites must land non-blocking first. SHIPPER: an atomic gate the maintainer routes around under deadline is worse than none — break-glass must be documented and logged. BREAKER concession preserved: the tsconfig blindness may partly reflect intent (canvas tree compiles under the bundler) — fix may be a second tsconfig.canvas.json, but the surface must be checked somewhere.","seats":"BUILDER,SHIPPER,BREAKER,USER-ADVOCATE,RESEARCH-GUEST","shape":"divergent"}},{"op":"upsert_element","element_id":"el_234b3ea6f0f9ba14938f","kind":"decision","name":"debate-shared-layer-architecture"}]}}
{"hash":"sha256:7b629ddb6ad96b9cdde66d4083ff665a9a9901b2f8a395890a8ccd0d54c04d20","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":707,"recorded_at":"2026-08-05T00:00:00Z","parents":["sha256:471439d17a99938d0ebfc8b9d7a7ed08a8d449ae278dfa0c2b499e23e2087394"],"decision":{"id":"d_8436a36aa0da0573eeb35053","title":"Plan: desktop-cloud live pairing (approach C2)","rationale":"Unify the cloud browser surface and the desktop onto the hub Hocuspocus doc via the shared-doc single-Y.Doc model (DDR-064), enabled in the cell. C2 = a loopback, commit-disabled HocuspocusProvider in the studio child (threads DDR-209: loopback = no dial-out, autocommit-off = hub sole committer). Rejects B (studio child dials its own external hub — reverses DDR-209). C1 (re-home browser collab onto the hub) noted as the cleaner follow-up. Hard verification bar: shared-doc-convergence property suite + a live desktop+browser cross-surface run (presence + edits cross, reload loses nothing, exactly one committer).","author":"1aGh","summary":"element desktop-cloud-live-pairing; element maude; element dev; link plan:desktop-cloud-live-pairing-IN_REPO-\u003erepo:maude; link plan:desktop-cloud-live-pairing-IN_DEPT-\u003edept:dev","shapes":["el_0ff662ca396f44215079","el_42b070d9886acea59c36","el_ec5ef965b4979595c248"],"targets":["el_0ff662ca396f44215079"],"mutations":[{"op":"upsert_element","element_id":"el_0ff662ca396f44215079","kind":"plan","name":"desktop-cloud-live-pairing","props":{"path":".ai/plans/feature-desktop-cloud-live-pairing.md","status":"active"}},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_0ff662ca396f44215079","from_id":"el_0ff662ca396f44215079","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_0ff662ca396f44215079","from_id":"el_0ff662ca396f44215079","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"}]}}
{"hash":"sha256:74c4d3a164304bca76693caa3ec81dda4f3cef88a4e7eab1f013d912a269f43a","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":708,"recorded_at":"2026-08-06T00:00:00Z","parents":["sha256:7b629ddb6ad96b9cdde66d4083ff665a9a9901b2f8a395890a8ccd0d54c04d20"],"decision":{"id":"d_4ac468274c622da79146a090","title":"A cell syncs to itself, or to nothing","rationale":"# DDR-213: A cell syncs to itself, or to nothing\n\n**Date:** 2026-08-06\n**Status:** accepted\n**Tags:** cloud/cell/collab/crdt/yjs/shared-doc/autocommit/extends-ddr-064/extends-ddr-209\n**Supersedes:** —\n**Extends:** [DDR-064](DDR-064-single-shared-collab-doc.md), [DDR-209](DDR-209-one-studio-three-shells-the-cell-serves-the-studio.md)\n**Plan:** [`feature-desktop-cloud-live-pairing.md`](../../plans/feature-desktop-cloud-live-pairing.md)\n\n## Context\n\nCloud Phase 27 put the real studio inside the cell, and a member opening\n`\u003cproject\u003e.cloud.maude.sh` now loads the byte-identical client the desktop\nloads. What they do not get is the other person.\n\nA cell holds **two disjoint `Y.Doc` worlds**:\n\n| | who connects | where it lives |\n| --- | --- | --- |\n| the hub's Hocuspocus documents | the **desktop** (`linkedHub` → the cell) | hub process, SQLite-backed, durable |\n| the studio child's collab rooms | **browsers** (`/_ws/collab/\u003cslug\u003e`, proxied) | studio process, in memory |\n\nThe only link between them is one-way and not live: the hub's\n`afterStoreDocument` writes to disk and commits. Presence is ephemeral room\nstate and cannot cross a filesystem at all, and a browser edit reaches the\nstudio room and the disk but never the hub's document — so a desktop peer\nsubscribed to that document sees nothing. Two people on one project, one in the\napp and one in a tab, never meet. That is the headline of the cloud product,\nand it did not exist.\n\nThis is not an oversight. The hub was the sync brain first (Phase 9), the\nbrowser-in-the-cloud surface reused the dev-server's own rooms (Phase 27), and\n**DDR-209 / Phase 27 D2 deliberately forbids the studio child from syncing\nout** — honouring the tenant's `linkedHub` inside a cell would dial OUT to a\nthird-party hub carrying their canvases, and start a SECOND autocommit over the\nworking tree the hub is already committing. The islands were left as islands on\npurpose.\n\nSo the question this decides is not \"should they be joined\" but **\"can they be\njoined without reversing DDR-209\"**.\n\n## Decision\n\n**Unify at the hub, by letting the cell pair with ITSELF.** The studio child\nruns a `HocuspocusProvider` to its own cell hub over **loopback**, with\n`MAUDE_SHARED_DOC` on, so the browser room's `Y.Doc` **is** the document that\nsyncs to the hub — the single-shared-doc model of DDR-064, which was fully\nbuilt and shipped OFF behind a flag out of caution, not because of a known bug.\nThe browser and the desktop then converge on one document by CRDT merge, and\npresence crosses for free because the awareness bridged into the room is the\nprovider's.\n\nDDR-209's two fears are preserved **literally**, as conditions rather than as\nargument:\n\n1. **No dial-out** — the hub URL must resolve to a loopback host\n   (127.0.0.1 / localhost / ::1). Anything else is refused. A cell talking to\n   127.0.0.1 is a process talking to itself; nothing leaves the container.\n2. **No second committer** — autocommit must be explicitly disabled\n   (`MAUDE_SYNC_NO_AUTOCOMMIT=1`), and under pairing the autocommit object is\n   never *constructed*, so there is no later branch that could reach a commit.\n   The hub's `afterStoreDocument` remains the only committer.\n\nA third condition is ours and narrows further: **shared-doc must be on**.\nWithout it the loopback provider would open a second `Y.Doc` per canvas beside\nthe room's — the two-doc world this exists to collapse — and its agent would\nbecome a second doc→file writer for no benefit at all.\n\nThe tenant's `.design/config.json` supplies **exactly one** field: `workspaceId`.\nNot the URL — that would be the dial-out. But the workspace id decides the wire\ndocument name (DDR-192 §5), and the desktop resolves it from that same field; a\ncell that derived a different one would put the browser and the desktop on two\ndifferently-named documents and this whole feature would silently keep doing\nwhat it did before.\n\n### Why the credential is a minted store token and not `HUB_SECRET`\n\nThe studio child's environment is deliberately minimal so that a process\nhandling tenant-shaped requests does not hold the hub's admin bearer. Passing\n`HUB_SECRET` to buy a Yjs connection would trade away the reason that minimal\nenvironment exists. The hub mints a scope-`*` token into its own store at boot\n(`INSERT OR REPLACE` by label, so the previous value dies rather than\naccumulates) and the raw value exists only long enough to reach the child it is\nabout to spawn. It unlocks documents; it does not unlock `/internal/*` or the\nadmin API.\n\nScope `*` rather than a narrower binding because **a cell hub holds exactly one\ntenant** — the container boundary already is the scope. Narrowing further would\nmean guessing the wire namespace (which depends on the tenant's config and their\ncurrent branch) and silently failing to sync whenever the guess was wrong, which\nis the failure being fixed.\n\n### Rollout\n\n`CELL_LIVE_PAIRING` is a **per-tenant allowlist**, default off, `*` spelled out\nfor the fleet. This changes the CRDT layer, so it rolls to one pilot project\n(`alligators`), is watched, and only then widens. A fleet-wide boolean would\nmake the pilot step impossible to express. Env applies at container start, so\nchanging it needs a real stop → wake, not a config re-push.\n\n## What this cost elsewhere\n\nFour things surfaced while threading the needle, and each is part of the\ndecision rather than incidental:\n\n- **`.claudeignore` is not written in a cell.** The untrusted-canvas markers\n  exist for Claude Code reading the checkout, and nothing runs Claude against a\n  cell's tree. `.claudeignore` lives at the repo root, so writing it would put a\n  machine-authored file into the tenant's repository, which the hub would then\n  commit and mirror to their GitHub. The canvases are no less untrusted; the\n  audience for the marker is absent.\n- **A projection write announces itself.** In a container the recursive\n  `fs.watch` misses our atomic tmp+rename writes, so a peer's edit reached the\n  doc and the disk and then stopped: no `fs:any`, no `canvas-hmr`, and the other\n  person's canvas iframe stayed on the old render until they reloaded by hand.\n  `createContainerWriteBridge` closes that for API writes and cannot close it\n  here (it triggers off `activity:suppress`, which the projector never arms), so\n  the projector emits the event itself — delayed by the bridge's own margin, so\n  a watcher that *does* fire wins and the HMR broadcaster's per-file coalescing\n  collapses the pair into one reload.\n- **A projection write that changes nothing now costs nothing.** The `last*`\n  guards compare against what the projector wrote, which is null on the first\n  pass, so the cold-start `reconcile()` re-wrote every canvas with content\n  identical to what was already there. Harmless while nobody was listening; the\n  moment the write announced itself it became a spurious reload of every open\n  canvas at boot.\n- **`sync/limits.ts` exists.** The byte ceilings lived in `sync/codec.ts`, which\n  imports `Y_TYPES` from `collab/persistence.ts`. The moment persistence needed\n  a ceiling of its own that became an import cycle whose only symptom would have\n  been a `const` read in its temporal dead zone — a crash whose stack points at\n  neither file. Numbers depend on nothing, so they now live where nothing has to\n  depend back.\n\n## The DDR-064 pre-cutover checklist, closed\n\nShared-doc shipped OFF behind an unfinished checklist. Turning it on for cells\nmeans closing it, so:\n\n| Item | Verdict |\n| --- | --- |\n| **A1** — gate `.html` bodies like `.tsx` | **Moot, recorded.** Since Phase 3.6 `.tsx` is the only canvas format; discovery admits `.html` for legacy compatibility and no real project has one. Left as a known residual rather than pretended away. |\n| **A4** — detect slug collisions | **Closed.** `slugFor` flattens `/` to `-`, so `ui/a/b.tsx` and `ui/a-b.tsx` produce one slug. Two files on one document is not degradation, it is each writing the other's body over itself forever. **Both** are excluded, not one picked: refusing to sync two canvases is recoverable by renaming; overwriting one with the other is not. |\n| **A6** — cap pinned rooms | **Closed.** A shared-doc canvas is a pinned room, deliberately immune to the last-browser-leaves drop — which is exactly why nothing else will ever reclaim it. Ceiling 500 (`MAUDE_MAX_PINNED_ROOMS`), far above the largest in-house project at 83, so it is a runaway guard rather than a product limit. |\n| **A7** — one-time consent notice | **Closed.** A one-time warn whenever shared-doc engages against a hub. A cell is exempt **and that is the decision**: the operator turned pairing on per project, the hub is the cell's own loopback, nothing leaves the container — consent was given by configuration, and repeating it per canvas boot trains an operator to skip the line that matters. |\n| **comments hub→disk lane** | **Closed.** The codec capped file→doc; this is the other direction, and until shared-doc it barely mattered because the room's doc was populated only by browsers on this machine. Now it is populated by the hub. Same ceilings, so a value that could never be imported can never be written either. The write is refused, not the sync — the doc keeps converging. |\n| **`@hocuspocus/provider` advisories** | **Closed.** 4.3.0, current major; `bun audit` reports nothing against `@hocuspocus/*` or `yjs`. (Nine unrelated transitive findings exist in the ACP SDK and `dom-to-svg` chains — pre-existing, off the sync path.) |\n\n## Alternatives rejected\n\n- **C1 — re-home the browser's collab onto the hub's Hocuspocus directly** (the\n  proxy stops forwarding `/_ws/collab`; the hub serves it). Cleaner: one doc, no\n  bridge, no second process in the loop. Rejected **for now, not on the merits**\n  — it requires re-homing the room's other lanes (comments, annotations,\n  activity) onto the hub doc, which is a large structural change to make on the\n  strength of a model that has never run in production. C2 is the reversible\n  pilot that earns the right to do C1; the follow-up is noted, not abandoned.\n- **B — the studio child dials its own external hub / the tenant's\n  `linkedHub`.** This is the thing DDR-209 forbids, wearing a feature's clothes:\n  dial-out plus a second autocommit. Off the table.\n- **Let the hub remain the only doc→file writer and have the studio skip its\n  projection.** Tempting, since the hub already writes on `afterStoreDocument`\n  through a twin of the same merge (`meta-merge.mjs`, held byte-identical by\n  test). Rejected because the ordering then stops being ours: the studio would\n  have to announce a reload for a file another process had not necessarily\n  written yet. Both writers producing identical bytes is redundant and cheap;\n  the echo guard drops the second event and the HMR debounce collapses the pair.\n\n## Consequences\n\n- **Attribution changes for browser-originated edits.** They now reach the hub\n  over the loopback connection, so `afterStoreDocument` attributes the commit to\n  the pairing token rather than to the member. The token is minted with **no\n  owner** deliberately — signing a tenant's git history with a machine identity\n  dressed up as a person is worse than an honest \"unknown\". Carrying the real\n  editor across the loopback lane is follow-up work, and it is the first thing\n  C1 would fix for free.\n- Beyond the cell, nothing moves: `MAUDE_SHARED_DOC` stays off by default for\n  desktop and self-hosted hubs, and the guard is inert outside workspace mode.\n- The live cross-surface run (two surfaces, cursors both ways, reload loses\n  nothing, exactly one committer in `git log`) is the gate this decision is\n  conditional on. Passing tests are the floor, not the proof.","author":"1aGh","summary":"element cloud; element DDR-213; link decision:DDR-213-ABOUT-\u003earea:cloud; element maude; link decision:DDR-213-IN_REPO-\u003erepo:maude; element dev; link decision:DDR-213-IN_DEPT-\u003edept:dev; element cell; link area:cloud-TOUCHES-\u003etopic:cell; element collab; link area:cloud-TOUCHES-\u003etopic:collab; element crdt; link area:cloud-TOUCHES-\u003etopic:crdt; element yjs; link area:cloud-TOUCHES-\u003etopic:yjs; element shared-doc; link area:cloud-TOUCHES-\u003etopic:shared-doc; element DDR-192; link decision:DDR-213-REFERENCES-\u003edecision:DDR-192; element DDR-209; link decision:DDR-213-EXTENDS-\u003edecision:DDR-209; element DDR-064; link decision:DDR-213-EXTENDS-\u003edecision:DDR-064","supersedes":["d_bb2a8e0852a9a8c8ae5d54a4"],"shapes":["el_089d057c99dcc24edad9","el_251c410de3233a1aeb10","el_42b070d9886acea59c36","el_4f650e68f97a1dabfe5b","el_992639d27bb455fc2acd","el_9afa54b829907b2c7cc5","el_bd50734bc195278caee8","el_c9d85ccf509eec6f59d7","el_ca487ac52d9fa289ed45","el_cb925bf1cd97e4c560f6","el_e28dc22ac3959e86511c","el_ec5ef965b4979595c248"],"targets":["el_251c410de3233a1aeb10","el_992639d27bb455fc2acd","el_ca487ac52d9fa289ed45"],"mutations":[{"op":"upsert_element","element_id":"el_ca487ac52d9fa289ed45","kind":"area","name":"cloud","props":{"last_ddr":"DDR-213"}},{"op":"upsert_element","element_id":"el_251c410de3233a1aeb10","kind":"decision","name":"DDR-213","props":{"date":"2026-08-06","path":".ai/archive/decisions/DDR-213-cell-pairs-with-itself-loopback-shared-doc.md","tags":"cloud,cell,collab,crdt,yjs,shared-doc","title":"A cell syncs to itself, or to nothing"}},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_ca487ac52d9fa289ed45","link_kind":"ABOUT"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_cb925bf1cd97e4c560f6","kind":"topic","name":"cell"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_cb925bf1cd97e4c560f6","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_089d057c99dcc24edad9","kind":"topic","name":"collab"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_089d057c99dcc24edad9","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_e28dc22ac3959e86511c","kind":"topic","name":"crdt"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_e28dc22ac3959e86511c","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_9afa54b829907b2c7cc5","kind":"topic","name":"yjs"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_9afa54b829907b2c7cc5","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_992639d27bb455fc2acd","kind":"topic","name":"shared-doc"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_992639d27bb455fc2acd","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_bd50734bc195278caee8","kind":"decision","name":"DDR-192"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_bd50734bc195278caee8","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_c9d85ccf509eec6f59d7","kind":"decision","name":"DDR-209"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_c9d85ccf509eec6f59d7","link_kind":"EXTENDS"},{"op":"upsert_element","element_id":"el_4f650e68f97a1dabfe5b","kind":"decision","name":"DDR-064"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_4f650e68f97a1dabfe5b","link_kind":"EXTENDS"}]}}
{"hash":"sha256:74f07d3f2ecd88a60d14fc844eee86012306443891ce4ae339bf6fc6c1c34626","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":709,"recorded_at":"2026-08-06T00:00:00Z","parents":["sha256:74c4d3a164304bca76693caa3ec81dda4f3cef88a4e7eab1f013d912a269f43a"],"decision":{"id":"d_a6ed52dc3904facba50f38e9","title":"A cell syncs to itself, or to nothing","rationale":"# DDR-213: A cell syncs to itself, or to nothing\n\n**Date:** 2026-08-06\n**Status:** accepted\n**Tags:** cloud/cell/collab/crdt/yjs/shared-doc/autocommit/extends-ddr-064/extends-ddr-209\n**Supersedes:** —\n**Extends:** [DDR-064](DDR-064-single-shared-collab-doc.md), [DDR-209](DDR-209-one-studio-three-shells-the-cell-serves-the-studio.md)\n**Plan:** [`feature-desktop-cloud-live-pairing.md`](../../plans/feature-desktop-cloud-live-pairing.md)\n\n## Context\n\nCloud Phase 27 put the real studio inside the cell, and a member opening\n`\u003cproject\u003e.cloud.maude.sh` now loads the byte-identical client the desktop\nloads. What they do not get is the other person.\n\nA cell holds **two disjoint `Y.Doc` worlds**:\n\n| | who connects | where it lives |\n| --- | --- | --- |\n| the hub's Hocuspocus documents | the **desktop** (`linkedHub` → the cell) | hub process, SQLite-backed, durable |\n| the studio child's collab rooms | **browsers** (`/_ws/collab/\u003cslug\u003e`, proxied) | studio process, in memory |\n\nThe only link between them is one-way and not live: the hub's\n`afterStoreDocument` writes to disk and commits. Presence is ephemeral room\nstate and cannot cross a filesystem at all, and a browser edit reaches the\nstudio room and the disk but never the hub's document — so a desktop peer\nsubscribed to that document sees nothing. Two people on one project, one in the\napp and one in a tab, never meet. That is the headline of the cloud product,\nand it did not exist.\n\nThis is not an oversight. The hub was the sync brain first (Phase 9), the\nbrowser-in-the-cloud surface reused the dev-server's own rooms (Phase 27), and\n**DDR-209 / Phase 27 D2 deliberately forbids the studio child from syncing\nout** — honouring the tenant's `linkedHub` inside a cell would dial OUT to a\nthird-party hub carrying their canvases, and start a SECOND autocommit over the\nworking tree the hub is already committing. The islands were left as islands on\npurpose.\n\nSo the question this decides is not \"should they be joined\" but **\"can they be\njoined without reversing DDR-209\"**.\n\n## Decision\n\n**Unify at the hub, by letting the cell pair with ITSELF.** The studio child\nruns a `HocuspocusProvider` to its own cell hub over **loopback**, with\n`MAUDE_SHARED_DOC` on, so the browser room's `Y.Doc` **is** the document that\nsyncs to the hub — the single-shared-doc model of DDR-064, which was fully\nbuilt and shipped OFF behind a flag out of caution, not because of a known bug.\nThe browser and the desktop then converge on one document by CRDT merge, and\npresence crosses for free because the awareness bridged into the room is the\nprovider's.\n\nDDR-209's two fears are preserved **literally**, as conditions rather than as\nargument:\n\n1. **No dial-out** — the hub URL must resolve to a loopback host\n   (127.0.0.1 / localhost / ::1). Anything else is refused. A cell talking to\n   127.0.0.1 is a process talking to itself; nothing leaves the container.\n2. **No second committer** — autocommit must be explicitly disabled\n   (`MAUDE_SYNC_NO_AUTOCOMMIT=1`), and under pairing the autocommit object is\n   never *constructed*, so there is no later branch that could reach a commit.\n   The hub's `afterStoreDocument` remains the only committer.\n\nA third condition is ours and narrows further: **shared-doc must be on**.\nWithout it the loopback provider would open a second `Y.Doc` per canvas beside\nthe room's — the two-doc world this exists to collapse — and its agent would\nbecome a second doc→file writer for no benefit at all.\n\nThe tenant's `.design/config.json` supplies **exactly one** field: `workspaceId`.\nNot the URL — that would be the dial-out. But the workspace id decides the wire\ndocument name (DDR-192 §5), and the desktop resolves it from that same field; a\ncell that derived a different one would put the browser and the desktop on two\ndifferently-named documents and this whole feature would silently keep doing\nwhat it did before.\n\n### Why the credential is a minted store token and not `HUB_SECRET`\n\nThe studio child's environment is deliberately minimal so that a process\nhandling tenant-shaped requests does not hold the hub's admin bearer. Passing\n`HUB_SECRET` to buy a Yjs connection would trade away the reason that minimal\nenvironment exists. The hub mints a scope-`*` token into its own store at boot\n(`INSERT OR REPLACE` by label, so the previous value dies rather than\naccumulates) and the raw value exists only long enough to reach the child it is\nabout to spawn. It unlocks documents; it does not unlock `/internal/*` or the\nadmin API.\n\nScope `*` rather than a narrower binding because **a cell hub holds exactly one\ntenant** — the container boundary already is the scope. Narrowing further would\nmean guessing the wire namespace (which depends on the tenant's config and their\ncurrent branch) and silently failing to sync whenever the guess was wrong, which\nis the failure being fixed.\n\n### Rollout\n\n`CELL_LIVE_PAIRING` is a **per-tenant allowlist**, default off, `*` spelled out\nfor the fleet. This changes the CRDT layer, so it rolls to one pilot project\n(`alligators`), is watched, and only then widens. A fleet-wide boolean would\nmake the pilot step impossible to express. Env applies at container start, so\nchanging it needs a real stop → wake, not a config re-push.\n\n## What this cost elsewhere\n\nFour things surfaced while threading the needle, and each is part of the\ndecision rather than incidental:\n\n- **`.claudeignore` is not written in a cell.** The untrusted-canvas markers\n  exist for Claude Code reading the checkout, and nothing runs Claude against a\n  cell's tree. `.claudeignore` lives at the repo root, so writing it would put a\n  machine-authored file into the tenant's repository, which the hub would then\n  commit and mirror to their GitHub. The canvases are no less untrusted; the\n  audience for the marker is absent.\n- **A projection write announces itself.** In a container the recursive\n  `fs.watch` misses our atomic tmp+rename writes, so a peer's edit reached the\n  doc and the disk and then stopped: no `fs:any`, no `canvas-hmr`, and the other\n  person's canvas iframe stayed on the old render until they reloaded by hand.\n  `createContainerWriteBridge` closes that for API writes and cannot close it\n  here (it triggers off `activity:suppress`, which the projector never arms), so\n  the projector emits the event itself — delayed by the bridge's own margin, so\n  a watcher that *does* fire wins and the HMR broadcaster's per-file coalescing\n  collapses the pair into one reload.\n- **A projection write that changes nothing now costs nothing.** The `last*`\n  guards compare against what the projector wrote, which is null on the first\n  pass, so the cold-start `reconcile()` re-wrote every canvas with content\n  identical to what was already there. Harmless while nobody was listening; the\n  moment the write announced itself it became a spurious reload of every open\n  canvas at boot.\n- **`sync/limits.ts` exists.** The byte ceilings lived in `sync/codec.ts`, which\n  imports `Y_TYPES` from `collab/persistence.ts`. The moment persistence needed\n  a ceiling of its own that became an import cycle whose only symptom would have\n  been a `const` read in its temporal dead zone — a crash whose stack points at\n  neither file. Numbers depend on nothing, so they now live where nothing has to\n  depend back.\n\n## The DDR-064 pre-cutover checklist, closed\n\nShared-doc shipped OFF behind an unfinished checklist. Turning it on for cells\nmeans closing it, so:\n\n| Item | Verdict |\n| --- | --- |\n| **A1** — gate `.html` bodies like `.tsx` | **Moot, recorded.** Since Phase 3.6 `.tsx` is the only canvas format; discovery admits `.html` for legacy compatibility and no real project has one. Left as a known residual rather than pretended away. |\n| **A4** — detect slug collisions | **Closed.** `slugFor` flattens `/` to `-`, so `ui/a/b.tsx` and `ui/a-b.tsx` produce one slug. Two files on one document is not degradation, it is each writing the other's body over itself forever. **Both** are excluded, not one picked: refusing to sync two canvases is recoverable by renaming; overwriting one with the other is not. |\n| **A6** — cap pinned rooms | **Closed.** A shared-doc canvas is a pinned room, deliberately immune to the last-browser-leaves drop — which is exactly why nothing else will ever reclaim it. Ceiling 500 (`MAUDE_MAX_PINNED_ROOMS`), far above the largest in-house project at 83, so it is a runaway guard rather than a product limit. |\n| **A7** — one-time consent notice | **Closed.** A one-time warn whenever shared-doc engages against a hub. A cell is exempt **and that is the decision**: the operator turned pairing on per project, the hub is the cell's own loopback, nothing leaves the container — consent was given by configuration, and repeating it per canvas boot trains an operator to skip the line that matters. |\n| **comments hub→disk lane** | **Closed.** The codec capped file→doc; this is the other direction, and until shared-doc it barely mattered because the room's doc was populated only by browsers on this machine. Now it is populated by the hub. Same ceilings, so a value that could never be imported can never be written either. The write is refused, not the sync — the doc keeps converging. |\n| **`@hocuspocus/provider` advisories** | **Closed.** 4.3.0, current major; `bun audit` reports nothing against `@hocuspocus/*` or `yjs`. (Nine unrelated transitive findings exist in the ACP SDK and `dom-to-svg` chains — pre-existing, off the sync path.) |\n\n## Alternatives rejected\n\n- **C1 — re-home the browser's collab onto the hub's Hocuspocus directly** (the\n  proxy stops forwarding `/_ws/collab`; the hub serves it). Cleaner: one doc, no\n  bridge, no second process in the loop. Rejected **for now, not on the merits**\n  — it requires re-homing the room's other lanes (comments, annotations,\n  activity) onto the hub doc, which is a large structural change to make on the\n  strength of a model that has never run in production. C2 is the reversible\n  pilot that earns the right to do C1; the follow-up is noted, not abandoned.\n- **B — the studio child dials its own external hub / the tenant's\n  `linkedHub`.** This is the thing DDR-209 forbids, wearing a feature's clothes:\n  dial-out plus a second autocommit. Off the table.\n- **Let the hub remain the only doc→file writer and have the studio skip its\n  projection.** Tempting, since the hub already writes on `afterStoreDocument`\n  through a twin of the same merge (`meta-merge.mjs`, held byte-identical by\n  test). Rejected because the ordering then stops being ours: the studio would\n  have to announce a reload for a file another process had not necessarily\n  written yet. Both writers producing identical bytes is redundant and cheap;\n  the echo guard drops the second event and the HMR debounce collapses the pair.\n\n## Consequences\n\n- **Attribution changes for browser-originated edits.** They now reach the hub\n  over the loopback connection, so `afterStoreDocument` attributes the commit to\n  the pairing token rather than to the member. The token is minted with **no\n  owner** deliberately — signing a tenant's git history with a machine identity\n  dressed up as a person is worse than an honest \"unknown\". Carrying the real\n  editor across the loopback lane is follow-up work, and it is the first thing\n  C1 would fix for free.\n- Beyond the cell, nothing moves: `MAUDE_SHARED_DOC` stays off by default for\n  desktop and self-hosted hubs, and the guard is inert outside workspace mode.\n- The live cross-surface run (two surfaces, cursors both ways, reload loses\n  nothing, exactly one committer in `git log`) is the gate this decision is\n  conditional on. Passing tests are the floor, not the proof.\n\n## Security review (2026-08-06, `/flow:validate`)\n\nDefender + adversarial passes ran against this diff\n(`.ai/logs/security-reviews/main-20260806-0554-defender.md` — 0 blockers, 4\nwarnings; `main-20260806-0600-attacker.md` — the chain below). Both\nindependently re-derived every claim in this DDR from the code rather than\ntaking it on faith, and both are worth reading in full; this section records\nwhat changed as a result and, honestly, what did not.\n\n**Fixed on the spot:** four log lines (`cell-pairing.ts` ×2,\n`sync/index.ts`, `studio-child.mjs`) interpolated `MAUDE_LOOPBACK_SYNC_URL`\nwithout stripping control characters, unlike the codebase's own\n`sanitizeForLog` precedent (`apps/hub/src/server.mjs`). Exploitability was\nalready near-zero — that env var is hub-set only, never tenant-reachable —\nbut a feature whose entire argument is \"the URL is provably loopback\" should\nnot be the one place still trusting the string past its hostname. All four\nnow scrub before logging (mirrored, not shared, per the same\ndeliberate-duplication reasoning as the loopback checks themselves —\n`cell-pairing.ts` and `studio-child.mjs` cannot import from each other\nwithout a cycle or an app boundary crossing).\n\n**Named, not fixed — a wildcard, non-expiring pairing token.**\n`mintLoopbackSyncToken` mints `scope: '*'`, no `owner`, no `expiresAt`. The\nattacker's chain (Chain 1, promoted to High, impact-conditional — no leak\ndemonstrated in this diff) composes that with the defender's own finding that\nthe scope is confined to one tenant *only* by the \"one hub process ⇒ one\ntenant\" architectural invariant, never by the token itself: if the raw value\never left the studio child's process environment by any channel, it would be\na full-project bearer credential, usable against the tenant's own public hub\nendpoint, valid until the next hub restart re-mints it by label.\n\nVerified before deciding, not assumed: `HUB_SECRET` — the hub's existing\nadmin escape-hatch, already in production — has the **identical** shape\n(`server.mjs`: `scope: '*'`, no `expiresAt`, no network-origin binding in\n`onAuthenticate`), and is *more* privileged than this token (it also reaches\n`/internal/*` and the admin API; the pairing token reaches documents only).\nThis diff does not introduce a new class of risk — it adds one narrower\ninstance of a bearer-credential shape this exact server already accepts for\nits most powerful credential. That is why this is recorded as a residual\nrather than treated as a blocker.\n\n**Why the two stronger mitigations the attacker recommended are follow-up\nwork, not today's fix:**\n- *Bind `onAuthenticate` to the token's `remoteAddress` for this one label.*\n  Sound in principle, symmetric with the two existing egress checks. Not\n  implemented because whether a raw `request.socket.remoteAddress` check\n  actually distinguishes \"the studio child's genuine loopback connection\"\n  from \"external traffic that arrived via the cell's own Cloudflare tunnel\"\n  depends on cloudflared's ingress configuration (server-side, not visible in\n  this repo) — if the tunnel proxies to `127.0.0.1:\u003cport\u003e` (a common\n  configuration), *every* external request would also present as loopback at\n  this exact check, making it a false safety net rather than a real one. A\n  control that creates false confidence is worse than no control; this needs\n  the actual ingress topology confirmed before it is written, not guessed.\n- *Give the token a short `expiresAt` and re-mint on a timer.* The mint\n  happens once, at hub boot; the token then rides into an already-spawned\n  child's environment, which cannot be updated without either restarting the\n  child or adding a live-refresh side-channel — neither of which exists\n  today. Adding an expiry without a refresh path would not reduce the leak\n  window; it would silently break live pairing on every cell that outlives\n  the TTL without a restart, trading a theoretical, undemonstrated risk for a\n  guaranteed operational regression on the pilot project. This needs a real\n  refresh mechanism designed alongside it, not a bolted-on deadline.\n- *Scope the token to `ws/\u003cworkspaceId\u003e/\u003cbranch\u003e/` instead of `*`.* The\n  prefix is only computable from the tenant's own `.design/config.json`,\n  which the studio reads and the hub deliberately does not (the hub minting a\n  token would need to either read the tenant's file itself — a new coupling —\n  or accept the prefix from the studio child post-boot — a new handshake).\n  Smaller than the other two, still a real design change, not a review-pass\n  patch.\n\nTracked as explicit follow-up, alongside C1 (which would remove this\ncredential class entirely by re-homing browser collab directly onto the hub\n— no loopback hop, no minted token, nothing to leak).","author":"1aGh","summary":"element cloud; element DDR-213; link decision:DDR-213-ABOUT-\u003earea:cloud; element maude; link decision:DDR-213-IN_REPO-\u003erepo:maude; element dev; link decision:DDR-213-IN_DEPT-\u003edept:dev; element cell; link area:cloud-TOUCHES-\u003etopic:cell; element collab; link area:cloud-TOUCHES-\u003etopic:collab; element crdt; link area:cloud-TOUCHES-\u003etopic:crdt; element yjs; link area:cloud-TOUCHES-\u003etopic:yjs; element shared-doc; link area:cloud-TOUCHES-\u003etopic:shared-doc; element DDR-192; link decision:DDR-213-REFERENCES-\u003edecision:DDR-192; element DDR-209; link decision:DDR-213-EXTENDS-\u003edecision:DDR-209; element DDR-064; link decision:DDR-213-EXTENDS-\u003edecision:DDR-064","supersedes":["d_4ac468274c622da79146a090"],"shapes":["el_089d057c99dcc24edad9","el_251c410de3233a1aeb10","el_42b070d9886acea59c36","el_4f650e68f97a1dabfe5b","el_992639d27bb455fc2acd","el_9afa54b829907b2c7cc5","el_bd50734bc195278caee8","el_c9d85ccf509eec6f59d7","el_ca487ac52d9fa289ed45","el_cb925bf1cd97e4c560f6","el_e28dc22ac3959e86511c","el_ec5ef965b4979595c248"],"targets":["el_251c410de3233a1aeb10","el_ca487ac52d9fa289ed45"],"mutations":[{"op":"upsert_element","element_id":"el_ca487ac52d9fa289ed45","kind":"area","name":"cloud","props":{"last_ddr":"DDR-213"}},{"op":"upsert_element","element_id":"el_251c410de3233a1aeb10","kind":"decision","name":"DDR-213","props":{"date":"2026-08-06","path":".ai/archive/decisions/DDR-213-cell-pairs-with-itself-loopback-shared-doc.md","tags":"cloud,cell,collab,crdt,yjs,shared-doc","title":"A cell syncs to itself, or to nothing"}},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_ca487ac52d9fa289ed45","link_kind":"ABOUT"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_cb925bf1cd97e4c560f6","kind":"topic","name":"cell"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_cb925bf1cd97e4c560f6","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_089d057c99dcc24edad9","kind":"topic","name":"collab"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_089d057c99dcc24edad9","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_e28dc22ac3959e86511c","kind":"topic","name":"crdt"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_e28dc22ac3959e86511c","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_9afa54b829907b2c7cc5","kind":"topic","name":"yjs"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_9afa54b829907b2c7cc5","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_992639d27bb455fc2acd","kind":"topic","name":"shared-doc"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_992639d27bb455fc2acd","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_bd50734bc195278caee8","kind":"decision","name":"DDR-192"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_bd50734bc195278caee8","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_c9d85ccf509eec6f59d7","kind":"decision","name":"DDR-209"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_c9d85ccf509eec6f59d7","link_kind":"EXTENDS"},{"op":"upsert_element","element_id":"el_4f650e68f97a1dabfe5b","kind":"decision","name":"DDR-064"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_4f650e68f97a1dabfe5b","link_kind":"EXTENDS"}]}}
{"hash":"sha256:9a8825505cdeb3bb209891dc3b216b761fd64a76e6ec9c7583fe829b316d95e4","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":710,"recorded_at":"2026-08-06T00:00:00Z","parents":["sha256:74f07d3f2ecd88a60d14fc844eee86012306443891ce4ae339bf6fc6c1c34626"],"decision":{"id":"d_c5b561ac6bd7e2249e51bcf0","title":"A cell syncs to itself, or to nothing","rationale":"# DDR-213: A cell syncs to itself, or to nothing\n\n**Date:** 2026-08-06\n**Status:** accepted\n**Tags:** cloud/cell/collab/crdt/yjs/shared-doc/autocommit/extends-ddr-064/extends-ddr-209\n**Supersedes:** —\n**Extends:** [DDR-064](DDR-064-single-shared-collab-doc.md), [DDR-209](DDR-209-one-studio-three-shells-the-cell-serves-the-studio.md)\n**Plan:** [`feature-desktop-cloud-live-pairing.md`](../../plans/feature-desktop-cloud-live-pairing.md)\n\n## Context\n\nCloud Phase 27 put the real studio inside the cell, and a member opening\n`\u003cproject\u003e.cloud.maude.sh` now loads the byte-identical client the desktop\nloads. What they do not get is the other person.\n\nA cell holds **two disjoint `Y.Doc` worlds**:\n\n| | who connects | where it lives |\n| --- | --- | --- |\n| the hub's Hocuspocus documents | the **desktop** (`linkedHub` → the cell) | hub process, SQLite-backed, durable |\n| the studio child's collab rooms | **browsers** (`/_ws/collab/\u003cslug\u003e`, proxied) | studio process, in memory |\n\nThe only link between them is one-way and not live: the hub's\n`afterStoreDocument` writes to disk and commits. Presence is ephemeral room\nstate and cannot cross a filesystem at all, and a browser edit reaches the\nstudio room and the disk but never the hub's document — so a desktop peer\nsubscribed to that document sees nothing. Two people on one project, one in the\napp and one in a tab, never meet. That is the headline of the cloud product,\nand it did not exist.\n\nThis is not an oversight. The hub was the sync brain first (Phase 9), the\nbrowser-in-the-cloud surface reused the dev-server's own rooms (Phase 27), and\n**DDR-209 / Phase 27 D2 deliberately forbids the studio child from syncing\nout** — honouring the tenant's `linkedHub` inside a cell would dial OUT to a\nthird-party hub carrying their canvases, and start a SECOND autocommit over the\nworking tree the hub is already committing. The islands were left as islands on\npurpose.\n\nSo the question this decides is not \"should they be joined\" but **\"can they be\njoined without reversing DDR-209\"**.\n\n## Decision\n\n**Unify at the hub, by letting the cell pair with ITSELF.** The studio child\nruns a `HocuspocusProvider` to its own cell hub over **loopback**, with\n`MAUDE_SHARED_DOC` on, so the browser room's `Y.Doc` **is** the document that\nsyncs to the hub — the single-shared-doc model of DDR-064, which was fully\nbuilt and shipped OFF behind a flag out of caution, not because of a known bug.\nThe browser and the desktop then converge on one document by CRDT merge, and\npresence crosses for free because the awareness bridged into the room is the\nprovider's.\n\nDDR-209's two fears are preserved **literally**, as conditions rather than as\nargument:\n\n1. **No dial-out** — the hub URL must resolve to a loopback host\n   (127.0.0.1 / localhost / ::1). Anything else is refused. A cell talking to\n   127.0.0.1 is a process talking to itself; nothing leaves the container.\n2. **No second committer** — autocommit must be explicitly disabled\n   (`MAUDE_SYNC_NO_AUTOCOMMIT=1`), and under pairing the autocommit object is\n   never *constructed*, so there is no later branch that could reach a commit.\n   The hub's `afterStoreDocument` remains the only committer.\n\nA third condition is ours and narrows further: **shared-doc must be on**.\nWithout it the loopback provider would open a second `Y.Doc` per canvas beside\nthe room's — the two-doc world this exists to collapse — and its agent would\nbecome a second doc→file writer for no benefit at all.\n\nThe tenant's `.design/config.json` supplies **exactly one** field: `workspaceId`.\nNot the URL — that would be the dial-out. But the workspace id decides the wire\ndocument name (DDR-192 §5), and the desktop resolves it from that same field; a\ncell that derived a different one would put the browser and the desktop on two\ndifferently-named documents and this whole feature would silently keep doing\nwhat it did before.\n\n### Why the credential is a minted store token and not `HUB_SECRET`\n\nThe studio child's environment is deliberately minimal so that a process\nhandling tenant-shaped requests does not hold the hub's admin bearer. Passing\n`HUB_SECRET` to buy a Yjs connection would trade away the reason that minimal\nenvironment exists. The hub mints a scope-`*` token into its own store at boot\n(`INSERT OR REPLACE` by label, so the previous value dies rather than\naccumulates) and the raw value exists only long enough to reach the child it is\nabout to spawn. It unlocks documents; it does not unlock `/internal/*` or the\nadmin API.\n\nScope `*` rather than a narrower binding because **a cell hub holds exactly one\ntenant** — the container boundary already is the scope. Narrowing further would\nmean guessing the wire namespace (which depends on the tenant's config and their\ncurrent branch) and silently failing to sync whenever the guess was wrong, which\nis the failure being fixed.\n\n### Rollout\n\n`CELL_LIVE_PAIRING` is a **per-tenant allowlist**, default off, `*` spelled out\nfor the fleet. This changes the CRDT layer, so it rolls to one pilot project\n(`alligators`), is watched, and only then widens. A fleet-wide boolean would\nmake the pilot step impossible to express. Env applies at container start, so\nchanging it needs a real stop → wake, not a config re-push.\n\n## What this cost elsewhere\n\nFour things surfaced while threading the needle, and each is part of the\ndecision rather than incidental:\n\n- **`.claudeignore` is not written in a cell.** The untrusted-canvas markers\n  exist for Claude Code reading the checkout, and nothing runs Claude against a\n  cell's tree. `.claudeignore` lives at the repo root, so writing it would put a\n  machine-authored file into the tenant's repository, which the hub would then\n  commit and mirror to their GitHub. The canvases are no less untrusted; the\n  audience for the marker is absent.\n- **A projection write announces itself.** In a container the recursive\n  `fs.watch` misses our atomic tmp+rename writes, so a peer's edit reached the\n  doc and the disk and then stopped: no `fs:any`, no `canvas-hmr`, and the other\n  person's canvas iframe stayed on the old render until they reloaded by hand.\n  `createContainerWriteBridge` closes that for API writes and cannot close it\n  here (it triggers off `activity:suppress`, which the projector never arms), so\n  the projector emits the event itself — delayed by the bridge's own margin, so\n  a watcher that *does* fire wins and the HMR broadcaster's per-file coalescing\n  collapses the pair into one reload. **Keyed by path** (a `Map`, not a flat\n  timer bag): three independent `/simplify` review passes (efficiency, reuse,\n  altitude) converged on the same gap in the first version of this — two\n  writes to the same file inside the 250ms delay window scheduled two\n  independent timers and emitted `fs:any` twice, which is two reloads for one\n  edit, exactly the failure this mechanism exists to prevent. The three passes\n  also proposed routing through `createContainerWriteBridge` itself (arm\n  `activity:suppress` from the projector) instead of a parallel mechanism —\n  sound in principle, not taken: that bridge is instantiated in `ws.ts` at\n  server boot, and coupling the sync runtime to it would make the runtime\n  untestable standalone, which every test in\n  `test/shared-doc-cell-pairing.test.ts` currently relies on. Duplicating the\n  small delay-then-emit shape, with the same per-path coalescing, keeps both\n  properties.\n- **A projection write that changes nothing now costs nothing.** The `last*`\n  guards compare against what the projector wrote, which is null on the first\n  pass, so the cold-start `reconcile()` re-wrote every canvas with content\n  identical to what was already there. Harmless while nobody was listening; the\n  moment the write announced itself it became a spurious reload of every open\n  canvas at boot.\n- **`sync/limits.ts` exists**, and grew a second job. The byte ceilings lived\n  in `sync/codec.ts`, which imports `Y_TYPES` from `collab/persistence.ts`. The\n  moment persistence needed a ceiling of its own that became an import cycle\n  whose only symptom would have been a `const` read in its temporal dead zone —\n  a crash whose stack points at neither file. Numbers depend on nothing, so\n  they now live where nothing has to depend back. The doc→disk cap-and-warn\n  CHECK itself (not just the numbers) was independently re-typed in both\n  `sync/projection.ts` and `collab/persistence.ts` in this same diff before a\n  `/simplify` reuse pass caught it; both now call one `withinByteCap` in\n  `limits.ts`, parameterized by a log-line label so each subsystem's warning\n  stays identifiable as to which one refused.\n- **`sync/loopback.ts` exists**, for the same reason a third time. The\n  loopback-host predicate was independently re-typed in THREE places inside\n  `apps/studio/sync/` — `checkUrlScheme`, `isLoopbackHubUrl`, and\n  `cell-pairing.ts`'s own copy, whose comment said out loud \"three places\n  asking is this loopback and disagreeing is how a guard becomes decorative\"\n  without fixing it. Now one function, imported by the two files that need it.\n  `apps/hub/src/log-safety.mjs` is the hub-side equivalent, for the SAME-app\n  duplication between `server.mjs` and `studio-child.mjs` (`isLoopbackSyncUrl`\n  and `sanitizeForLog` were byte-for-byte copies with no cycle risk once\n  extracted). The CROSS-app duplication between `cell-pairing.ts` and\n  `studio-child.mjs` stays two independent checks, deliberately — see Task 1's\n  own reasoning above; a `/simplify` reuse pass agreed and did not flag it.\n\n## The DDR-064 pre-cutover checklist, closed\n\nShared-doc shipped OFF behind an unfinished checklist. Turning it on for cells\nmeans closing it, so:\n\n| Item | Verdict |\n| --- | --- |\n| **A1** — gate `.html` bodies like `.tsx` | **Moot, recorded.** Since Phase 3.6 `.tsx` is the only canvas format; discovery admits `.html` for legacy compatibility and no real project has one. Left as a known residual rather than pretended away. |\n| **A4** — detect slug collisions | **Closed.** `slugFor` flattens `/` to `-`, so `ui/a/b.tsx` and `ui/a-b.tsx` produce one slug. Two files on one document is not degradation, it is each writing the other's body over itself forever. **Both** are excluded, not one picked: refusing to sync two canvases is recoverable by renaming; overwriting one with the other is not. |\n| **A6** — cap pinned rooms | **Closed.** A shared-doc canvas is a pinned room, deliberately immune to the last-browser-leaves drop — which is exactly why nothing else will ever reclaim it. Ceiling 500 (`MAUDE_MAX_PINNED_ROOMS`), far above the largest in-house project at 83, so it is a runaway guard rather than a product limit. |\n| **A7** — one-time consent notice | **Closed.** A one-time warn whenever shared-doc engages against a hub. A cell is exempt **and that is the decision**: the operator turned pairing on per project, the hub is the cell's own loopback, nothing leaves the container — consent was given by configuration, and repeating it per canvas boot trains an operator to skip the line that matters. |\n| **comments hub→disk lane** | **Closed.** The codec capped file→doc; this is the other direction, and until shared-doc it barely mattered because the room's doc was populated only by browsers on this machine. Now it is populated by the hub. Same ceilings, so a value that could never be imported can never be written either. The write is refused, not the sync — the doc keeps converging. |\n| **`@hocuspocus/provider` advisories** | **Closed.** 4.3.0, current major; `bun audit` reports nothing against `@hocuspocus/*` or `yjs`. (Nine unrelated transitive findings exist in the ACP SDK and `dom-to-svg` chains — pre-existing, off the sync path.) |\n\n## Alternatives rejected\n\n- **C1 — re-home the browser's collab onto the hub's Hocuspocus directly** (the\n  proxy stops forwarding `/_ws/collab`; the hub serves it). Cleaner: one doc, no\n  bridge, no second process in the loop. Rejected **for now, not on the merits**\n  — it requires re-homing the room's other lanes (comments, annotations,\n  activity) onto the hub doc, which is a large structural change to make on the\n  strength of a model that has never run in production. C2 is the reversible\n  pilot that earns the right to do C1; the follow-up is noted, not abandoned.\n- **B — the studio child dials its own external hub / the tenant's\n  `linkedHub`.** This is the thing DDR-209 forbids, wearing a feature's clothes:\n  dial-out plus a second autocommit. Off the table.\n- **Let the hub remain the only doc→file writer and have the studio skip its\n  projection.** Tempting, since the hub already writes on `afterStoreDocument`\n  through a twin of the same merge (`meta-merge.mjs`, held byte-identical by\n  test). Rejected because the ordering then stops being ours: the studio would\n  have to announce a reload for a file another process had not necessarily\n  written yet. Both writers producing identical bytes is redundant and cheap;\n  the echo guard drops the second event and the HMR debounce collapses the pair.\n\n## Consequences\n\n- **Attribution changes for browser-originated edits.** They now reach the hub\n  over the loopback connection, so `afterStoreDocument` attributes the commit to\n  the pairing token rather than to the member. The token is minted with **no\n  owner** deliberately — signing a tenant's git history with a machine identity\n  dressed up as a person is worse than an honest \"unknown\". Carrying the real\n  editor across the loopback lane is follow-up work, and it is the first thing\n  C1 would fix for free.\n- Beyond the cell, nothing moves: `MAUDE_SHARED_DOC` stays off by default for\n  desktop and self-hosted hubs, and the guard is inert outside workspace mode.\n- The live cross-surface run (two surfaces, cursors both ways, reload loses\n  nothing, exactly one committer in `git log`) is the gate this decision is\n  conditional on. Passing tests are the floor, not the proof.\n\n## Security review (2026-08-06, `/flow:validate`)\n\nDefender + adversarial passes ran against this diff\n(`.ai/logs/security-reviews/main-20260806-0554-defender.md` — 0 blockers, 4\nwarnings; `main-20260806-0600-attacker.md` — the chain below). Both\nindependently re-derived every claim in this DDR from the code rather than\ntaking it on faith, and both are worth reading in full; this section records\nwhat changed as a result and, honestly, what did not.\n\n**Fixed on the spot:** four log lines (`cell-pairing.ts` ×2,\n`sync/index.ts`, `studio-child.mjs`) interpolated `MAUDE_LOOPBACK_SYNC_URL`\nwithout stripping control characters, unlike the codebase's own\n`sanitizeForLog` precedent (`apps/hub/src/server.mjs`). Exploitability was\nalready near-zero — that env var is hub-set only, never tenant-reachable —\nbut a feature whose entire argument is \"the URL is provably loopback\" should\nnot be the one place still trusting the string past its hostname. All four\nnow scrub before logging (mirrored, not shared, per the same\ndeliberate-duplication reasoning as the loopback checks themselves —\n`cell-pairing.ts` and `studio-child.mjs` cannot import from each other\nwithout a cycle or an app boundary crossing).\n\n**Named, not fixed — a wildcard, non-expiring pairing token.**\n`mintLoopbackSyncToken` mints `scope: '*'`, no `owner`, no `expiresAt`. The\nattacker's chain (Chain 1, promoted to High, impact-conditional — no leak\ndemonstrated in this diff) composes that with the defender's own finding that\nthe scope is confined to one tenant *only* by the \"one hub process ⇒ one\ntenant\" architectural invariant, never by the token itself: if the raw value\never left the studio child's process environment by any channel, it would be\na full-project bearer credential, usable against the tenant's own public hub\nendpoint, valid until the next hub restart re-mints it by label.\n\nVerified before deciding, not assumed: `HUB_SECRET` — the hub's existing\nadmin escape-hatch, already in production — has the **identical** shape\n(`server.mjs`: `scope: '*'`, no `expiresAt`, no network-origin binding in\n`onAuthenticate`), and is *more* privileged than this token (it also reaches\n`/internal/*` and the admin API; the pairing token reaches documents only).\nThis diff does not introduce a new class of risk — it adds one narrower\ninstance of a bearer-credential shape this exact server already accepts for\nits most powerful credential. That is why this is recorded as a residual\nrather than treated as a blocker.\n\n**Why the two stronger mitigations the attacker recommended are follow-up\nwork, not today's fix:**\n- *Bind `onAuthenticate` to the token's `remoteAddress` for this one label.*\n  Sound in principle, symmetric with the two existing egress checks. Not\n  implemented because whether a raw `request.socket.remoteAddress` check\n  actually distinguishes \"the studio child's genuine loopback connection\"\n  from \"external traffic that arrived via the cell's own Cloudflare tunnel\"\n  depends on cloudflared's ingress configuration (server-side, not visible in\n  this repo) — if the tunnel proxies to `127.0.0.1:\u003cport\u003e` (a common\n  configuration), *every* external request would also present as loopback at\n  this exact check, making it a false safety net rather than a real one. A\n  control that creates false confidence is worse than no control; this needs\n  the actual ingress topology confirmed before it is written, not guessed.\n- *Give the token a short `expiresAt` and re-mint on a timer.* The mint\n  happens once, at hub boot; the token then rides into an already-spawned\n  child's environment, which cannot be updated without either restarting the\n  child or adding a live-refresh side-channel — neither of which exists\n  today. Adding an expiry without a refresh path would not reduce the leak\n  window; it would silently break live pairing on every cell that outlives\n  the TTL without a restart, trading a theoretical, undemonstrated risk for a\n  guaranteed operational regression on the pilot project. This needs a real\n  refresh mechanism designed alongside it, not a bolted-on deadline.\n- *Scope the token to `ws/\u003cworkspaceId\u003e/\u003cbranch\u003e/` instead of `*`.* The\n  prefix is only computable from the tenant's own `.design/config.json`,\n  which the studio reads and the hub deliberately does not (the hub minting a\n  token would need to either read the tenant's file itself — a new coupling —\n  or accept the prefix from the studio child post-boot — a new handshake).\n  Smaller than the other two, still a real design change, not a review-pass\n  patch.\n\nTracked as explicit follow-up, alongside C1 (which would remove this\ncredential class entirely by re-homing browser collab directly onto the hub\n— no loopback hop, no minted token, nothing to leak).","author":"1aGh","summary":"element cloud; element DDR-213; link decision:DDR-213-ABOUT-\u003earea:cloud; element maude; link decision:DDR-213-IN_REPO-\u003erepo:maude; element dev; link decision:DDR-213-IN_DEPT-\u003edept:dev; element cell; link area:cloud-TOUCHES-\u003etopic:cell; element collab; link area:cloud-TOUCHES-\u003etopic:collab; element crdt; link area:cloud-TOUCHES-\u003etopic:crdt; element yjs; link area:cloud-TOUCHES-\u003etopic:yjs; element shared-doc; link area:cloud-TOUCHES-\u003etopic:shared-doc; element DDR-192; link decision:DDR-213-REFERENCES-\u003edecision:DDR-192; element DDR-209; link decision:DDR-213-EXTENDS-\u003edecision:DDR-209; element DDR-064; link decision:DDR-213-EXTENDS-\u003edecision:DDR-064","supersedes":["d_a6ed52dc3904facba50f38e9"],"shapes":["el_089d057c99dcc24edad9","el_251c410de3233a1aeb10","el_42b070d9886acea59c36","el_4f650e68f97a1dabfe5b","el_992639d27bb455fc2acd","el_9afa54b829907b2c7cc5","el_bd50734bc195278caee8","el_c9d85ccf509eec6f59d7","el_ca487ac52d9fa289ed45","el_cb925bf1cd97e4c560f6","el_e28dc22ac3959e86511c","el_ec5ef965b4979595c248"],"targets":["el_251c410de3233a1aeb10","el_ca487ac52d9fa289ed45"],"mutations":[{"op":"upsert_element","element_id":"el_ca487ac52d9fa289ed45","kind":"area","name":"cloud","props":{"last_ddr":"DDR-213"}},{"op":"upsert_element","element_id":"el_251c410de3233a1aeb10","kind":"decision","name":"DDR-213","props":{"date":"2026-08-06","path":".ai/archive/decisions/DDR-213-cell-pairs-with-itself-loopback-shared-doc.md","tags":"cloud,cell,collab,crdt,yjs,shared-doc","title":"A cell syncs to itself, or to nothing"}},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_ca487ac52d9fa289ed45","link_kind":"ABOUT"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_cb925bf1cd97e4c560f6","kind":"topic","name":"cell"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_cb925bf1cd97e4c560f6","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_089d057c99dcc24edad9","kind":"topic","name":"collab"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_089d057c99dcc24edad9","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_e28dc22ac3959e86511c","kind":"topic","name":"crdt"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_e28dc22ac3959e86511c","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_9afa54b829907b2c7cc5","kind":"topic","name":"yjs"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_9afa54b829907b2c7cc5","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_992639d27bb455fc2acd","kind":"topic","name":"shared-doc"},{"op":"add_link","element_id":"el_ca487ac52d9fa289ed45","from_id":"el_ca487ac52d9fa289ed45","to_id":"el_992639d27bb455fc2acd","link_kind":"TOUCHES"},{"op":"upsert_element","element_id":"el_bd50734bc195278caee8","kind":"decision","name":"DDR-192"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_bd50734bc195278caee8","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_c9d85ccf509eec6f59d7","kind":"decision","name":"DDR-209"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_c9d85ccf509eec6f59d7","link_kind":"EXTENDS"},{"op":"upsert_element","element_id":"el_4f650e68f97a1dabfe5b","kind":"decision","name":"DDR-064"},{"op":"add_link","element_id":"el_251c410de3233a1aeb10","from_id":"el_251c410de3233a1aeb10","to_id":"el_4f650e68f97a1dabfe5b","link_kind":"EXTENDS"}]}}
{"hash":"sha256:929d8c0b0dd2c5b3f103deb47a5286c5a37f86c8ce17d4c8f54d876e1e460da9","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":711,"recorded_at":"2026-08-06T00:00:00Z","parents":["sha256:9a8825505cdeb3bb209891dc3b216b761fd64a76e6ec9c7583fe829b316d95e4"],"decision":{"id":"d_d764549fbaaa11c9c5e64f55","title":"Closed: desktop ↔ cloud live pairing","rationale":"C2 (loopback shared-doc pairing) shipped code-complete: all 6 tasks done, DDR-064 pre-cutover checklist closed, security-reviewed (defender+attacker, wildcard-token residual evaluated against HUB_SECRET precedent and recorded), 4-lens simplify pass applied. Gated behind CELL_LIVE_PAIRING pilot allowlist (alligators only) pending the live cross-surface run — the one remaining acceptance blocker.","author":"1aGh","summary":"element feature-desktop-cloud-live-pairing","shapes":["el_8a7d1becc749b9eded03"],"targets":["el_8a7d1becc749b9eded03"],"mutations":[{"op":"upsert_element","element_id":"el_8a7d1becc749b9eded03","kind":"plan","name":"feature-desktop-cloud-live-pairing","props":{"status":"done"}}]}}
{"hash":"sha256:664446ac99d1c956ae7ffcfaf215d7098b71680b1851997fc1e8ea8e81021a80","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":712,"recorded_at":"2026-08-06T00:00:00Z","parents":["sha256:929d8c0b0dd2c5b3f103deb47a5286c5a37f86c8ce17d4c8f54d876e1e460da9"],"decision":{"id":"d_a8b2ef67e80987a0341ebee5","title":"In a cloud cell the Changes panel withdraws to History — the hub already committed it","rationale":"A cell's hub owns the tenant's history and commits every edit as it lands (the studio child's own autocommit is disabled so there is one committer). The shared studio client nonetheless rendered the full working-tree surface there — an 'unsaved changes' list, Save version, Publish, and a read-only hint telling a browser user to 'save and publish from your terminal', which is advice a cloud user cannot take. The list was also literally false: it described work that was already saved.\n\nDecision: the client withdraws the working-tree HALF of the Changes panel wherever the server owns history, and keeps History, which is the part someone actually wants there. Driven by `cfg.cloud` — present exactly when the hub runs the workspace agent that owns history, so the UI follows a server-STATED fact rather than re-deriving one. A new `historyOnly` prop on GitPanel; panel title, dock tab and View-menu row all read 'History'.\n\nDeliberately NOT applied to a hub-linked DESKTOP peer, though it syncs to the same cloud project: verified that such a peer does not self-commit (its tree stays dirty), so hiding the surface there would remove the only way to commit its local checkout. The condition is 'the server owns history', not 'the project is in the cloud'.\n\nKnown edge: a cell whose git failed to initialise reports no history either way; the browser could not act on it regardless, since the panel is read-only there.\n\nFound while verifying DDR-213 pairing on a local cloud-faithful cell. The '7 unsaved' that prompted it was a real bug (the hub never committed paired edits) fixed in the same change; with that fixed the status chip reads 'all saved' on its own.","author":"1aGh","summary":"element studio-client-changes-panel","shapes":["el_92b76815a4efbc8cd869"],"targets":["el_92b76815a4efbc8cd869"],"mutations":[{"op":"upsert_element","element_id":"el_92b76815a4efbc8cd869","kind":"area","name":"studio-client-changes-panel"}]}}
{"hash":"sha256:43331016f62f104cb8a006baa7bfc17c5b15ff06cfadc3d2ef61eca31a9e99fb","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":713,"recorded_at":"2026-08-06T00:00:00Z","parents":["sha256:664446ac99d1c956ae7ffcfaf215d7098b71680b1851997fc1e8ea8e81021a80"],"decision":{"id":"d_06b441591bf302b43d909a04","title":"Security review (defender): cell autocommit commit-set widening + annotations path","rationale":"## Security defender audit — 6 files scanned (cell autocommit commit-set broadening + annotations path)\n\n### Evidence sources\n- Diff base: `512ab5a4` (branch `main`, uncommitted working tree)\n- Config: no `security` block in `.ai/workflows.config.json` → `severityFloor = medium`, `scope` default (classic in scope)\n- Changed files in scope: `apps/hub/src/workspace-agent.mjs`, `apps/hub/src/workspace-files.mjs`, `apps/hub/test/workspace-agent.test.mjs`, `apps/studio/client/app.jsx`, `apps/studio/client/panels/GitPanel.jsx` (+ `apps/studio/dist/client.bundle.js` — generated, skipped)\n- Also in the working tree, audited for surface: `pnpm-lock.yaml`, `.claude/settings.json`\n- Lockfile changes: yes (see Dependency surface)\n- Prior decisions read: DDR-213, DDR-209, DDR-115, DDR-064, DDR-054\n- Two git behaviours below were verified empirically in a throwaway repo, not asserted from memory.\n\n---\n\n### Blockers (severity \u003e= medium)\n\n#### 1. `apps/hub/src/workspace-agent.mjs:232-239` — **A3 authz / trust-boundary control bypass** (high)\n\nSnippet:\n```js\n.filter(([, inDoc, onDiskText]) =\u003e inDoc !== null \u0026\u0026 onDiskText !== null)\n```\n\n**Why.** The meta lane's *write* gate and its new *commit* gate disagree, and the commit gate is the looser one.\n`filesForCanvas` only writes meta when `mergeSharedMetaIntoLocal(...) !== null`, and its own docblock states the contract:\n\"null means the doc's shared meta did not parse. Skipping is the only safe answer.\" `committable` gates on\n`content.meta !== null` — true for *any* non-empty string, including one that is not JSON, not an object, or an array.\nSo when a peer puts a non-object string in the meta `Y.Text`, the hub writes nothing (correctly refusing) and then\nstages and commits `\u003cstem\u003e.meta.json` **verbatim, unmerged, unvalidated, and un-authored by the hub**.\n\nThat is the one lane where committed bytes can differ from doc bytes. Body / css / annotations are safe by\nconstruction: when the doc lane differs from disk, `put()` overwrites disk with the doc content *before* staging, so\ncommitted == doc. Meta is the exception precisely because it has a validation gate — and the commit path routes\naround it.\n\nThe sink is external. `scheduleMirror` (`apps/hub/src/cell-ops.mjs:197`) pushes the checkout to the customer's GitHub\non a clock — `backup` mode to a disjoint branch, `design-sync` mode as a PR into their working repo. And the disk\nside is reachable from the untrusted principal: `PATCH /_api/canvas-meta` is in `CANVAS_SAFE_API`\n(`apps/studio/http.ts:4671`), and `patchCanvasMeta`'s layout lane (`apps/studio/api.ts:1757-1779`) writes\n`next.layout = patch.layout` — an arbitrary JSON object, no schema, no size cap beyond `readJson`'s 256 KB — into the\nversioned `.meta.json`, refused only for `projectReadOnly` sessions. So a DDR-054-untrusted canvas origin plants the\nbytes and a peer's malformed meta lane makes the hub publish them.\n\nUnder DDR-209 the hub is the sole committer specifically so that one validated process decides what enters history.\nThis is that control being bypassed by the process that owns it.\n\n**Fix.** Derive the meta entry of `committable` from the same decision the write path made — gate it on\n`mergeSharedMetaIntoLocal(content.meta, onDisk.meta) !== null`, or simply drop meta from `committable` and let it be\nstaged only when it appears in `writes`. The other three lanes do not need this.\n\n---\n\n#### 2. `apps/studio/sync/autocommit.ts:242-246` (reached via the widened note-set) — **A12 error handling / silent history loss** (medium)\n\nSnippet:\n```js\nconst add = await run(['add', '--', ...files], { cwd: repoRoot });\nif (add.code !== 0) { log.warn?.(...); return { ok: false, reason: 'git-failed', ... }; }\n```\n\n**Why.** Verified: `git add -- \u003cmissing untracked path\u003e` exits **128**. On `add` failure `commitCycle` returns\nwithout re-queuing `files` — unlike the `commit` failure branch three lines below, which does\n(`for (const f of files) touched.add(f)`). So one bad pathspec silently drops the **entire coalesced batch**,\nincluding other users' real edits, from history forever.\n\nThe diff materially widens the window. Previously every noted path had just been written by the hub, so it provably\nexisted. Now paths are noted on the strength of a `readIfPresent` taken up to `debounceMs` (default **3000 ms** — the\ncell passes no override) before the `git add`, for files whose lifecycle the hub does not own (the studio child's\nprojector, `collab/persistence.ts`, `DELETE /_api/canvas`). A canvas deleted inside the quiescence window now takes\nan unrelated peer's commit down with it.\n\n**Fix.** Re-queue on `add` failure exactly as the `commit` branch does, and/or filter `files` through an\n`existsSync` immediately before staging (accepting that a tracked-and-deleted path should still stage as a deletion).\n\n---\n\n#### 3. `apps/studio/sync/autocommit.ts:250-271` (reached via the widened note-set) — **A12 error handling / autocommit wedge** (medium)\n\n**Why.** Verified: with any unrelated entry already staged in the index, `git diff --cached --name-only` is non-empty,\nso `commitCycle` proceeds past its short-circuit — and `git commit --only -- \u003cpaths with no changes\u003e` exits **1**\n(\"no changes added to commit\"). That returns `git-failed`; `touched` is re-queued but **no timer is armed**, so\nrecovery waits on the next `note()` and then fails identically for as long as the stray index entry survives.\nThe tenant's history stops, and `/health` stays 200.\n\nBefore this diff, a batch consisting entirely of unchanged paths was structurally impossible — only written (therefore\nchanged) paths were noted. After it, that is the *normal* case: the comment correctly reasons that \"an unchanged lane\ncosts one `git add` and produces no empty commit\", which holds only while the index is otherwise clean. A stray staged\nentry is plausible in a cell (a killed studio git verb under `MAUDE_USE_SYSTEM_GIT=1`, an ACP terminal `git add`).\n\n**Fix.** Scope the emptiness probe to the batch — `git diff --cached --name-only -- ...files` — so the\n`nothing-to-commit` short-circuit answers the question `commit --only` will actually be asked.\n\n---\n\n#### 4. `apps/hub/src/workspace-files.mjs:191` + `apps/hub/src/workspace-agent.mjs:246` — **A6 stored-content / missing sanitizer + byte cap** (medium)\n\n**Why.** Every other doc→file writer in the system caps and (for annotations) sanitizes; the hub's does neither.\n- `api.saveAnnotations` (`apps/studio/api.ts:1809-1831`) applies `sanitizeAnnotationSvg` and a 1 MB ceiling, with an\n  explicit comment that the sanitizer exists \"so 'inert' stays true for any future raw-render consumer and for the\n  synced file a peer/Claude-context ingests\".\n- `collab/persistence.ts:166-173` gates through `withinByteCap(..., MAX_ANNOTATIONS_BYTES)`.\n- `sync/projection.ts` gates html/css/meta through the same `limits.ts` ceilings (DDR-054 §2d).\n- The hub takes the raw `Y.Map.get('svg')` string and `atomicWrite`s it. No sanitizer, no cap, on **any** of the four\n  lanes (`MAX_HTML_BYTES` 4 MB, `MAX_CSS_BYTES` 4 MB, `MAX_META_BYTES` / `MAX_ANNOTATIONS_BYTES` 1 MB all unenforced).\n\nThe `siblingPaths` change is what makes this land somewhere that matters. For a **nested** canvas the hub previously\nwrote `ui/Card.annotations.svg` — a junk path nobody reads (which is exactly the bug the change fixes). It now writes\n`ui-card.annotations.svg`, the file `GET /_api/annotations`, the studio renderer, `read-annotations.mjs` and\n`annotate.mjs` all actually read. So unsanitized, uncapped, peer-supplied SVG reaches the live consumers for the\nnested case for the first time. (Flat canvases were already in this state — the two derivations coincide there.)\n\nThe XSS chain stays latent (`svgToStrokes` parses inertly, and `annotations-model.ts:1330` already treats a peer's\nsidecar as untrusted foreign input), so this is medium, not high. The uncapped write is also the honest DoS answer\nfor Q4: a peer can push an arbitrarily large body/css/annotations lane straight through the hub onto disk, into a git\nobject, and out to the mirror, past ceilings every other writer respects.\n\n**Fix.** Port `withinByteCap` (or a plain byte-length check against the same `limits.ts` constants — they are numbers\nwith no imports, so the hub's no-studio-source constraint is not a problem) into `filesForCanvas`, and route the\nannotations lane through a sanitizer before `atomicWrite`.\n\n---\n\n### Warnings (below floor)\n\n- `apps/hub/src/workspace-agent.mjs:199` — **low** — the containment check is **lexical**: `resolve()` does not\n  resolve symlinks, and `atomicWrite`'s `mkdirSync(dirname, { recursive: true })` would follow a symlinked\n  intermediate directory out of the design root. Currently unreachable — `listFiles` skips symlinks (`Dirent`\n  `isDirectory()`/`isFile()` are lstat-based, so a symlink is neither) and `defaultBodyPath` is always flat — but the\n  diff pushes three more derived paths through this check, so the margin is worth knowing about. A `realpathSync`\n  containment assert would close it permanently.\n- `apps/hub/src/workspace-files.mjs:53-62` — **low** — `canvasSlug` can return the empty string. `DOCUMENT_NAME_REGEX`\n  (`/^[A-Za-z0-9._\\/-]{1,256}$/`) permits `ws/a/main/..`, which yields body `...tsx` and an annotations lane of\n  `\u003cdesignRoot\u003e/.annotations.svg` — a hidden file at the design root, shared by every dot-only slug. Contained, no\n  traversal, but it is a junk write and a small cross-document collision.\n- `apps/hub/src/workspace-agent.mjs:241` — **low** — every store event now arms the debounce timer, because\n  `committable` is non-empty whenever body+meta merely exist on disk. Previously an event with nothing to write\n  returned early. Under DDR-213 pairing that is per-keystroke-batch, and each firing takes the cross-process\n  `maude-repo.lock` (`DEFAULT_WAIT_MS` 15 s) plus 2-3 git subprocesses, contending with the studio's browser git verbs\n  which hold the same lock across network operations. No commit storm — the `nothing-to-commit` short-circuit is\n  correct and the new test covers it — but the baseline lock and subprocess rate rose materially.\n- `apps/studio/client/app.jsx:14098` / `apps/studio/client/panels/GitPanel.jsx:158` — **low** — `historyOnly` is\n  presentation, not a control. Correct as written (the real gates are server-side `projectReadOnly` and the panel's\n  own `readOnly={!isNativeApp() || viewerMode}`), but nothing should ever come to depend on it for authorization.\n- `apps/hub/src/workspace-agent.mjs:87-92` — **low, pre-existing** — `atomicWrite`'s `.tmp-\u003cpid\u003e` sibling is left at\n  the design root if the process dies mid-write. Never staged (never noted), so cosmetic only.\n\n### Dependency surface\n- New deps: 7 × `@1agh/maude-{darwin-arm64,darwin-x64,linux-arm64,linux-arm64-musl,linux-x64,linux-x64-musl,win32-x64}@0.55.0`\n  added as `optionalDependencies`. First-party (own npm scope), **exact-pinned** (no range), sha512 integrity present,\n  cpu/os/libc constrained, no install scripts declared in the lockfile entries.\n- Flagged: none. No typosquat candidates, no `postinstall`, no abandoned packages.\n- `.claude/settings.json` drops `vercel@claude-plugins-official` (surface reduction) and relocates the existing\n  `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` env. `permissions.defaultMode: \"bypassPermissions\"` is pre-existing, untouched.\n\n---\n\n### Answers to the four threat questions\n\n**Q1 — Can the broadened commit set escape the design root or reach a path the tenant did not author?**\nNo escape. The containment loop (`workspace-agent.mjs:198-204`) is computed over exactly the four values\n`committable` later consumes — `bodyRel`, `sib.meta`, `sib.css`, `sib.annotations` — and runs before any read, write\nor `note`. It still dominates every new path including the `canvasSlug`-derived annotations path. The slug is doubly\ncharset-constrained (`DOCUMENT_NAME_REGEX` at the hub door, `[^/]+` in `slugFromDocName`), so no separator and no\ntraversal component can survive; `..` degrades to the literal filename `...tsx`. `canvasSlug` removes separators and\nstrips leading dots, so it cannot introduce one. Every derived path is therefore either a real on-disk path from\n`listFiles(designRoot)` or a single flat filename directly under the design root.\n\nThe `canvasSlug` collision is **not** exploitable for cross-canvas overwrite. `ui/Foo.tsx` and `ui-foo.tsx` already\ncollapse to one hub *document* (the doc namespace is built from the same lossy slug), and `indexCanvasPaths`\ndeterministically resolves the slug to exactly one body path. I diffed the three transforms —\n`workspace-files.canvasSlug`, `api.fileSlug` → `canvasSlugFromRel`, and `sync/index.slugFor` — and they are the same\ntransform character-for-character, so the change makes the hub *agree* with `api.annotationsPath()` rather than\ncreating a collision. The residual attacker choice is \"which `*.annotations.svg` at the design root\", which is not an\nescalation in a single-tenant cell (a peer can already open that canvas's own document) and can never collide with a\nbody, `.meta.json`, `.css`, or `config.json` because of the suffix.\n\n**Q2 — Does staging lanes we did NOT write let a peer get arbitrary working-tree content into history / the mirror?**\nFor body, css and annotations: **no**. When the doc lane differs from disk, `put()` overwrites disk with the doc bytes\nbefore anything is staged, so committed == doc; when they are identical the commit is a git no-op. For meta: **yes** —\nfinding #1. There is also a shared ~3 s TOCTOU window on all four lanes (disk read at store time, `git add` after\nquiescence) which is inherent to any autocommit, but which now spans paths the hub never authored rather than paths it\nhad just written.\n\n**Q3 — Can the meta lane leak or reset the `syncable: false` opt-out?**\n**No reset.** `mergeSharedMetaIntoLocal` layers `META_LOCAL_KEYS` back from disk over the shared payload, nothing in\nthis diff writes meta, and `patchCanvasMeta` exposes only the `layout` lane — the canvas origin cannot set `syncable`.\n**No steady-state leak either**, because staging an unchanged file produces no commit content; a `.meta.json` reaches\nhistory only when it genuinely diverged from HEAD. The real exposure folds into finding #1: an untracked-or-diverged\n`.meta.json` the hub never authored now enters history and the mirror, and in the unparseable-meta case it does so\nwith the merge gate bypassed. Note also that `syncable` is legitimately a versioned key, so committing it is not in\nitself a violation — the problem is the *unvalidated* commit, not the key.\n\n**Q4 — DoS: unbounded `git add` churn, commit storms, lock contention?**\nNo commit storm — the `nothing-to-commit` short-circuit is correct and the new regression test covers it. The real\navailability items are findings #2 (a deletion race silently drops a whole batch of history), #3 (a stray index entry\nwedges autocommit permanently and silently), the raised lock/subprocess baseline in the warnings, and the missing byte\ncaps in finding #4 (a peer can push an unbounded lane through the hub onto disk, into a git object, and out to the\ntenant's GitHub).\n\n### Notes\n- The `siblingPaths` annotations fix is **correct** and closes a real bug: `api.ts:1797`, `sync/index.ts:1344` and\n  `canvas-artifacts.ts:95` all key the sidecar by the flat slug at the design root, so the previous sibling derivation\n  wrote and committed a path nobody read. The regression test asserts the right thing.\n- The `historyOnly` client change is clean — no injection, no `innerHTML`, no user-controlled data. The `labels?.[id]`\n  lookup is over an internally-constructed object with internal-constant keys.\n- Prompt-injection surface worth passing to `ethical-hacker` (§B, not my lane): `read-annotations.mjs` and\n  `annotate.mjs` feed `.annotations.svg` text into Claude context, and finding #4 removes the sanitizer from the\n  writer that now populates that file for nested canvases.\n\nSummary: 4 blockers, 5 warnings, 3 notes.\n","author":"1aGh","summary":"element cell-autocommit-annotations-path","shapes":["el_c15110fae23b614a5c47"],"targets":["el_c15110fae23b614a5c47"],"mutations":[{"op":"upsert_element","element_id":"el_c15110fae23b614a5c47","kind":"security-review","name":"cell-autocommit-annotations-path"}]}}
{"hash":"sha256:91eb32c586059a792f60b65771c174a49a0e1911efeec020873557ae3c61fb16","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":714,"recorded_at":"2026-08-06T00:00:00Z","parents":["sha256:43331016f62f104cb8a006baa7bfc17c5b15ff06cfadc3d2ef61eca31a9e99fb"],"decision":{"id":"d_474d5541cc6638ea37ee0d28","title":"Security review (attacker): cell autocommit commit-set widening + annotations path","rationale":"## Security attacker review — adversarial threat model\n\n**Target:** uncommitted working-tree change on `main`, HEAD `512ab5a4`\n**Date:** 2026-08-06\n**Posture:** paid red-team pass. The defender's checklist is assumed done; nothing here is a re-file.\n\n### Change summary\n- Plan: no active plan (`.ai/state/STATE.md` → \"done — desktop ↔ cloud live pairing closed 2026-08-06\"); the change is post-DDR-213 follow-up.\n- Diff base: working tree vs. `HEAD` (512ab5a4).\n- Files in scope: `apps/hub/src/workspace-agent.mjs`, `apps/hub/src/workspace-files.mjs`, `apps/hub/test/workspace-agent.test.mjs`, `apps/studio/client/app.jsx`, `apps/studio/client/panels/GitPanel.jsx`. (Also `.claude/settings.json`, `pnpm-lock.yaml`, `dist/client.bundle.js` — noted, not the subject.)\n- AI / MCP surface touched: **yes, indirectly** — the change decides which bytes land in a file that `read-annotations` / `annotate` / `/design:new` ingest feed into a model context, and which bytes reach the tenant's GitHub mirror.\n\n### Threat model — trust boundaries\n\n| Boundary | What this diff changes about it |\n| --- | --- |\n| Y.Doc (semi-trusted, DDR-054) → cell checkout | Unchanged in the write direction. **Changed in the commit direction**: the hub no longer commits *what it wrote*, it commits *what is at four derived paths*. |\n| Cell checkout → tenant's git history | Widened. The set of `git add`-ed paths went from \"paths the hub materialised in this pass\" to \"every lane the doc carries that also exists on disk\", including byte-identical no-ops. |\n| Cell checkout → tenant's GitHub | Unchanged mechanically, but the input set feeding it widened by the row above. The mirror is an **unattended clock** (`cell-ops.mjs` `scheduleMirror` → `setInterval(tick)`), not a human action. |\n| Studio child (writer) ↔ hub (committer) | New: the hub now takes the child's on-disk output on faith. The child runs the full studio API surface and answers untrusted-canvas-origin requests. |\n| Untrusted canvas iframe → design root | Unchanged route-wise (`PUT /_api/annotations`, canvas-safe, accepts **any** `file`), but the file it writes is now the exact path the hub commits — for nested canvases it previously was not. |\n| Hub slug space ↔ studio slug space | **New**: `canvasSlug()` was an index lookup key; it is now a *path constructor* on the write side. |\n| Browser shell → git service | Unchanged server-side. Only the client rendering changed, and the diff's own comment reasons about safety from it. |\n\n### Findings (attacker narrative)\n\n---\n\n#### A1 — Commit-debounce starvation: a **viewer** can freeze the tenant's entire git history, silently (**High**, new)\n\n- **I would do:** hold the cell's autocommit timer open forever, so that *nothing anyone does in the project ever reaches `git log`*, for as long as I stay connected.\n- **By:** the removed early return. Before this change, a store event that produced no writes returned `null` before ever touching `auto.note()`. Now:\n\n  ```js\n  if (writes.length === 0 \u0026\u0026 committable.length === 0) return null;   // rarely true\n  ...\n  for (const rel of toStage) auto.note(relative(repoDir, abs(rel)), who);\n  ```\n\n  `committable` is non-empty for *any* canvas whose body exists on disk and in the doc — i.e. essentially every live canvas. And `auto.note()` (`apps/studio/sync/autocommit.ts`) **restarts the quiescence timer on every call**:\n\n  ```js\n  if (timer) clearTimeout(timer);\n  timer = setTimeout(() =\u003e { ... flush() ... }, debounceMs);\n  ```\n\n  `debounceMs` is not passed by `server.mjs` → `DEFAULT_DEBOUNCE_MS = 3000`. Hocuspocus's own store debounce is ~2 s. So a doc that receives an update stream faster than 3 s produces an `afterStoreDocument` roughly every 2 s → `note()` every 2 s → **the 3 s timer never expires**. `flush()` then only runs on `stop()` (SIGTERM / migration) or a branch switch.\n\n  The cheapest driver is the lane the lowest role holds. `studio-manifest.mjs`: `'/_comments': { safe: 'read', unsafe: 'comment' }`, and `role-matrix.mjs` gives `viewer: { comment: true }` — deliberately (\"comment is the single write a viewer holds\"). Each comment mutation → `onCommentsChanged` → `syncRoomFromComments` → Y.Array update → store → `note()`. The proxied studio routes are **not** rate-limited (`rate-store` covers the auth/admin lanes in `server.mjs`, not the studio proxy). Pre-change a comments-only store produced `writes.length === 0` and touched the timer not at all — this is exactly the property the change removed.\n- **Resulting in:** total, silent suppression of the one thing DDR-209 says a cell owns on the tenant's behalf. There is no error, no `nothing-to-commit`, no log line — `commitCycle` simply never runs. `MAUDE_SYNC_NO_AUTOCOMMIT=1` means the studio child will not cover for it. On an OOM kill / SIGKILL (rather than a graceful SIGTERM) every edit since boot is gone from history. The mirror clock keeps pushing a `HEAD` that never advances, so the tenant's GitHub silently reports \"nothing changed today\".\n- **Cite:** §A (availability / integrity of an audit-relevant record); §B5-adjacent (a low-trust principal steering a high-trust batching mechanism).\n- **Mitigation:** cap the debounce — keep a `firstNotedAt` and force a flush when `now - firstNotedAt \u003e maxDebounceMs` (mirror Hocuspocus's own `maxDebounce`); and/or only `note()` a lane when it is genuinely dirty (`inDoc !== onDiskText` for the byte lanes, `merged !== onDisk.meta` for meta), which restores the pre-change timer semantics without reintroducing the pairing bug.\n\n---\n\n#### A2 — The hub now `git add`s bytes it neither wrote nor validated in this pass (**High**, new — structural)\n\n- **I would do:** decouple \"the hub committed this canvas\" from \"these are the doc's bytes\", so that a commit signed with a human's name carries content the hub never derived.\n- **By:** `committable` filters on `inDoc !== null \u0026\u0026 onDiskText !== null` — *existence*, not *agreement*. Two consequences, one deterministic and one racy:\n\n  **(a) Deterministic — the meta lane the hub deliberately refused to write is committed anyway.** `filesForCanvas()` skips the meta write when `mergeSharedMetaIntoLocal()` returns `null` (shared payload unparseable, or not a plain object). The doc's `meta` lane is a Y.Text any member holds. One character (`[`, or `nope`) is enough. `committable` does not consult the merge result, so the hub stages `\u003cstem\u003e.meta.json` and commits **whatever is on disk**, unmodified, while the code comment two functions away explains that refusing to write is the safe answer *precisely because* writing the wrong meta \"would drop `syncable: false`, which is a security opt-out\". The refusal is now decorative: the file is committed either way, just not by the sanitising path.\n\n  **(b) Racy, and now the normal path.** The disk read (`readIfPresent`) and the `git add` are separated by ≥ `debounceMs`, unbounded under A1. Pre-change, every staged path had been `atomicWrite`n microseconds earlier with hub-computed bytes; the race existed but the *authored* bytes were the hub's. Post-change the whole point is that the hub wrote nothing — the bytes are the studio child's, and the child is the process that serves untrusted-canvas-origin writes. `commitCycle` then runs `git commit --only -- \u003cfiles\u003e`, which commits the state of exactly those paths as of staging, so anything that landed in the window rides along under the last-noted author.\n- **Resulting in:** the cell's history stops being an attestation of the collaborative document and becomes an attestation of a directory listing. `git blame` on a design answers \"who designed this\" with a name that did not produce those bytes.\n- **Cite:** §A (integrity / non-repudiation), §B5 (confused deputy — the hub commits on the child's say-so, and \"say-so\" is `existsSync`).\n- **Mitigation:** stage a lane only when the hub can name the byte source — `writes` (it wrote them) **or** `inDoc === onDiskText` (it verified them equal to the doc). Never stage a lane whose write was refused. Re-read and compare immediately before `git add`, inside the repo lock.\n\n---\n\n#### A3 — `historyOnly` is asserted as a security property in a comment; the server disagrees (**Medium** standalone, **High** in Chain 3)\n\n- **I would do:** do from a `fetch()` exactly what the UI now says cannot be done, on a cell where the surface that would have shown the damage was removed in the same commit.\n- **By:** the new comment in `app.jsx` reasons: *\"the browser could not act on it regardless, since this panel is read-only there.\"* `readOnly` is a React prop. `studio-manifest.mjs` says:\n\n  ```js\n  '/_api/git/commit':  { safe: null, unsafe: 'edit' },\n  '/_api/git/discard': { safe: null, unsafe: 'edit' },\n  '/_api/git/checkout':{ safe: null, unsafe: 'edit' },\n  '/_api/git/pull':    { safe: null, unsafe: 'edit' },\n  ```\n\n  `edit` is held by **member**, not just owner, and none of these is in the `REFUSED` block that `pruneForWorkspace()` 404s. So any member's session can POST them through the proxy. `commit(body)` accepts a caller-supplied `files[]` validated only by `isContainedRepoPath(dir, f)` — *repo*-contained, not design-root-contained. `discard(body)` reverts the working tree.\n\n  The change removes the only cloud surface that rendered working-tree state (the Changes tab, the \"N unsaved\" count, the `aria-label=\"Changes\"`). History remains — so a rogue *commit* is visible after the fact, but a `discard` (which leaves no commit) and a mid-flight dirty tree are now invisible to every member including the owner.\n- **Resulting in:** a member can destroy another member's uncommitted work, or author a second-committer commit, in a system whose whole design rests on \"the hub is the sole committer\" — and the tenant has no in-product view that would show it.\n- **Cite:** §A (broken access control by UI assumption), §B10 (a privileged action gated on a client render flag).\n- **Mitigation:** do not reason about authorisation in a client comment. If `commit`/`discard` are meant to be unreachable in a cell, put them in `studio-manifest.mjs`'s `REFUSED` block (they will then be pruned server-side) — and, separately, restore a read-only working-tree view so a dirty cell tree is observable.\n\n---\n\n#### A4 — `canvasSlug()` was promoted from index key to path constructor, and it has drifted from the twin that decides the real filename (**Medium**, new)\n\n- **I would do:** make the hub commit a decoy sidecar while the file the studio actually reads stays untracked — i.e. reintroduce the exact bug this change was written to fix, on names I control.\n- **By:** the new `siblingPaths()` derives `annotations` from the hub's `canvasSlug()`, and the docstring claims it \"reproduc[es] the studio's transform exactly\". It does not. The transform that decides the real on-disk path is `annotationsPath()` → `api.fileSlug()` → `canvasSlugFromRel()` (`apps/studio/canvas-slug.ts`), which **percent-decodes**:\n\n  ```ts\n  try { p = decodeURIComponent(p); } catch { /* ignore */ }\n  ```\n\n  The hub's `canvasSlug()` does not decode (and, unrelatedly, converts `\\` → `/` where the studio does not). For a canvas path containing `%` — e.g. `a%2Fb.tsx`, which reaches the design root from the seeded clone (`MAUDE_SEED_REPO` is an `https://` git clone of the tenant's repo, so filenames are an external input) or from a `git pull` — the studio resolves slug `a-b` and the hub resolves `a%2fb`. Two different sidecars; the hub commits its one, the studio reads the other. Under the new `committable` rule, if both exist, the hub commits the decoy while the live sidecar stays untracked — which is *verbatim* the failure the change's own commentary calls out as \"worse\".\n\n  Same drift on the body lane, and there it clobbers rather than decoys: `pathIndex.get('a-b')` resolves to `a/b.tsx`, so browser edits to `a%2Fb.tsx` (opened at studio slug `a-b`) are projected by the hub onto a **different canvas's file**. That half is pre-existing; A4 is that the change made the same lossy function decide a filename.\n\n  The parity test added in this diff does not catch it — `workspace-agent.test.mjs` models the studio with a hand-copied `slugFor()` (which also does not decode), not with `canvasSlugFromRel()` (which does), and the corpus is `['Foo.tsx','ui/Foo.tsx','UI/Deep/Bar Baz.tsx','a.html','.hidden/x.tsx']` — no `%`, no `\\`. The test asserts parity against the wrong twin for the new use.\n- **Resulting in:** silent divergence between what the tenant sees and what their history/mirror records, on attacker-influenceable filenames.\n- **Cite:** §A (path handling / normalisation divergence between duplicated implementations).\n- **Mitigation:** make the hub's `canvasSlug()` a literal port of `canvasSlugFromRel()` (including the decode), and change the parity test to import `canvasSlugFromRel` — the real function — with a corpus that includes `%2F`, `%2E`, backslashes and mixed case. This is the same \"twin must be provably identical\" discipline `meta-merge.mjs` already documents and tests correctly.\n\n---\n\n#### A5 — The hub's annotations write is unsanitised and uncapped, and now lands on the path everything actually reads (**Medium** standalone; **High** as Chain 2)\n\n- **I would do:** put content of my choosing into a file that the tenant's own agent tooling parses and that gets pushed to their GitHub, without going through the sanitiser that was written to stop exactly this.\n- **By:** two writers, one file, one sanitiser.\n  - Studio path: `saveAnnotations()` runs `sanitizeAnnotationSvg()` (element allowlist, `on*`/`style`/`href` denylist, `script`/`style`/`foreignObject`/`title`/`desc` **content** removal) and caps at 1 MB. `collab/persistence.ts` routes doc→file through the same function.\n  - Hub path: `filesForCanvas()` → `put(sib.annotations, content.annotations, …)` → `atomicWrite()`. **No sanitiser. No byte cap.** `readDocContent()` takes `doc.getMap('annotations').get('svg')` verbatim.\n\n  So writing the doc's `annotations` Y.Map directly over the collab WS bypasses the sanitiser entirely and the hub materialises the raw string. That much is pre-existing — but pre-change, for a *nested* canvas the hub wrote it to `ui/Card.annotations.svg`, a path the change itself describes as \"a file NOBODY READS\". This change repoints the hub's unsanitised writer at `ui-card.annotations.svg` — the exact path `GET /_api/annotations` serves, `annotations-layer.tsx` renders, `maude design read-annotations` parses, `annotate` mutates, and `canvas-rects` consumes.\n- **Resulting in:** the sanitiser becomes an optional route rather than a chokepoint, for the canvas shapes it previously still covered by accident. Plus an unbounded write primitive into the tenant's repo (no cap on any of the four lanes on the hub side, where the studio caps all of them).\n- **Cite:** §B2 (insecure output handling — content crossing into a rendering/parsing consumer without the gate its consumers assume), §B1 (the parsed text reaches a model context — see the AI section).\n- **Mitigation:** port `sanitizeAnnotationSvg` (or a plain-JS twin, tested for byte-equality like `meta-merge.mjs`) into the hub's projection, and apply `MAX_ANNOTATIONS_BYTES` / `MAX_HTML_BYTES` / `MAX_CSS_BYTES` / `MAX_META_BYTES` on the hub side. A cell should not be the one writer in the system that skips both.\n\n---\n\n#### A6 — `canvasSlug()` can return the empty string; the annotations sidecar becomes a dotfile shared by every all-dots slug (**Low**)\n\n- **I would do:** open a doc named `ws/\u003cws\u003e/\u003cbranch\u003e/..`.\n- **By:** `DOC_NAME`'s slug group is `([^/]+)` — `..`, `.`, `...` all parse. `defaultBodyPath('..')` → `'...tsx'`; `canvasSlug('...tsx')` strips the extension → `'..'` → strips leading dots → `''`. `sib.annotations` becomes `.annotations.svg` — a hidden file at the design root, which passes the containment check and which **every** all-dots slug collapses onto. Not a traversal (all `/` and `\\` are dashed before the dot strip, and the `resolve()` containment check is intact) — a namespace collision plus a hidden file the tenant will never look at.\n- **Cite:** §A (input validation on a derived path).\n- **Mitigation:** treat an empty `canvasSlug()` result as unprojectable and `return null`, the same way `slugFromDocName()` already refuses a name it cannot parse (\"guessing writes a tenant's work to the wrong file\").\n\n---\n\n#### A7 — Attribution and commit-message truthfulness regressed with the wider stage set (**Medium**)\n\n- **I would do:** get my edit into a commit authored by somebody else, and make the tenant's history describe canvases that did not change.\n- **By:** `note()` is last-writer-wins for the author across the whole coalesced batch, and the batch is now much larger: every store event contributes up to four paths regardless of change, and (under A1) batches accumulate without bound. `commitMessage(files, who)` enumerates every path in `touched`, so `git log` reads `design: update 7 canvases` listing canvases with no diff in that commit. `CommitOutcome.files` — the value surfaces report — is likewise wrong.\n\n  Under pairing the author for a loopback-originated store resolves through `attributionFor({ name: 'cell-loopback-sync' })` → the label fails the `u-[0-9a-f]{8,}` session-label test and no email is present, so history is signed `cell-loopback-sync \u003ccell-loopback-sync@workspace.invalid\u003e`. That is honest as far as it goes; the repudiation risk is not the machine label but the human one it displaces when a browser store lands last in a batch that also carries somebody else's lane.\n- **Cite:** §A (non-repudiation / audit-record integrity).\n- **Mitigation:** carry per-path attribution and commit the subset that belongs to one author, or at minimum build `commitMessage` from `git diff --cached --name-only` (what actually changed) rather than from the noted set.\n\n---\n\n### Exploit chains\n\n#### Chain 1 — \"Starve, accumulate, poison\": total silent loss of a session's history (promoted **High**)\n\nComposed from A1 (new) + a pre-existing fragility in `commitCycle` that A1 makes reliably reachable.\n\n1. As a **viewer** (or any member), post a comment every ~2 s in any canvas — `/_comments`, unrate-limited through the proxy, `comment` capability, explicitly viewer-held.\n2. Each mutation drives a doc update → `afterStoreDocument` → `note()` → the 3 s quiescence timer resets. No commit ever fires (A1).\n3. `touched` grows across **every** canvas anyone edits, because one timer governs all of them.\n4. Now poison the batch: `POST /_api/canvas` to create a canvas, let one store event note its untracked `\u003cslug\u003e.tsx`, then `DELETE /_api/canvas?file=…` (soft-delete moves it under `_trash/`).\n5. Stop commenting. The timer finally fires. `flush()` does `touched.clear()` **before** `commitCycle`. `commitCycle` runs `git add -- …` over the accumulated list; the deleted, never-tracked path yields `fatal: pathspec … did not match any files`, exit ≠ 0.\n6. `commitCycle`'s `add` failure branch returns `{ ok:false, reason:'git-failed' }` and — unlike the `commit` failure branch, which re-queues — **does not put the files back in `touched`**. The entire accumulated session is dropped from the queue, permanently.\n7. Nothing surfaces it: the log line is `[autocommit] git add failed: …` inside the container; the browser panel that would have shown a dirty tree was removed in this same change (A3).\n\nImpact: unbounded, attacker-timed, silent loss of the tenant's design history — from the role that is specified to hold no write power over the project. The mirror faithfully pushes the un-advanced `HEAD`.\n\n#### Chain 2 — Untrusted annotation content → tenant's GitHub → tenant's own agent (promoted **High**)\n\nComposed from A5 + A2 + the unattended mirror clock.\n\n1. Get content into a canvas's `annotations` lane. Two doors: `PUT /_api/annotations` from the **untrusted canvas iframe origin** (canvas-safe by design, accepts *any* `file`, `annotate` capability borrowed from the viewing member — a confused deputy the code already documents: *\"a hub-pushed canvas can write a sibling's `.annotations.svg`\"*), or the collab WS annotations lane directly, which bypasses `sanitizeAnnotationSvg` entirely.\n2. The hub materialises it **raw and uncapped** (A5) at `\u003cslug\u003e.annotations.svg` — post-change, the path everything actually reads, including for nested canvases where it previously wrote a decoy.\n3. The hub stages and commits it without having authored or verified the bytes (A2), under whoever's name landed last in the batch (A7).\n4. `scheduleMirror`'s `setInterval` pushes it. In `backup` mode that is a direct branch push to the tenant's GitHub; in `design-sync` mode it writes the `.design` tree into a folder of the tenant's **working** repo and opens a PR. No human gate on either.\n5. The file is now read back by `maude design read-annotations` — whose header states its output is \"structured JSON the `/design:new` ingest path folds into a generation brief\" — and by `annotate`, and by any Claude Code session over the mirrored repo. Sticky/text strokes carry `text` verbatim into the brief.\n\nImpact: an untrusted canvas obtains a durable, replicated, human-reviewed-nowhere write into the tenant's system of record and into their agents' context. This is the pattern the 2026 GitInject work names as the dominant failure mode — instruction-bearing files loaded from the same repository context the agent is working in — and the same shape as GitLost's public-issue → agent-context lane.\n\n#### Chain 3 — Hidden UI as the control (promoted **High**)\n\nComposed from A3 (new) + A2 (new).\n\n1. A member POSTs `/_api/git/discard` with a victim's in-flight paths (`edit` capability, allowed by the manifest, not pruned). The working tree reverts to `HEAD`.\n2. Within the same debounce window, the hub — which already noted those lanes as `committable` without writing them (A2) — runs `git add` over the reverted bytes. `git diff --cached` is empty for them → `nothing-to-commit`, so the victim's work is not merely uncommitted, it produces no record that anything was attempted.\n3. The victim's browser shows the **History** panel (this change), which lists commits. There is no commit. There is no Changes tab, no \"N unsaved\", no dirty-tree view — removed in the same diff.\n4. Alternatively the member POSTs `/_api/git/commit` with a hand-picked `files[]` (validated only as repo-contained, so *any* path in the checkout) and becomes a second committer in a system whose invariant is that the hub is the only one.\n\nImpact: destructive, cross-member, and unobservable from the product. The one comment that would have made a reviewer check — \"the browser could not act on it regardless\" — is in the diff, and is false.\n\n#### Chains explicitly ruled out\n- No path traversal out of the design root: every `/` and `\\` is dashed before the leading-dot strip, and the `resolve()`+`startsWith(designRoot + sep)` gate is intact and correctly placed before every write.\n- No arbitrary *filename* in the tenant's repo from the annotations lane: the `.annotations.svg` suffix is structural, and the stem is bounded by the doc slug.\n- **No viable chain across a `security-auditor` report for this HEAD — none exists** (`.ai/archive/logs/security-reviews/` has no `*-defender.md` for this change). The three chains above are composed from fresh attacker findings.\n- **DDR-213's wildcard, non-expiring loopback pairing token is NOT re-reported** — it is an accepted residual. Assessed against this change: it is **materially worse by one degree**, not by kind. That token's holder could previously write docs that the hub would materialise and commit *as the hub's own derived bytes*; it can now additionally cause the hub to commit **disk bytes the token holder never had to put in the doc at all** (A2). The token's blast radius grew from \"what I can say\" to \"what anything in this container has written\".\n\n### AI / MCP attack surface\n\n`security.includeAi` is unset in `.ai/workflows.config.json` → default `true`. Section is in scope.\n\n- **Tool surface:** no MCP server added, no tool description changed, no `plugins/*/` tool definition touched in this diff.\n- **Model output → downstream:** not touched. No model output flows into a shell, SQL, path or HTTP body in this change.\n- **Tool output → model input:** **yes, and it is the load-bearing one.** `\u003cslug\u003e.annotations.svg` is read by `maude design read-annotations` (own header: output is \"structured JSON the `/design:new` ingest path folds into a generation brief\"), by `annotate`, and by `canvas-rects`. Sticky and text strokes carry attacker-authored `text` into that brief verbatim. This change repoints the hub's **unsanitised, uncapped** writer at that exact file (A5) and makes it committed and mirrored (A2), so the injection is durable and replicated rather than transient on one cell's disk. `annotate.mjs` already sanitises on read for its own parse — `read-annotations.mjs` does not sanitise, it just declines to DOM-parse; regex-extracted `text` reaches the brief whatever it says. **LLM01.**\n- **Trifecta check: FAIL — all three capabilities are in one loop.**\n  - (a) *Private data:* the tenant's checkout, their `.design` tree, their GitHub mirror configuration; a control-plane-minted repo-scoped push token in the cell's `git push` argv.\n  - (b) *Untrusted content:* the annotations lane (untrusted canvas origin, DDR-054; the collab WS lane bypassing the sanitiser; filenames and file content arriving via `MAUDE_SEED_REPO`'s `https://` clone).\n  - (c) *Outbound channel:* `scheduleMirror`'s `setInterval` — an **unattended clock**, no human gate, pushing to the tenant's real GitHub (branch push) or opening a PR into their working repo (`design-sync`).\n  The mitigation is architectural, not textual: sanitise at the hub write (A5), and gate the mirror on something other than a timer for content that no member authored through a sanitising route.\n- **Confused-deputy paths:** two. (1) The untrusted canvas iframe borrows the *viewing member's* `annotate` capability to write any canvas's sidecar — pre-existing and documented in `api.ts`, but this change is what connects it to the committed, mirrored path. (2) **New:** the hub commits on the studio child's say-so, where \"say-so\" is nothing but the existence of a file at a path the hub derived (A2). §B5 both times.\n- **Excessive-agency tools:** none added. Worth naming: `/_api/git/discard` and `/_api/git/commit` are destructive, member-level, and have no per-action confirmation server-side (A3) — the confirmation was the button this change deleted.\n- **MCP supply chain:** N/A for the hub/studio diff. Note out of band: `.claude/settings.json` in this same working tree drops `vercel@claude-plugins-official` and turns on `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`. Neither is a finding; flagging that plugin-set changes ride in the same commit as cell security logic is a review-hygiene risk (a reviewer reading the hub diff will not look at them).\n- **Output handling:** the hub writes SVG to disk with no sanitiser (A5). Consumers currently parse it inertly (`svgToStrokes`, regex readers), so stored-XSS stays **latent** — but \"latent\" was `api.ts`'s justification for sanitising on write anyway, and this change removes that gate for the hub-written copy on the path that matters.\n- **Secret leakage via context:** none introduced. `mirror-push.mjs`'s discipline (token in argv only, never in `.git/config`, never logged) is intact and untouched.\n- **Jailbreak resilience for safety-critical decisions:** N/A — no model gates any decision in this change. The nearest analogue is A3, where a *client render flag* gates one, which is the same category of error.\n\n### Adversarial-creativity finding\n\n**The quiescence timer became an attacker-held lock, and the lowest role in the product holds the key.**\n\nNobody reviews a debounce for authorisation. The change reads as \"we now also note lanes we did not write\" — a widening of *what* gets committed. The interesting consequence is a widening of *when*: `note()` is not just a queue-append, it is a `clearTimeout`. Moving the early return from \"we wrote nothing\" to \"we wrote nothing and there is nothing to stage\" means the ~always-true branch now resets the timer, and a comment — the one write `role-matrix.mjs` deliberately grants a viewer, with its own row and its own test because it is \"exactly where a scope bug turns 'may leave a note' into 'may edit the project'\" — is enough to drive it. The scope bug that row was watching for did not appear in the role matrix. It appeared in a `setTimeout` in a different app, three hops downstream, and it converts \"may leave a note\" into \"may prevent the project from ever being saved\".\n\nSecond, smaller one worth the same look: `flush()` clears `touched` before `commitCycle`, and only the `commit` failure re-queues — the `add` failure does not. That asymmetry is invisible while batches are small and same-tick. A1 makes batches large and long-lived, which is what turns it from a theoretical drop into Chain 1's payload.\n\n### Recommended follow-ups\n\n1. **Bound the debounce.** Add a `maxDebounceMs` (first-note deadline) to `createAutoCommit`, and/or only `note()` genuinely-dirty lanes. Test: N no-op stores at `debounceMs/2` intervals must still produce a commit. *(A1, Chain 1 — blocker.)*\n2. **Stage only bytes the hub can account for.** `writes` ∪ `{ lanes where inDoc === onDiskText }`. Never stage a lane whose write was refused (the meta case). Test: a doc with unparseable `meta` must not stage `.meta.json`. *(A2 — blocker.)*\n3. **Make `add` failure non-destructive.** Re-queue `files` on `add.code !== 0`, exactly as the `commit` branch does, and surface the failure on a status route. *(Chain 1 — blocker.)*\n4. **Port the sanitiser and the byte caps into the hub's projection.** Plain-JS twin of `sanitizeAnnotationSvg`, tested byte-equal against the real one over a corpus, in the style `meta-merge.mjs` already established. *(A5, Chain 2 — blocker.)*\n5. **Fix the slug twin and the parity test.** Port `decodeURIComponent`; assert against `canvasSlugFromRel` (the function that decides the real path), not a hand-copied `slugFor`; corpus must include `%2F`, `%2E`, `\\`, mixed case. *(A4 — blocker.)*\n6. **Decide `/_api/git/commit` + `/_api/git/discard` in a cell, server-side.** Either move them into `studio-manifest.mjs`'s `REFUSED` block or raise them to `mirror` (owner). Delete the client-side safety claim from the `app.jsx` comment either way. *(A3, Chain 3 — blocker.)*\n7. **Restore an observable working-tree state in the cloud shell.** History-only is the right *default*; a read-only \"uncommitted in this cell\" strip is what makes a rogue `discard` or a starved autocommit visible to the tenant. *(A3, A1.)*\n8. **Refuse an empty derived slug** the way `slugFromDocName` refuses an unparseable name. *(A6 — below floor, cheap.)*\n9. **Build the commit message from `git diff --cached --name-only`,** not from the noted set, so history stops naming canvases that did not change. *(A7.)*\n10. **Gate the mirror clock on provenance** for lanes no member authored through a sanitising route — or at minimum log, per push, which commits it is replicating to the tenant's GitHub. *(Chain 2, trifecta (c).)*\n\n### Sources consulted\n- Hocuspocus store-hook + debounce semantics (`onStoreDocument` / `afterStoreDocument`, debounce \u0026 maxDebounce, flush-on-destroy) — \u003chttps://tiptap.dev/docs/hocuspocus/server/hooks\u003e, \u003chttps://github.com/ueberdosis/hocuspocus/blob/main/packages/server/src/Hocuspocus.ts\u003e. *Applies because this hub runs `@hocuspocus/server` 4.x and `afterStoreDocument`'s ~2 s cadence is what makes the 3 s `DEFAULT_DEBOUNCE_MS` starvable.*\n- GitInject — prompt injection in AI-powered CI/CD, arXiv 2606.09935 — \u003chttps://arxiv.org/html/2606.09935v1\u003e. *Applies because its named dominant failure mode (instruction-bearing files loaded from the same repository context the agent operates in) is exactly Chain 2's `.annotations.svg` → mirror → tenant's agent lane.*\n- GitLost (Noma Security) — untrusted content → GitHub agent context → exfil — \u003chttps://noma.security/blog/gitlost-how-we-tricked-githubs-ai-agent-into-leaking-private-repos/\u003e, \u003chttps://www.infoq.com/news/2026/07/gitlost-github-prompt-injection/\u003e. *Applies as the closest published analogue of the trifecta shape here: untrusted content reaching a repo-scoped agent with an outbound channel.*\n- Simon Willison's lethal-trifecta framing (private data + untrusted content + outbound channel in one loop) — used as the structural test in the AI section.\n","author":"1aGh","summary":"element cell-autocommit-annotations-path","shapes":["el_c15110fae23b614a5c47"],"mutations":[{"op":"upsert_element","element_id":"el_c15110fae23b614a5c47","kind":"security-review","name":"cell-autocommit-annotations-path"}],"provenance_only":true}}
{"hash":"sha256:1ef24ae20d54e44382a3928f46011c039f92c74229e26b4e43000436462c7da3","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":715,"recorded_at":"2026-08-06T00:00:00Z","parents":["sha256:91eb32c586059a792f60b65771c174a49a0e1911efeec020873557ae3c61fb16"],"decision":{"id":"d_422bd88733417c34692d5fdd","title":"Closed: the desktop↔cloud pairing live-run gate","rationale":"The deferred manual acceptance criterion ran against a cloud-faithful local cell and found three real defects in a code-complete, test-green feature: the hub never committed paired edits (sole-committer passed vacuously with zero), the annotations sidecar was written to a path no reader uses (and committed, and mirrorable), and the browser offered Save/Publish for work already committed. A defender+attacker fan-out on the fix found three more — the commit path bypassing the meta write-gate, unbounded quiescence batching reachable by the viewer role via comments, and an unscoped no-op probe that could wedge autocommit permanently. All fixed with regression tests verified to fail without them. Lesson: an acceptance criterion parked because it needs real infrastructure is the only test that runs the real topology.","author":"1aGh","summary":"element feature-desktop-cloud-live-pairing","supersedes":["d_d764549fbaaa11c9c5e64f55"],"shapes":["el_8a7d1becc749b9eded03"],"targets":["el_8a7d1becc749b9eded03"],"mutations":[{"op":"upsert_element","element_id":"el_8a7d1becc749b9eded03","kind":"plan","name":"feature-desktop-cloud-live-pairing","props":{"status":"done"}}]}}
{"hash":"sha256:c8d1ab6c409778548c397029e1e4777ad1e6a66a6e8c9732330a39232b6d9a27","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":716,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:1ef24ae20d54e44382a3928f46011c039f92c74229e26b4e43000436462c7da3"],"decision":{"id":"d_62552bcaacc0b9db14012d43","title":"ACP writes are project-scoped; ACP bridges outlive their socket; the desktop sidecar becomes a pool","rationale":"Four structural changes, executed together from .ai/plans/feature-acp-write-path-scope.md. They ship as one because the last one is only correct in the presence of the others.\n\n1. WHERE THE WRITE DECISION LIVES — MOVED, not widened. `Edit`/`Write`/`NotebookEdit` are REMOVED from `MAUDE_DEFAULT_ALLOWED_TOOLS` (apps/studio/acp/bridge.ts). A bare tool name on that list means the Claude CLI approves the call ITSELF and the bridge's `requestPermission` gate is never invoked, so a path condition cannot be expressed next to an allow-list entry at all — only by moving the decision. Auto-approval now happens inside `requestPermission` via the new module `apps/studio/acp/write-scope.ts` (`writeTargetsInsideProject`), which grants exactly DDR-184's no-prompt-per-edit outcome for every write resolving inside the session's project root, and routes every other write to the prompt that already existed (Milestone B). This SUPERSEDES the claim recorded in bridge.ts that 'auto-approving Edit/Write is the accepted residual: edits land in the served project and are reversible via the _history/ snapshot stack' — that was not merely incomplete, it was the wrong claim: nothing constrained the target path, so neither half held.\n\nMeasured adapter contract (claude-agent-acp@0.57.0, 2026-08-07) that shaped the design and disagrees with the ACP type declarations: `toolCall.locations[].path` is `input.file_path` VERBATIM (no normalization/absolutization), so the plan's locations-vs-rawInput cross-check is TAUTOLOGICAL for Write/Edit; `NotebookEdit` has no case in the adapter's tool mapper and emits NO locations at all, making the rawInput (`notebook_path`) fallback mandatory; and the permission request carries NO TOOL NAME — it rides only on the streamed `tool_call`'s `_meta.claudeCode.toolName`, which the bridge now harvests into a bounded map. Every failure of that channel fails closed to the prompt.\n\nRejected: sniffing `toolCall.kind === 'edit'` (shared with any future edit-shaped tool) or `title` (a display string). Rejected: falling back to `allow_always` when no `allow_once` is offered — the adapter turns `allow_always` into a session-wide `addRules` standing rule for the tool NAME, which would silently restore the unscoped Write grant from inside the code removing it.\n\n2. HOW CONSENT IS EXPOSED for an out-of-project write. `allow_always` options are stripped SERVER-side before being offered, and `resolvePermission` validates against the same filtered set, so consent is per-call and a crafted frame cannot pin a stripped option. The prompt renders the REALPATH-RESOLVED absolute path and substitutes its own headline — the adapter builds `toolCall.title` from the model's string, so a write to `~/.zshenv` was headlined 'Write docs/../../../.zshenv'.\n\n3. ACP BRIDGE LIFETIME — RE-KEYED from socket to chat (apps/studio/acp/index.ts). `bridges` was keyed by `ws.data.id` and `onClose` called `bridge.stop()` immediately, so any socket close killed the running agent; `RepoBranchSwitcher.jsx` reloads unconditionally at three call sites, so a branch switch silently destroyed in-flight work. Bridges are now keyed by CHAT and outlive their socket, with a grace TTL, a `MAX_DETACHED_BRIDGES` ceiling and least-recently-active eviction (detaching removed the natural reaper that socket-close used to be). The re-attach seam is the TRANSCRIPT LINE COUNT: every `update` frame carries its `seq`, `GET /_api/acp/chat` returns the marker as `X-Maude-Chat-Seq`, and a new `attach` frame replays exactly the gap — no duplicated tail, no hole. A permission request raised while detached still hits the existing timeout and fail-closed denies; 'no UI attached' must never read as consent.\n\n4. DESKTOP SIDECAR — SINGLE CHILD BECOMES A POOL (apps/desktop/src-tauri/src/sidecar.rs). `switch_project` killed the child so the supervisor would respawn at the new root, tearing down the whole server process and every chat in the project being left. It is now spawn-or-attach over a `MAX_INSTANCES`-capped pool keyed by project root; the supervisor respawns the root that actually died rather than 'the current project'; `_server.json` is deleted ONLY on the spawn path (with a pool it may be a live instance's state, and deleting it would orphan a healthy server); the idle reaper asks each instance's own new `GET /_api/acp/running` endpoint; and `kill_server` drains the whole pool on quit. This is Options 1+2 of the plan's table. Option 3 (one long-lived cross-project agent host) was REJECTED: an agent process spanning projects would dissolve the per-project boundary decision 1 draws and reopen the scope question in the worst place. A pool keeps repoRoot — and therefore the write gate's scope root — one-to-one with a server.\n\nTHE SEAM BETWEEN THEM: the write gate resolves against a `scopeRoot` PINNED at bridge construction and never recomputed. Today a bridge's lifetime is one project's lifetime so the pin looks like ceremony; once decisions 3 and 4 let a session outlive a project switch, a gate re-reading 'the open project' would hand project A's session write access to project B. The pin ships with the lifetime change or not at all.\n\nSCOPE NOT CLAIMED (recorded so no later document asserts a property the system lacks): `Read`/`Grep`/`Glob` stay unscoped — this closes WRITE egress, not read. And `Bash(maude:*)` remains auto-approved and unscoped, so this does NOT establish 'an ACP session cannot write outside its project'. Named open items found by the Task-5 audit: (a) `maude design draw-build --script` ends in `exec bun run` and `to-lottie` runs an agent-authored python — an in-project write (now correctly auto-approved) followed by that verb is arbitrary code execution with zero prompts; (b) unscoped `--out`/`--out-dir` on eight helper verbs, of which `draw-build --out` writes agent-authored content to an arbitrary path; (c) `--root` re-targeting lets several helpers write under ANOTHER project's design root.\n\nSTATUS AT RECORDING: implementation complete and green (91 feature tests, full dev-server suite clean apart from one pre-existing flaky export-jobs test; biome/cargo check/clippy/import-coherence/version-parity all clean). The MANDATORY security-auditor + ethical-hacker fan-out this repo requires for every ACP-permission-surface change had NOT returned at recording time, and the desktop A→B→A switch plus the e2e scenario are verified by construction only, not by a live run.","author":"1aGh","summary":"element acp-write-path-scope-and-session-lifetime; element maude; element dev; link decision:acp-write-path-scope-and-session-lifetime-IN_REPO-\u003erepo:maude; link decision:acp-write-path-scope-and-session-lifetime-IN_DEPT-\u003edept:dev; link decision:acp-write-path-scope-and-session-lifetime-SUPERSEDES-\u003edecision:DDR-184; link decision:acp-write-path-scope-and-session-lifetime-EXTENDS-\u003edecision:DDR-185; link decision:acp-write-path-scope-and-session-lifetime-REFERENCES-\u003edecision:DDR-125; link decision:acp-write-path-scope-and-session-lifetime-REFERENCES-\u003edecision:DDR-054; link decision:acp-write-path-scope-and-session-lifetime-REFERENCES-\u003edecision:DDR-166","shapes":["el_0ab75d7d664128328b86","el_1baa412e5f22c7573a57","el_42b070d9886acea59c36","el_6d3b97502f935e613ec2","el_c6548aed5761e851aa21","el_cdc050b23678a64f9b51","el_ec5ef965b4979595c248","el_fe9cc86d9dd12a98652b"],"targets":["el_fe9cc86d9dd12a98652b"],"mutations":[{"op":"upsert_element","element_id":"el_fe9cc86d9dd12a98652b","kind":"decision","name":"acp-write-path-scope-and-session-lifetime","props":{"date":"2026-08-07","path":".ai/plans/feature-acp-write-path-scope.md","tags":"repo:maude,dept:dev,acp,security,permission-surface,session-lifetime,desktop"}},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_fe9cc86d9dd12a98652b","from_id":"el_fe9cc86d9dd12a98652b","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_fe9cc86d9dd12a98652b","from_id":"el_fe9cc86d9dd12a98652b","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_6d3b97502f935e613ec2","kind":"decision","name":"DDR-184"},{"op":"add_link","element_id":"el_fe9cc86d9dd12a98652b","from_id":"el_fe9cc86d9dd12a98652b","to_id":"el_6d3b97502f935e613ec2","link_kind":"SUPERSEDES"},{"op":"upsert_element","element_id":"el_0ab75d7d664128328b86","kind":"decision","name":"DDR-185"},{"op":"add_link","element_id":"el_fe9cc86d9dd12a98652b","from_id":"el_fe9cc86d9dd12a98652b","to_id":"el_0ab75d7d664128328b86","link_kind":"EXTENDS"},{"op":"upsert_element","element_id":"el_1baa412e5f22c7573a57","kind":"decision","name":"DDR-125"},{"op":"add_link","element_id":"el_fe9cc86d9dd12a98652b","from_id":"el_fe9cc86d9dd12a98652b","to_id":"el_1baa412e5f22c7573a57","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_cdc050b23678a64f9b51","kind":"decision","name":"DDR-054"},{"op":"add_link","element_id":"el_fe9cc86d9dd12a98652b","from_id":"el_fe9cc86d9dd12a98652b","to_id":"el_cdc050b23678a64f9b51","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_c6548aed5761e851aa21","kind":"decision","name":"DDR-166"},{"op":"add_link","element_id":"el_fe9cc86d9dd12a98652b","from_id":"el_fe9cc86d9dd12a98652b","to_id":"el_c6548aed5761e851aa21","link_kind":"REFERENCES"}]}}
{"hash":"sha256:3a7f50996aa0344daaa5d9a109f25904da337a72bad43949b5e1d7f1dfa37d4f","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":717,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:c8d1ab6c409778548c397029e1e4777ad1e6a66a6e8c9732330a39232b6d9a27"],"decision":{"id":"d_2e2a8c8625f532235dbe4cda","title":"Cut the read-only Bash verb group from the ACP allow-list — prefix rules don't stop `\u003e`","rationale":"The nine read-only filesystem verbs — Bash(ls|cat|pwd|head|tail|wc|tree|file|stat:*) — are REMOVED from `MAUDE_DEFAULT_ALLOWED_TOOLS` (apps/studio/acp/bridge.ts). `bashRules` is now exactly `['Bash(maude:*)']`. This SUPERSEDES the DDR-185 addendum's decision to admit that group.\n\nWHY. Claude Code's `Bash(\u003ccmd\u003e:*)` prefix rule does NOT reject shell redirection, and every verb in the group accepts `\u003e`. So `cat \u003e ~/.zshenv \u003c\u003c'EOF' … EOF` matched `Bash(cat:*)`, was self-approved by the CLI, never reached the bridge's `requestPermission`, and wrote MODEL-AUTHORED ARBITRARY CONTENT to an ARBITRARY PATH in one command — with no write tool involved at all. Found and PoC'd live against claude 2.1.220 under `--permission-mode default` by the mandatory security-auditor fan-out (finding F1, HIGH). The group was therefore an unrestricted arbitrary-WRITE surface, not a read surface, and a cheaper delivery path for the DDR-054 / A2 chain than the write tools sitting beside it — which the sibling decision `acp-write-path-scope-and-session-lifetime` had just spent a whole feature scoping. That feature's write gate does not and cannot see a Bash call.\n\nThe prior justification (kept in the source, annotated) argued the group added no incremental READ capability because Read/Grep/Glob are already auto-approved and unscoped. That reasoning was CORRECT AND BESIDE THE POINT: it reasoned about the read surface and never asked what the shell does with the same grant. Recording this because the failure mode is a reasoning error, not an oversight — the next person to add a 'harmless, read-only' Bash verb will make it again.\n\nCUT, NOT PATCHED — the same call DDR-185's own security addendum made for `find` (`-exec` is arbitrary command execution) and `Bash(agent-browser:*)` (a zero-click session-hijack primitive), and for the identical reason: a prefix rule cannot inspect what follows the command name, so there is nothing to patch. `pwd` is cut too; `pwd \u003e file` redirects exactly like the rest, and 'but pwd is harmless' is the same beside-the-point argument.\n\nACCEPTED COST, deliberately: these verbs prompt again, giving back part of DDR-185's friction win. Judged the right trade — a write gate whose headline claim is defeated by `cat \u003e` is worse than a prompt on `ls`. Read/Grep/Glob remain auto-approved, so the actual read workflow the group existed to smooth is untouched; what returns is a prompt on the CONVENIENCE INTERFACE to power already granted. Route them back, if the friction proves unacceptable, through a hardened `maude design` wrapper verb the way `agent-browser-safe` and `curl-local` already are (covered for free by `Bash(maude:*)`, zero Bash-surface widening) — a wrapper CAN reject redirection.\n\nRESIDUAL, NAMED SO THIS DOES NOT READ AS EXHAUSTIVE (the exact mistake the F1 audit corrected): `Bash(maude:*)` has the SAME redirection property — `maude design slug foo \u003e ~/.zshenv` matches the prefix. NOT cut, because `Bash(maude:*)` IS the design workflow (DDR-062) and removing it would put a prompt on every step of the thing DDR-184 exists to unblock — a materially larger decision than dropping nine convenience verbs. What redirection buys there is helper-CHOSEN stdout to an attacker-chosen path, weaker than model-authored content, but it compounds with the `exec bun run` of `draw-build` and the unscoped `--out` of eight helper verbs on the same rule. Open MEDIUM.\n\nCONSEQUENTLY the write-scope feature's claim is bounded: the direct A2 delivery path is closed THROUGH THE WRITE TOOLS AND THIS GROUP, not in general. No document may state it more strongly while `Bash(maude:*)` carries those residuals.\n\nGuarded by a dedicated test in `apps/studio/test/acp-session-allowed-tools.test.ts` asserting each of the nine is absent, carrying the PoC and its provenance, so re-adding one fails loudly instead of silently reopening the hole.","author":"1aGh","summary":"element acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write; element maude; element dev; link decision:acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write-IN_REPO-\u003erepo:maude; link decision:acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write-IN_DEPT-\u003edept:dev; link decision:acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write-SUPERSEDES-\u003edecision:DDR-185; link decision:acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write-EXTENDS-\u003edecision:acp-write-path-scope-and-session-lifetime; link decision:acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write-REFERENCES-\u003edecision:DDR-184; link decision:acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write-REFERENCES-\u003edecision:DDR-062; link decision:acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write-REFERENCES-\u003edecision:DDR-054","shapes":["el_0ab75d7d664128328b86","el_12fbeaae68862adfff12","el_42b070d9886acea59c36","el_6d3b97502f935e613ec2","el_74b127e473bd49413e26","el_cdc050b23678a64f9b51","el_ec5ef965b4979595c248","el_fe9cc86d9dd12a98652b"],"targets":["el_74b127e473bd49413e26"],"mutations":[{"op":"upsert_element","element_id":"el_74b127e473bd49413e26","kind":"decision","name":"acp-readonly-bash-verbs-cut-redirection-is-arbitrary-write","props":{"date":"2026-08-07","path":"apps/studio/acp/bridge.ts","tags":"repo:maude,dept:dev,acp,security,permission-surface,allow-list,bash"}},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_74b127e473bd49413e26","from_id":"el_74b127e473bd49413e26","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_74b127e473bd49413e26","from_id":"el_74b127e473bd49413e26","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_0ab75d7d664128328b86","kind":"decision","name":"DDR-185"},{"op":"add_link","element_id":"el_74b127e473bd49413e26","from_id":"el_74b127e473bd49413e26","to_id":"el_0ab75d7d664128328b86","link_kind":"SUPERSEDES"},{"op":"upsert_element","element_id":"el_fe9cc86d9dd12a98652b","kind":"decision","name":"acp-write-path-scope-and-session-lifetime"},{"op":"add_link","element_id":"el_74b127e473bd49413e26","from_id":"el_74b127e473bd49413e26","to_id":"el_fe9cc86d9dd12a98652b","link_kind":"EXTENDS"},{"op":"upsert_element","element_id":"el_6d3b97502f935e613ec2","kind":"decision","name":"DDR-184"},{"op":"add_link","element_id":"el_74b127e473bd49413e26","from_id":"el_74b127e473bd49413e26","to_id":"el_6d3b97502f935e613ec2","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_12fbeaae68862adfff12","kind":"decision","name":"DDR-062"},{"op":"add_link","element_id":"el_74b127e473bd49413e26","from_id":"el_74b127e473bd49413e26","to_id":"el_12fbeaae68862adfff12","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_cdc050b23678a64f9b51","kind":"decision","name":"DDR-054"},{"op":"add_link","element_id":"el_74b127e473bd49413e26","from_id":"el_74b127e473bd49413e26","to_id":"el_cdc050b23678a64f9b51","link_kind":"REFERENCES"}]}}
{"hash":"sha256:0b46cbc0b0c47e72ef46368c79d4231f55901ee19efe6cf6ff6e3e2ab2e606aa","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":718,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:3a7f50996aa0344daaa5d9a109f25904da337a72bad43949b5e1d7f1dfa37d4f"],"decision":{"id":"d_1771d8dabf6c34d7b485b15c","title":"ACP control frames are attachment-scoped — a socket may only address chats it is attached to","rationale":"A `/_ws/acp` socket may act on a chat ONLY if it is attached to that chat. Enforced in one place — `entryForSocket` (apps/studio/acp/index.ts) — which every control frame now routes through: `cancel`, `permission-response`, `elicitation-response`, `set-mode`, `set-config`. `prompt`/`warm` deliberately do NOT, because opening a chat IS an attach and that is the feature.\n\nWHY THIS NEEDS SAYING OUT LOUD. The property used to be free. Before bridges were re-keyed by chat (decision `acp-write-path-scope-and-session-lifetime`, Addendum Task 8), every control-frame lookup was `bridges.get(ws.data.id)` — the key WAS the socket, so a frame was structurally incapable of naming another socket's bridge. Re-keying moved the key into the FRAME (`{t:'cancel', chat}`), and the first cut resolved that `chat` against the whole `bridges` map. That silently widened the authorization boundary: any socket could cancel another chat's in-flight turn, flip its permission mode or model, or answer its pending permission request. Caught during the write-path-scope security fan-out; fixed by requiring membership in `wsChats` before the lookup resolves.\n\nThe practical exposure was low — `/_ws/acp` is main-origin + loopback only (never the untrusted canvas origin, DDR-054/DDR-123), and permission ids are `crypto.randomUUID` nonces only ever sent to attached sockets, so answering another chat's prompt required guessing a UUID. But 'you would have to guess a UUID' is a materially weaker guarantee than 'the frame cannot name that bridge at all', and DDR-125 F1's posture is explicitly the latter: a loopback frame must not be able to pin a value it was never offered. `set-mode` is the sharpest case — `bypassPermissions` is a real advertised mode, and flipping another chat into it would disable that chat's entire permission gate.\n\nTHE GENERAL LESSON, which is why this is recorded separately rather than folded into the re-keying decision: re-keying a registry can silently DELETE an authorization property that was never written down because the old key structurally provided it. Any future change to what `bridges` is keyed by must re-establish this explicitly. Pinned by two tests in `acp-bridge-lifetime.test.ts` — a socket naming another socket's chat is a no-op (both bridges survive), and a socket naming its OWN chat still works (the client echoes `chat` on every response frame, so over-tightening would break the normal path).","author":"1aGh","summary":"element acp-control-frames-are-attachment-scoped; element maude; element dev; link decision:acp-control-frames-are-attachment-scoped-IN_REPO-\u003erepo:maude; link decision:acp-control-frames-are-attachment-scoped-IN_DEPT-\u003edept:dev; link decision:acp-control-frames-are-attachment-scoped-EXTENDS-\u003edecision:acp-write-path-scope-and-session-lifetime; link decision:acp-control-frames-are-attachment-scoped-REFERENCES-\u003edecision:DDR-125; link decision:acp-control-frames-are-attachment-scoped-REFERENCES-\u003edecision:DDR-123","shapes":["el_1baa412e5f22c7573a57","el_42b070d9886acea59c36","el_c1586d2aa659981b5f4e","el_ec5ef965b4979595c248","el_f9abaaaba5f8310f0b25","el_fe9cc86d9dd12a98652b"],"targets":["el_f9abaaaba5f8310f0b25"],"mutations":[{"op":"upsert_element","element_id":"el_f9abaaaba5f8310f0b25","kind":"decision","name":"acp-control-frames-are-attachment-scoped","props":{"date":"2026-08-07","path":"apps/studio/acp/index.ts","tags":"repo:maude,dept:dev,acp,security,authorization,websocket,session-lifetime"}},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_f9abaaaba5f8310f0b25","from_id":"el_f9abaaaba5f8310f0b25","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_f9abaaaba5f8310f0b25","from_id":"el_f9abaaaba5f8310f0b25","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_fe9cc86d9dd12a98652b","kind":"decision","name":"acp-write-path-scope-and-session-lifetime"},{"op":"add_link","element_id":"el_f9abaaaba5f8310f0b25","from_id":"el_f9abaaaba5f8310f0b25","to_id":"el_fe9cc86d9dd12a98652b","link_kind":"EXTENDS"},{"op":"upsert_element","element_id":"el_1baa412e5f22c7573a57","kind":"decision","name":"DDR-125"},{"op":"add_link","element_id":"el_f9abaaaba5f8310f0b25","from_id":"el_f9abaaaba5f8310f0b25","to_id":"el_1baa412e5f22c7573a57","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_c1586d2aa659981b5f4e","kind":"decision","name":"DDR-123"},{"op":"add_link","element_id":"el_f9abaaaba5f8310f0b25","from_id":"el_f9abaaaba5f8310f0b25","to_id":"el_c1586d2aa659981b5f4e","link_kind":"REFERENCES"}]}}
{"hash":"sha256:e92667d3c5c67ff2047b9cd3f57e2b44597801112e95372b9bbee14252be9a89","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":719,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:0b46cbc0b0c47e72ef46368c79d4231f55901ee19efe6cf6ff6e3e2ab2e606aa"],"decision":{"id":"d_6230b8b1f2ab9d73faec01e4","title":"`_server.json`'s url is untrusted at every consumer — validate loopback at each, not just at navigate","rationale":"`_server.json`'s `url` is UNTRUSTED INPUT at EVERY consumer, not just at the webview-navigate site, and each consumer must run it through `server_json::is_loopback_url` before use.\n\nWHY THIS IS A STANDING RULE AND NOT A ONE-OFF FIX. The file lives under the served PROJECT root, which DDR-054 designates untrusted, and it is now WRITABLE BY THE AGENT WITHOUT A PROMPT: the write gate added in `acp-write-path-scope-and-session-lifetime` auto-approves in-project writes, and `_server.json` is in-project. So a prompt-injected session can choose its contents freely. `server_json.rs` already documented the field as untrusted and shipped `is_loopback_url` for it — citing an earlier security review F3 — yet the FIRST NEW consumer written after that (the sidecar pool's idle reaper, `has_running_chat` in sidecar.rs) passed the raw string straight into curl's argv, skipping a guard sitting 25 lines above it in the same file. The failure mode is that a validator attached to ONE call site reads as a property of the SYSTEM; it is not, and the second consumer inherits nothing.\n\nWHAT IT BOUGHT AN ATTACKER (security-auditor A1, HIGH, blocking): `{\"url\":\"http://attacker.tld\"}` → an outbound beacon from the native app, entirely off the permission surface; `{\"url\":\"-K/path/to/attacker.conf\"}` → the leading `-` makes curl read the argument as `--config`, a form that can specify BOTH an arbitrary URL and an arbitrary output file, i.e. arbitrary outbound request plus arbitrary local write from the signed desktop process. Triggered whenever `reap_instances` probes that project — a background action the user never initiates.\n\nTHE RULE, in three parts, all now applied in `has_running_chat`:\n  1. Parse to a `tauri::Url` and require `is_loopback_url` — the SAME predicate the navigate site uses. One rule, one place; do not hand-roll a second policy.\n  2. Rebuild any derived URL from the PARSED value's `origin().ascii_serialization()`, never from the raw string and never from `Url`'s `Display`. Raw preserves a leading `-`; Display preserves whatever PATH the file supplied, so `http://localhost:4399/foo` would concatenate into `/foo_api/acp/running`, 404, and — because this particular probe reads a non-answer as 'busy' — quietly make that project un-evictable. Found while self-checking the fix for (1).\n  3. When shelling out, pass `--` before the URL so no argv position can be reinterpreted as a flag even if validation is later weakened.\nFailure of any check answers with the caller's SAFE default (here: 'evictable'), never with a fallback that proceeds using the untrusted value.\n\nSCOPE. Current consumers are the webview navigate in `sidecar.rs::switch_project` (already guarded, DDR-109), `lib.rs`'s export-download URL resolution, and `has_running_chat`. Any new reader of `read_server_url` inherits this obligation. The pool (Task 10) multiplies the exposure: several `_server.json` files from several projects are now live at once, and one poisoned project's file is read by machinery acting on behalf of the whole app.","author":"1aGh","summary":"element server-json-url-is-untrusted-validate-at-every-consumer; element maude; element dev; link decision:server-json-url-is-untrusted-validate-at-every-consumer-IN_REPO-\u003erepo:maude; link decision:server-json-url-is-untrusted-validate-at-every-consumer-IN_DEPT-\u003edept:dev; link decision:server-json-url-is-untrusted-validate-at-every-consumer-EXTENDS-\u003edecision:DDR-109; link decision:server-json-url-is-untrusted-validate-at-every-consumer-REFERENCES-\u003edecision:acp-write-path-scope-and-session-lifetime; link decision:server-json-url-is-untrusted-validate-at-every-consumer-REFERENCES-\u003edecision:DDR-054","shapes":["el_2bcf2d5523ddb4a0986d","el_42b070d9886acea59c36","el_6d0ab3ef7c0d465f5d9a","el_cdc050b23678a64f9b51","el_ec5ef965b4979595c248","el_fe9cc86d9dd12a98652b"],"targets":["el_6d0ab3ef7c0d465f5d9a"],"mutations":[{"op":"upsert_element","element_id":"el_6d0ab3ef7c0d465f5d9a","kind":"decision","name":"server-json-url-is-untrusted-validate-at-every-consumer","props":{"date":"2026-08-07","path":"apps/desktop/src-tauri/src/sidecar.rs","tags":"repo:maude,dept:dev,desktop,security,untrusted-input,loopback,sidecar"}},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_6d0ab3ef7c0d465f5d9a","from_id":"el_6d0ab3ef7c0d465f5d9a","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_6d0ab3ef7c0d465f5d9a","from_id":"el_6d0ab3ef7c0d465f5d9a","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_2bcf2d5523ddb4a0986d","kind":"decision","name":"DDR-109"},{"op":"add_link","element_id":"el_6d0ab3ef7c0d465f5d9a","from_id":"el_6d0ab3ef7c0d465f5d9a","to_id":"el_2bcf2d5523ddb4a0986d","link_kind":"EXTENDS"},{"op":"upsert_element","element_id":"el_fe9cc86d9dd12a98652b","kind":"decision","name":"acp-write-path-scope-and-session-lifetime"},{"op":"add_link","element_id":"el_6d0ab3ef7c0d465f5d9a","from_id":"el_6d0ab3ef7c0d465f5d9a","to_id":"el_fe9cc86d9dd12a98652b","link_kind":"REFERENCES"},{"op":"upsert_element","element_id":"el_cdc050b23678a64f9b51","kind":"decision","name":"DDR-054"},{"op":"add_link","element_id":"el_6d0ab3ef7c0d465f5d9a","from_id":"el_6d0ab3ef7c0d465f5d9a","to_id":"el_cdc050b23678a64f9b51","link_kind":"REFERENCES"}]}}
{"hash":"sha256:a308ff3b994f100a00db692a447bb4877837e511b2b652328f1e29865f3a30fc","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":720,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:e92667d3c5c67ff2047b9cd3f57e2b44597801112e95372b9bbee14252be9a89"],"decision":{"id":"d_b9c531628fc001b4a034252e","title":"Closed: ACP write-path scope + session lifetime","rationale":"Feature closed 2026-08-07 on `main` (640b69db + 8ac447d9). Write gate, allow-list cut, chat-keyed bridges with a transcript-line re-attach seam, branch-switch warning, sidecar pool, pinned scope root. Both halves of the mandatory security fan-out ran: security-auditor PASS WITH SUGGESTIONS; ethical-hacker NEEDS FIXES (CRITICAL) -\u003e fixed -\u003e PASS WITH SUGGESTIONS. The adversarial half found a `.git/config` core.fsmonitor -\u003e RCE chain the defender pass missed entirely ('it examined the gate; A1 lives in what the gate lets through'), which is the retro's main transferable lesson: the two halves are not redundant. Scope claim is deliberately qualified and binds release notes and product copy, not just the record: write egress is closed THROUGH THE WRITE TOOLS AND THE READ-ONLY BASH GROUP, not in general - `Bash(maude:*)` remains auto-approved and reaches `exec bun run` plus unscoped --out (9 helpers) and --root (26). Open, recorded with owners in the archived plan: the `Bash(maude:*)` HIGH, the unscoped-destination class, LOW-1 (MAX_BRIDGES exhaustible by attached entries), LOW-3 (/_api/git/status has no host guard), the F5 re-rate of the one-click bypassPermissions path, and the A3 discovery question (.claude/skills, .mcp.json). Never verified: no exploit was executed in either review, Windows was not tested on real hardware, the desktop A-\u003eB-\u003eA switch and the e2e scenario are correct-by-construction only.","author":"1aGh","summary":"element feature-acp-write-path-scope; element maude; element dev; link plan:feature-acp-write-path-scope-IN_REPO-\u003erepo:maude; link plan:feature-acp-write-path-scope-IN_DEPT-\u003edept:dev","supersedes":["d_7e4a63dc8353a12006653d26"],"shapes":["el_42b070d9886acea59c36","el_63e33b67b7bb66cc2f60","el_ec5ef965b4979595c248"],"targets":["el_63e33b67b7bb66cc2f60"],"mutations":[{"op":"upsert_element","element_id":"el_63e33b67b7bb66cc2f60","kind":"plan","name":"feature-acp-write-path-scope","props":{"date":"2026-08-07","path":".ai/plans/archive/feature-acp-write-path-scope.md","status":"done"}},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_63e33b67b7bb66cc2f60","from_id":"el_63e33b67b7bb66cc2f60","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_63e33b67b7bb66cc2f60","from_id":"el_63e33b67b7bb66cc2f60","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"}]}}
{"hash":"sha256:dcb281b2478330cc51462a16b3f80c2591fcc279638221a4ff448721d07d055d","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":721,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:a308ff3b994f100a00db692a447bb4877837e511b2b652328f1e29865f3a30fc"],"decision":{"id":"d_2d9c16e53c660de267546915","title":"RCA: cell live pairing never engages (env drift) + Open-in-Maude reports syncing that never starts","rationale":"# RCA: live pairing never engages on the cell · and \"Open in Maude\" reports syncing that never starts\n\n**Date:** 2026-08-07 · **Reporter:** 1aGh · **Investigator:** Claude (Opus 5)\n**Source:** manual (tracker is `github`, but this arrived as a description, not an issue number)\n\n## Summary\n\nTwo defects, unrelated in code and identical in shape: **a status that reports an\nintention as if it were an outcome.**\n\n**A.** `CELL_LIVE_PAIRING` is deployed, the Worker computes `MAUDE_CELL_PAIRING: \"1\"`\nfor `alligators`, the fleet runs `maude-cell:v0.56.0` — and live pairing is *not*\nrunning. Two explicit `/_cell/restart` cycles (the runbook's sanctioned recompute)\ndid not change that. The release-guide's claim that `/_cell/restart` forces a fresh\n`cellEnv` is, at minimum, incomplete.\n\n**B.** After \"Open in Maude\" the desktop says *\"Syncing with \u003cproject\u003e — 68\ncanvases.\"* That sentence is emitted when the sync runtime has been **asked to\nstart** and the project **has** 68 syncable canvases. It is not evidence that a\nsocket opened, that the hub accepted the token, or that one byte moved. It is\nrendered once, statically, and never corrected — so a lane that dies immediately\nafter start looks identical to one that works.\n\n## A. Live pairing never engages\n\n### Confirmed (measured, not inferred)\n\n| Check | Result |\n| --- | --- |\n| `cell-loopback-sync` token in the cell's token store | **absent** (42 tokens, 0 matching) — queried via `/admin/api/tokens` with the derived operator bearer |\n| `peersCount` after a clean restart, no tabs open | **0** |\n| `CELL_LIVE_PAIRING` on the deployed Worker | **present**, `\"alligators\"` (Cloudflare API, not the repo) |\n| Deployed Worker code | contains `...livePairingEnabled(env, tenantId) ? { MAUDE_CELL_PAIRING: \"1\" } : {}` |\n| `livePairingEnabled()` logic | returns `true` for `alligators` (also with padding / different case) |\n| Container application | `maude-cell:v0.56.0`, config version 34, `instances: 5` |\n\nThe token is the load-bearing signal: `mintLoopbackSyncToken` runs at **hub boot**\nand is the first thing pairing does. `addToken` is `INSERT OR REPLACE` by label, so\na pre-existing token would not raise the count — but it would still be *in the\nlist*. It is not. Therefore `MAUDE_CELL_PAIRING` is not truthy in the hub process,\ni.e. **the variable is not reaching the container**.\n\n### Root cause (confirmed mechanism, one gap)\n\n`@cloudflare/containers` applies `envVars` **only on an actual container start**:\n\n```js\n// dist/lib/container.js — startContainerIfNotRunning\nif (this.startInFlight) return this.startInFlight;\nif (this.container.running) {            // ← fast path, returns here\n  ...\n  return 0;\n}\nconst startPromise = this.doStartContainer(waitOptions, options);\n// ...\n// doStartContainer, ~40 lines later, is the ONLY reader of options.envVars:\nconst envVars = options?.envVars ?? this.envVars;\nif (envVars \u0026\u0026 Object.keys(envVars).length \u003e 0) startConfig.env = envVars;\n```\n\nSo `await this.start({ envVars: await cellEnv(...) })` in `cell-do.mjs` is, against a\nrunning container, a **silent no-op for environment** — exactly what its own comment\nsays (\"Idempotent when already running\") without spelling out the consequence. Any\npath where the container is up before the DO's `fetch()` calls `start()` keeps the\nprevious environment forever, and nothing anywhere reports the discrepancy.\n\n**The gap:** `restart()` does `container.destroy()`, so the *next* request should\nfind `running === false` and start with fresh env. It did not — across two attempts,\nthe second with 75 s of settle time and a single wake request. The most probable\nremaining explanation is that the container is being started by the **platform**\nrather than by the DO (`instances: 5` for a single tenant suggests instances are\nmaintained independently of the DO's start call), which would mean the stored start\nconfig — not `cellEnv` — decides the environment on every platform-initiated boot.\n\n**This is not yet proven.** The decisive evidence is the hub's own stdout: it logs\n`[hub] live pairing ON — minted…` or `could not mint…` at boot. Container stdout was\nnot reachable from here.\n\n### Impact\n\n- **Severity: high for the feature, zero for data.** Live pairing — the headline of\n  the v0.56.0 cloud release — is dark in production for the one pilot tenant it was\n  enabled for. Desktop and browser still persist to the same disk; nothing is lost,\n  they just never meet live.\n- **Wider blast radius than pairing:** *any* future change to `cellEnv` derivation\n  is subject to the same silence. This already burned v0.54.0 (`HUB_PUBLIC_URL` /\n  `frame-ancestors`), and the runbook's documented remedy is now known to be\n  insufficient at least some of the time.\n- No security impact: the failure is fail-closed (a feature stays off).\n\n### Proposed fix\n\n1. **Make it observable first.** Add pairing state to `/health` (e.g.\n   `pairing: { enabled, tokenMinted, peerConnected }`). Today the only way to answer\n   \"is pairing on\" from outside is to infer it from a token list via an operator\n   credential. A feature gated per-tenant needs to say whether it is on.\n2. **Close the env-drift class.** Have the DO compare a hash of the freshly computed\n   `cellEnv` against the hash it last started with (persist in DO storage); on a\n   mismatch, `destroy()` then start, rather than calling `start()` and trusting it.\n   This is the \"env-hash drift-restart\" already noted as a follow-up in the release\n   guide — this incident is the argument for doing it.\n3. **Correct the runbook.** `/_cell/restart` is documented as *the* sanctioned\n   recompute; this run shows it is not sufficient on its own. Record what actually\n   works once (2) lands.\n\nFiles: `apps/cells/cell-do.mjs` (drift detection), `apps/hub/src/server.mjs`\n(`/health` pairing block), `.ai/release-guide.md`.\n\n### Testing requirements\n\n- Unit: given a stored env hash ≠ freshly computed, the DO destroys before starting;\n  given equal, it does not churn the container.\n- Unit: `/health` reports `pairing.enabled` from `MAUDE_CELL_PAIRING` and\n  `tokenMinted` from the token store.\n- Live: after a deploy that changes a `cellEnv`-derived value, one wake shows the new\n  value in `/health` without an operator credential.\n\n**Complexity: Medium** (the DO change is small; proving it in the live topology is\nthe expensive half).\n\n## B. \"Open in Maude\" claims syncing that may never happen\n\n### Root cause\n\n`apps/studio/sync/supervisor.ts:147` returns the claim:\n\n```ts\nawait runtime.start();          // starting providers; does NOT await connect/auth\nconst canvases = runtime.size(); // how many are ELIGIBLE, not how many synced\nif (canvases === 0) return { syncing: false, reason: 'nothing-syncable', … };\nreturn { syncing: true, canvases };\n```\n\n`cloud/endpoints.ts` passes that straight through, and `CloudBar.jsx` renders it\n**once**:\n\n```js\nsetNote(connectOutcomeNote(r.json.project ?? projectId, r.json.sync));\n// → \"Syncing with alligators — 68 canvases.\"\n```\n\n`connectOutcomeNote`'s own hover text sends the user somewhere else for the truth\n(\"Live status is in the 'hub sync' slot in the status bar\") — an admission that the\nmodal is not showing it. Nothing re-renders that note when the WebSocket is refused,\nthe token is rejected, or the hub is unreachable, because `syncing: true` was never\na statement about the connection in the first place.\n\nThe irony is documented in the same file: `connectOutcomeNote`'s comment describes\nreplacing an earlier lie (\"restart the studio server to start syncing\") with an\noutcome — but the value it now reports is still an intention.\n\n### Impact\n\n- **Severity: medium-high, UX/trust.** This is the first thing a new cloud user does.\n  A silent failure here is indistinguishable from success, and the user's own report\n  is exactly that (\"vyskočí modal … ale reálně se nic nestane\").\n- Makes defect **A** invisible from the product side: a desktop attached to a cell\n  with pairing dark would show the same cheerful sentence.\n\n### Proposed fix\n\n1. **Report the connection, not the attempt.** Let `boot()` await first-connect (or a\n   short bounded window) and return a state machine — `connecting` → `synced` /\n   `refused` / `unreachable` — instead of a boolean latched at start.\n2. **Make the note live.** The status already flows: `_sync.json` + the `sync:status`\n   bus feed the status bar. Subscribe the modal to the same source so it updates in\n   place, and surface per-canvas progress (`n/68 synced`) rather than the eligible\n   count.\n3. **Verbose on demand** (the user's own suggestion): a disclosure in the modal\n   showing the last N sync events — connect, auth, per-document join, rejections.\n   The events exist; nothing renders them.\n4. **Never render a terminal-sounding sentence for a non-terminal state.** \"Syncing\n   with X — 68 canvases\" should be \"Connecting…\" until something confirms it.\n\nFiles: `apps/studio/sync/supervisor.ts`, `apps/studio/cloud/endpoints.ts`,\n`apps/studio/client/panels/CloudBar.jsx` (+ the committed client bundle).\n\n### Testing requirements\n\n- Unit: a runtime whose provider is refused (401) yields `syncing: false` with a\n  reason, not `syncing: true`.\n- Unit: `connectOutcomeNote` renders a connecting state distinctly from a synced one.\n- E2E (desktop): attach to a hub that refuses auth → the modal ends in a visible\n  failure, not a \"syncing\" sentence.\n\n**Complexity: Medium.**\n\n## The pattern worth naming\n\nThree defects in two days, one shape: **the code reported what it had asked for\nrather than what happened.** The hub committed only what it wrote (so paired edits\nsilently left no history); the annotation PUT warned to a console nobody reads; the\nattach modal reports the intent to sync. Where a status is user-visible, the check\nshould be \"what observation would prove this false, and does the UI survive it?\"\n","author":"1aGh","summary":"element cell-pairing-env-and-cloud-attach-progress","shapes":["el_02c7fb605e62b2119eaa"],"targets":["el_02c7fb605e62b2119eaa"],"mutations":[{"op":"upsert_element","element_id":"el_02c7fb605e62b2119eaa","kind":"rca","name":"cell-pairing-env-and-cloud-attach-progress"}]}}
{"hash":"sha256:dc76e7b6effdb3c8f0c67f3cf24a950569478319504cb4c1d735ac4dfd550756","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":722,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:dcb281b2478330cc51462a16b3f80c2591fcc279638221a4ff448721d07d055d"],"decision":{"id":"d_1e034e261c70a6547bcc8875","title":"RCA (updated with live evidence): cloud sync mirrors the local file set — hub-only canvases never arrive","rationale":"# RCA: live pairing never engages on the cell · and \"Open in Maude\" reports syncing that never starts\n\n**Date:** 2026-08-07 · **Reporter:** 1aGh · **Investigator:** Claude (Opus 5)\n**Source:** manual (tracker is `github`, but this arrived as a description, not an issue number)\n\n## Summary\n\nTwo defects, unrelated in code and identical in shape: **a status that reports an\nintention as if it were an outcome.**\n\n**A.** `CELL_LIVE_PAIRING` is deployed, the Worker computes `MAUDE_CELL_PAIRING: \"1\"`\nfor `alligators`, the fleet runs `maude-cell:v0.56.0` — and live pairing is *not*\nrunning. Two explicit `/_cell/restart` cycles (the runbook's sanctioned recompute)\ndid not change that. The release-guide's claim that `/_cell/restart` forces a fresh\n`cellEnv` is, at minimum, incomplete.\n\n**B.** After \"Open in Maude\" the desktop says *\"Syncing with \u003cproject\u003e — 68\ncanvases.\"* That sentence is emitted when the sync runtime has been **asked to\nstart** and the project **has** 68 syncable canvases. It is not evidence that a\nsocket opened, that the hub accepted the token, or that one byte moved. It is\nrendered once, statically, and never corrected — so a lane that dies immediately\nafter start looks identical to one that works.\n\n## A. Live pairing never engages\n\n### Confirmed (measured, not inferred)\n\n| Check | Result |\n| --- | --- |\n| `cell-loopback-sync` token in the cell's token store | **absent** (42 tokens, 0 matching) — queried via `/admin/api/tokens` with the derived operator bearer |\n| `peersCount` after a clean restart, no tabs open | **0** |\n| `CELL_LIVE_PAIRING` on the deployed Worker | **present**, `\"alligators\"` (Cloudflare API, not the repo) |\n| Deployed Worker code | contains `...livePairingEnabled(env, tenantId) ? { MAUDE_CELL_PAIRING: \"1\" } : {}` |\n| `livePairingEnabled()` logic | returns `true` for `alligators` (also with padding / different case) |\n| Container application | `maude-cell:v0.56.0`, config version 34, `instances: 5` |\n\nThe token is the load-bearing signal: `mintLoopbackSyncToken` runs at **hub boot**\nand is the first thing pairing does. `addToken` is `INSERT OR REPLACE` by label, so\na pre-existing token would not raise the count — but it would still be *in the\nlist*. It is not. Therefore `MAUDE_CELL_PAIRING` is not truthy in the hub process,\ni.e. **the variable is not reaching the container**.\n\n### Root cause (confirmed mechanism, one gap)\n\n`@cloudflare/containers` applies `envVars` **only on an actual container start**:\n\n```js\n// dist/lib/container.js — startContainerIfNotRunning\nif (this.startInFlight) return this.startInFlight;\nif (this.container.running) {            // ← fast path, returns here\n  ...\n  return 0;\n}\nconst startPromise = this.doStartContainer(waitOptions, options);\n// ...\n// doStartContainer, ~40 lines later, is the ONLY reader of options.envVars:\nconst envVars = options?.envVars ?? this.envVars;\nif (envVars \u0026\u0026 Object.keys(envVars).length \u003e 0) startConfig.env = envVars;\n```\n\nSo `await this.start({ envVars: await cellEnv(...) })` in `cell-do.mjs` is, against a\nrunning container, a **silent no-op for environment** — exactly what its own comment\nsays (\"Idempotent when already running\") without spelling out the consequence. Any\npath where the container is up before the DO's `fetch()` calls `start()` keeps the\nprevious environment forever, and nothing anywhere reports the discrepancy.\n\n**The gap:** `restart()` does `container.destroy()`, so the *next* request should\nfind `running === false` and start with fresh env. It did not — across two attempts,\nthe second with 75 s of settle time and a single wake request. The most probable\nremaining explanation is that the container is being started by the **platform**\nrather than by the DO (`instances: 5` for a single tenant suggests instances are\nmaintained independently of the DO's start call), which would mean the stored start\nconfig — not `cellEnv` — decides the environment on every platform-initiated boot.\n\n**This is not yet proven.** The decisive evidence is the hub's own stdout: it logs\n`[hub] live pairing ON — minted…` or `could not mint…` at boot. Container stdout was\nnot reachable from here.\n\n### Impact\n\n- **Severity: high for the feature, zero for data.** Live pairing — the headline of\n  the v0.56.0 cloud release — is dark in production for the one pilot tenant it was\n  enabled for. Desktop and browser still persist to the same disk; nothing is lost,\n  they just never meet live.\n- **Wider blast radius than pairing:** *any* future change to `cellEnv` derivation\n  is subject to the same silence. This already burned v0.54.0 (`HUB_PUBLIC_URL` /\n  `frame-ancestors`), and the runbook's documented remedy is now known to be\n  insufficient at least some of the time.\n- No security impact: the failure is fail-closed (a feature stays off).\n\n### Proposed fix\n\n1. **Make it observable first.** Add pairing state to `/health` (e.g.\n   `pairing: { enabled, tokenMinted, peerConnected }`). Today the only way to answer\n   \"is pairing on\" from outside is to infer it from a token list via an operator\n   credential. A feature gated per-tenant needs to say whether it is on.\n2. **Close the env-drift class.** Have the DO compare a hash of the freshly computed\n   `cellEnv` against the hash it last started with (persist in DO storage); on a\n   mismatch, `destroy()` then start, rather than calling `start()` and trusting it.\n   This is the \"env-hash drift-restart\" already noted as a follow-up in the release\n   guide — this incident is the argument for doing it.\n3. **Correct the runbook.** `/_cell/restart` is documented as *the* sanctioned\n   recompute; this run shows it is not sufficient on its own. Record what actually\n   works once (2) lands.\n\nFiles: `apps/cells/cell-do.mjs` (drift detection), `apps/hub/src/server.mjs`\n(`/health` pairing block), `.ai/release-guide.md`.\n\n### Testing requirements\n\n- Unit: given a stored env hash ≠ freshly computed, the DO destroys before starting;\n  given equal, it does not churn the container.\n- Unit: `/health` reports `pairing.enabled` from `MAUDE_CELL_PAIRING` and\n  `tokenMinted` from the token store.\n- Live: after a deploy that changes a `cellEnv`-derived value, one wake shows the new\n  value in `/health` without an operator credential.\n\n**Complexity: Medium** (the DO change is small; proving it in the live topology is\nthe expensive half).\n\n## B. \"Open in Maude\" — sync mirrors the LOCAL file set, so cloud-only canvases never arrive\n\n### The measurement that settles it\n\nTaken live, desktop connected to `alligators.cloud.maude.sh`:\n\n| | |\n| --- | --- |\n| Desktop log / `_sync.json` | `72/72 synced`, `state: online`, 0 pending, **0 rejected** |\n| Hub documents | **75** |\n| Overlap (hub ∩ local) | **72** — exactly what the desktop synced |\n| **Hub-only** | **3**: `ui-welcome` (2 931 B), `ui-how_to_use_maude` (36 532 B), `ui-how_to_make_video` (31 911 B) |\n| Local-only | 8, all `_draw/*.proof` + `_photo-*` runtime state — correctly not synced |\n\nThe namespaces match perfectly, nothing is auth-rejected, and the sync is genuinely\nhealthy. **The three canvases that exist only in the cloud are real content, and the\ndesktop will never receive them.** That is the user's \"nothing happens\", made\nmeasurable.\n\nThey cannot arrive by git either: the local clone's remote is\n`github.com/1aGh/alligators`, it is 0 behind origin, and none of the three are\ntracked there — so \"Get latest\" would also do nothing.\n\n### Root cause\n\nThe sync runtime builds its provider set **exclusively from the local disk**:\n\n```ts\nconst canvases = admitCanvases(scan.canvases, useSharedDoc);   // sync/index.ts:427\n// scanCanvases() walks the local design root's canvas groups. There is NO\n// hub-side document enumeration anywhere in the runtime.\n```\n\nOne Yjs provider is opened per **local** canvas. A document that exists only on the\nhub has no local file → no provider → it is never requested. Sync is a *mirror of\nwhat this machine already has*, not a materialisation of the project.\n\nFor \"Connect an existing local project to a hub\" that is defensible. For **\"Open in\nMaude\" from the cloud dashboard** it is the wrong model: the user's intent is \"give\nme this cloud project\", and the flow silently delivers only the intersection with\nwhatever happened to be on disk.\n\n### Why the modal makes it invisible\n\n`supervisor.ts:147` returns `{ syncing: true, canvases }` where `canvases =\nruntime.size()` — the count of **local** syncable canvases. `CloudBar.jsx` renders it\nonce, statically:\n\n```js\nsetNote(connectOutcomeNote(r.json.project ?? projectId, r.json.sync));\n// → \"Syncing with alligators — 72 canvases.\"\n```\n\nSo the sentence is *true and misleading at the same time*: 72 canvases really did\nsync, but 72 is the size of the local set, while the project has 75. The number can\nnever reveal the gap because it is computed from the wrong universe. `connectOutcomeNote`'s\nown hover text then sends the user elsewhere for live status — an admission the modal\nisn't showing it.\n\n(Secondary: after boot the hub shows **1** peer, not 72 — providers do not hold all\ndocuments open. Expected, but it makes \"72 synced\" unverifiable from the hub side at\nany later moment.)\n\n### Original root-cause note (superseded by the measurement above, kept for the reasoning)\n\n`apps/studio/sync/supervisor.ts:147` returns the claim:\n\n```ts\nawait runtime.start();          // starting providers; does NOT await connect/auth\nconst canvases = runtime.size(); // how many are ELIGIBLE, not how many synced\nif (canvases === 0) return { syncing: false, reason: 'nothing-syncable', … };\nreturn { syncing: true, canvases };\n```\n\n`cloud/endpoints.ts` passes that straight through, and `CloudBar.jsx` renders it\n**once**:\n\n```js\nsetNote(connectOutcomeNote(r.json.project ?? projectId, r.json.sync));\n// → \"Syncing with alligators — 68 canvases.\"\n```\n\n`connectOutcomeNote`'s own hover text sends the user somewhere else for the truth\n(\"Live status is in the 'hub sync' slot in the status bar\") — an admission that the\nmodal is not showing it. Nothing re-renders that note when the WebSocket is refused,\nthe token is rejected, or the hub is unreachable, because `syncing: true` was never\na statement about the connection in the first place.\n\nThe irony is documented in the same file: `connectOutcomeNote`'s comment describes\nreplacing an earlier lie (\"restart the studio server to start syncing\") with an\noutcome — but the value it now reports is still an intention.\n\n### Impact\n\n- **Severity: medium-high, UX/trust.** This is the first thing a new cloud user does.\n  A silent failure here is indistinguishable from success, and the user's own report\n  is exactly that (\"vyskočí modal … ale reálně se nic nestane\").\n- Makes defect **A** invisible from the product side: a desktop attached to a cell\n  with pairing dark would show the same cheerful sentence.\n\n### Proposed fix\n\n**The real one — pull what the project has, not what the disk has.** On attach,\nenumerate the hub's documents and materialise the ones with no local counterpart.\nThe hub already exposes exactly this: `GET /admin/api/canvases` returns\n`{ name, bytes, peers }` per document. It needs a non-admin, project-scoped\nequivalent (the desktop holds a member token, not the operator bearer), then a\nprovider per remote-only doc whose first sync writes the file. Guard it with the\nexisting containment + `syncable` rules — a hub-named document must not be able to\nwrite outside the design root (DDR-054).\n\nUntil that lands, \"Open in Maude\" on a project the user has never opened locally\ndelivers an empty-looking project, which is the worst case of this bug, not the\nmildest.\n\n**The reporting, which hid it:**\n\n1. **Count the project, not the mirror.** Report `local ∩ hub`, `hub-only`, and\n   `local-only` — a single \"72 canvases\" cannot express the gap that matters.\n2. **Report the connection, not the attempt.** `boot()` should return a state\n   machine — `connecting` → `synced` / `refused` / `unreachable` — rather than a\n   boolean latched before the handshake.\n3. **Make the note live.** `_sync.json` + the `sync:status` bus already feed the\n   status bar; subscribe the modal to the same source instead of telling the user to\n   look somewhere else.\n4. **Verbose on demand** (the user's own suggestion): a disclosure listing the last N\n   sync events — connect, auth, per-document join, rejections, and *documents skipped\n   because they are not on this machine*. The events exist; nothing renders them.\n\nFiles: `apps/studio/sync/index.ts` (remote enumeration), `apps/hub/src/server.mjs`\n(project-scoped document list), `apps/studio/sync/supervisor.ts`,\n`apps/studio/cloud/endpoints.ts`, `apps/studio/client/panels/CloudBar.jsx`\n(+ the committed client bundle).\n\n### Testing requirements\n\n- Unit: a runtime whose provider is refused (401) yields `syncing: false` with a\n  reason, not `syncing: true`.\n- Unit: `connectOutcomeNote` renders a connecting state distinctly from a synced one.\n- E2E (desktop): attach to a hub that refuses auth → the modal ends in a visible\n  failure, not a \"syncing\" sentence.\n\n**Complexity: Medium.**\n\n## The pattern worth naming\n\nThree defects in two days, one shape: **the code reported what it had asked for\nrather than what happened.** The hub committed only what it wrote (so paired edits\nsilently left no history); the annotation PUT warned to a console nobody reads; the\nattach modal reports the intent to sync. Where a status is user-visible, the check\nshould be \"what observation would prove this false, and does the UI survive it?\"\n","author":"1aGh","summary":"element cell-pairing-env-and-cloud-attach-progress","shapes":["el_02c7fb605e62b2119eaa"],"mutations":[{"op":"upsert_element","element_id":"el_02c7fb605e62b2119eaa","kind":"rca","name":"cell-pairing-env-and-cloud-attach-progress"}],"provenance_only":true}}
{"hash":"sha256:5e688becd8d3d9ae6ee5a7d7a04de903f2e606284b23894b8f160041efb6fb6c","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":723,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:dc76e7b6effdb3c8f0c67f3cf24a950569478319504cb4c1d735ac4dfd550756"],"decision":{"id":"d_4452f1a1210446ecbafbb247","title":"Plan: ACP turn notifications (finish + needs-input), cross-project and detached","rationale":"Move the notify DECISION out of the webview into the native shell. The client-side notification already exists (app.jsx notifyDesktop/handleAssistantFinished/handleAssistantAttention, the latter added by DDR-185) and cannot cover the two cases the write-path-scope Addendum created: a project you are not looking at (the sidecar pool keeps its server and turns alive) and a DETACHED bridge (no socket, so no client code exists to run). The shell is the only component that outlives any one project's page. Approach: measure first whether the Web Notification API works at all in WKWebView (tauri-plugin-notification is NOT currently a dependency), then add the native plugin, extend /_api/acp/running into a per-chat activity snapshot (running/awaiting-input/idle, sourced from the bridge's existing pendingPermissions+pendingElicitations), and have the shell poll each pooled instance and fire on TRANSITIONS. Load-bearing constraint: the notification body carries the project name and a fixed string and NEVER a chat title - titles are model-generated from untrusted project content (DDR-054) and a lock screen is the worst possible render surface, made worse by cross-project polling carrying it out of its project. Explicitly NOT a cross-project chat LIST: declined 2026-08-07 because a shared list is one step from a shared session, which would dissolve the write-scope boundary. Awareness that another project is busy is safe; showing its contents is not.","author":"1aGh","summary":"element feature-acp-turn-notifications; element maude; element dev; link plan:feature-acp-turn-notifications-IN_REPO-\u003erepo:maude; link plan:feature-acp-turn-notifications-IN_DEPT-\u003edept:dev; link plan:feature-acp-turn-notifications-EXTENDS-\u003eplan:feature-acp-write-path-scope","shapes":["el_42b070d9886acea59c36","el_63e33b67b7bb66cc2f60","el_ec5ef965b4979595c248","el_edff36aef13890112ad5"],"targets":["el_edff36aef13890112ad5"],"mutations":[{"op":"upsert_element","element_id":"el_edff36aef13890112ad5","kind":"plan","name":"feature-acp-turn-notifications","props":{"date":"2026-08-07","path":".ai/plans/feature-acp-turn-notifications.md","status":"active"}},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_edff36aef13890112ad5","from_id":"el_edff36aef13890112ad5","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_edff36aef13890112ad5","from_id":"el_edff36aef13890112ad5","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_63e33b67b7bb66cc2f60","kind":"plan","name":"feature-acp-write-path-scope"},{"op":"add_link","element_id":"el_edff36aef13890112ad5","from_id":"el_edff36aef13890112ad5","to_id":"el_63e33b67b7bb66cc2f60","link_kind":"EXTENDS"}]}}
{"hash":"sha256:ed018cc42eb23a005317b91e34145e7d5ddbe932884d8729287bb7b099bad0c4","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":724,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:5e688becd8d3d9ae6ee5a7d7a04de903f2e606284b23894b8f160041efb6fb6c"],"decision":{"id":"d_75d4e96cf4a1ba1a5a181bd8","title":"A peer may ask the hub what the project contains — GET /api/documents, scope-bound to its own sync token","rationale":"Yjs has no enumeration, and the sync runtime opens one provider per canvas found on LOCAL disk. A document that exists only on the hub is therefore invisible to a peer by construction: a desktop carrying 72 of a project's 75 canvases syncs 72, reports \"72/72 synced\", and is accurate about the wrong universe. Reported as \"Open in Maude does nothing\" — three real canvases lived only in the cloud and could never arrive.\n\nDecision: the hub gains a READ-ONLY document listing, GET /api/documents, returning names and byte sizes.\n\nAuthenticated by the SAME peer token the caller syncs with, never the admin bearer — a peer must not need operator credentials to ask what it is syncing, and the cell operator secret is derived from the platform master. Every name is filtered through the same matchesScope gate onAuthenticate applies per document, so a token that may not open a document may not learn that it exists; the route can never widen what its holder already sees over the wire. No document CONTENT crosses. A missing and an invalid token get the identical 401 so it cannot become a token-probing oracle. The hub-chosen name list is capped where it reaches the client (payload size would otherwise be hub-controlled).\n\nDELIBERATELY NOT INCLUDED: materialising a hub-only document as a NEW local file. DDR-054 treats hub-pushed content as untrusted, and creating files is a wider grant than updating ones the user already chose to have. That is a trust-boundary change deserving its own decision and security review rather than riding along with a reporting fix. Consequence, accepted: after this change the missing canvases still do not arrive — but the gap is named instead of silent.\n\nThe consuming diff (apps/studio/sync/remote-docs.ts) compares WIRE document names, not raw slugs: comparing a namespaced hub against flat local names would report everything as missing on both sides, which is the doc-identity bug (DDR-192 §5) re-entering through the reporting path. An unreachable or older hub yields null and sync proceeds unchanged — making the listing load-bearing would trade a working feature for a nicer message.","author":"1aGh","summary":"element hub-document-listing","shapes":["el_94d9646d7f071208e464"],"targets":["el_94d9646d7f071208e464"],"mutations":[{"op":"upsert_element","element_id":"el_94d9646d7f071208e464","kind":"service","name":"hub-document-listing"}]}}
{"hash":"sha256:d674101de0e980594385495504dcb8d5c8a6b0a502381ecd53d801dc6897d440","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":725,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:ed018cc42eb23a005317b91e34145e7d5ddbe932884d8729287bb7b099bad0c4"],"decision":{"id":"d_d9ac6ae6f2f73f3e608acdba","title":"Cloud sync is complete and bidirectional — hub-only documents are pulled down, not just reported","rationale":"SUPERSEDES the same-day decision that listed hub-only documents but deliberately declined to fetch them. That call was made on a DDR-054 reading — a hub creating NEW files on your disk is a wider grant than updating canvases you already chose to have — and the owner overruled it on product grounds: the listing is already behind a login, the caller is authenticated into a project someone granted them access to, and inside that grant there is nothing to filter. A partial project is not a project.\n\nSync now runs both ways in full. Local canvases go up as they always did; documents the hub has and this machine does not are materialised as real local files before providers are built, so they sync from the first boot. Verified live against a hub with two documents the project did not have: both arrived with real content (2539 B and 264 B, not stubs), the local-only canvas appeared on the hub, and the runtime reported 3/3.\n\nWHAT IS KEPT, and why it is not filtering: (1) containment — a document NAME can never place a file outside the design root; that is the difference between writing your project and writing your filesystem. (2) The document name must match one of the two shapes a hub may legitimately use (flat slug, or ws/\u003cworkspace\u003e/\u003cbranch\u003e/\u003cslug\u003e), validated WHOLE. Taking the last segment of an arbitrary string accepted \"../../etc/passwd\" as \"passwd\" — contained, and still a file nobody asked for from a name that should have been refused. A regression test pins it. (3) The token scope gate on the listing, which is the access grant itself, not a filter over it.\n\nPlacement is FLAT under the design root, matching the hub own mirror-direction convention (workspace-files.mjs defaultBodyPath) and for the same reason: a slug is lossy, ui-card cannot be un-flattened into ui/Card.tsx without guessing, and a flat file is trivially moved while an invented directory tree is not. Moving it later does not break sync — both paths slug to the same document.\n\nDegradation: an older hub without GET /api/documents, or an unreachable one, returns null and sync proceeds exactly as before. The listing is never load-bearing.","author":"1aGh","summary":"element hub-document-listing","shapes":["el_94d9646d7f071208e464"],"mutations":[{"op":"upsert_element","element_id":"el_94d9646d7f071208e464","kind":"service","name":"hub-document-listing"}],"provenance_only":true}}
{"hash":"sha256:dfde93f673d5055ef30bbad994aa5a61e9419ed2357bcf697d0afab5a3ae22e0","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":726,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:d674101de0e980594385495504dcb8d5c8a6b0a502381ecd53d801dc6897d440"],"decision":{"id":"d_c5d2716258136a4570a1cc09","title":"ACP turn notifications: move the notify decision to the native shell","rationale":"The existing in-webview Notification path (notifyDesktop/handleAssistantFinished/handleAssistantAttention in app.jsx) only ever knows about the project currently loaded in the webview, and cannot see a DETACHED chat (feature-acp-write-path-scope Addendum: a bridge now outlives its socket) at all. So the notify DECISION moves out of the webview into the Tauri native shell, which is the only component that outlives any single project's page. Mechanism: the dev-server gains a read-only per-chat activity snapshot (AcpBridge.awaitingInputCount -\u003e acp/running.ts activitySnapshot() -\u003e GET /_api/acp/activity, main-origin-only, absent from both canvas-origin allowlists), and a new Rust module (apps/desktop/src-tauri/src/notify.rs) adds a tauri-plugin-notification dependency plus a background poller that walks the sidecar pool, skips the CURRENTLY-DISPLAYED project (the existing client path stays the fast path for that one), and fires a native OS notification on a STATE TRANSITION (never a level) for every other pooled instance. The notification body deliberately carries NO project content -- only the directory name (from the path the user chose) plus a fixed string -- because a chat title is model-generated from untrusted project content (DDR-054) and a lock-screen surface is the worst place to leak it. Decided explicitly NOT to add an in-app settings toggle for v1: macOS's own per-app Notification Center control already covers it, and an in-app toggle sitting next to the (unrelated) badge/finished-ping controls is a more likely place a user reflexively mutes and thereby silently loses the awaiting-input signal, which exists specifically to beat the 120s fail-closed permission-timeout deny. A mandatory security-auditor + ethical-hacker fan-out (Task 8) found the poller re-trusts _server.json fresh every poll with no spawn-time authentication (same class as the pre-existing has_running_chat pattern in sidecar.rs), letting a compromised project fabricate an unbounded-count notification stream and potentially social-engineer a user into disabling that same fail-closed escape hatch -- mitigated with a per-project-per-session notification cap + sink-side sanitization, with the deeper spawn-time-authentication fix recorded as a named follow-up rather than done now (it also touches has_running_chat and is a bigger architectural change than this feature's scope).","author":"1aGh","summary":"element acp-turn-notifications-native-shell-owns-notify; element maude; element dev; link decision:acp-turn-notifications-native-shell-owns-notify-IN_REPO-\u003erepo:maude; link decision:acp-turn-notifications-native-shell-owns-notify-IN_DEPT-\u003edept:dev","shapes":["el_42b070d9886acea59c36","el_8e37ba8cb90c9f4a9dbc","el_ec5ef965b4979595c248"],"targets":["el_8e37ba8cb90c9f4a9dbc"],"mutations":[{"op":"upsert_element","element_id":"el_8e37ba8cb90c9f4a9dbc","kind":"decision","name":"acp-turn-notifications-native-shell-owns-notify"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_8e37ba8cb90c9f4a9dbc","from_id":"el_8e37ba8cb90c9f4a9dbc","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"add_link","element_id":"el_8e37ba8cb90c9f4a9dbc","from_id":"el_8e37ba8cb90c9f4a9dbc","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"}]}}
{"hash":"sha256:84543087db7aee00f770f2a054486be34f8a99dd1d29a08ced73184fd6206465","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":727,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:dfde93f673d5055ef30bbad994aa5a61e9419ed2357bcf697d0afab5a3ae22e0"],"decision":{"id":"d_448bd4f3532acea6b55fc78a","title":"Plan: cloud connect tells the truth, live","rationale":"Two phases: make the sync status pipeline honest (monitor no longer born online, docs.synced can fall, remoteGap stops naming documents already pulled, status bar reads docs before state), then make the existing connect sentence live from the already-wired sync:status payload. Divergent debate ran builder/shipper/breaker; breaker blocked a live surface until the pipeline stops being optimistic and was upheld by code (state seeded online, aggregate returns connected on first match, docs.synced monotonic, syncSlot references docs zero times). A dedicated Connect Run job object was designed and deliberately rejected as a fourth consumer of a payload whose three existing readers already disagree — revisit if reports persist after the honest note ships.","author":"1aGh","summary":"element feature-cloud-connect-honest-status","shapes":["el_16d25ad840a95c981dee"],"targets":["el_16d25ad840a95c981dee"],"mutations":[{"op":"upsert_element","element_id":"el_16d25ad840a95c981dee","kind":"plan","name":"feature-cloud-connect-honest-status","props":{"path":".ai/plans/feature-cloud-connect-honest-status.md","status":"active"}}]}}
{"hash":"sha256:1909e861704aab94d3851f13e6d960daa66bfc1111b5b82794dd9c9168dadc97","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":728,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:84543087db7aee00f770f2a054486be34f8a99dd1d29a08ced73184fd6206465"],"decision":{"id":"d_730025bdde545150318f9d88","title":"DDR-214-one-honest-sync-status-rule-for-every-surface.md","rationale":"# DDR-214 — One honest sync-status rule, read by every surface\n\n**Date:** 2026-08-07\n**Status:** accepted\n**Scope:** `repo:maude` · `dept:dev`\n**Supersedes:** nothing. **Extends:** DDR-102 (per-document sync state), DDR-054 (hub content is untrusted).\n\n## Context\n\nA user connected a Maude Cloud project from the desktop app, watched the confirm\ndialog close, read one sentence — *\"Syncing with alligators — 75 canvases.\"* —\nand then nothing ever happened or ever changed. Their report: *\"vyskočí modal,\npak vidím jen syncing canvases, ale reálně se nic nestane.\"*\n\nThe obvious reading is that the modal needed a progress bar. It did not. Four\nindependent defects meant the signal a progress bar would have rendered was\noptimistic by construction, and rendering it faster would only have turned a\nstatic wrong sentence into an animated, more credible wrong one:\n\n1. **The monitor was born `online`** (`connection-state.ts`). From the instant a\n   link existed — before a socket, before a token was accepted, before a byte\n   moved — every reader of `state` reported success.\n2. **`aggregate()` returns `'connected'` on the first match.** One connected\n   provider out of seventy-five, with the other seventy-four auth-rejected,\n   still read green.\n3. **`docs.synced` could only rise.** No path demoted a `connected` document, so\n   a hub that died left the last count frozen on screen — the most convincing\n   possible lie, because it was true a moment ago.\n4. **The status bar referenced `docs` zero times.** It keyed on `state` alone,\n   which is hub *reachability*, not whether any document is syncing.\n\nAnd a fifth, ours, shipped days earlier in `231ead01`: `remoteGap` was computed\nbefore the pull and recorded after it, so it enumerated exactly the documents\nthat had just arrived. Observed live — `_sync.json` naming two canvases that\nwere sitting on disk.\n\nCompounding all of it: the connect note was derived from the **attach\nresponse**, which reports an *intention* (`{ syncing: true }` means\n`runtime.start()` did not throw and one local canvas qualified), and was\ncomputed once and stored as a rendered string. Its own hover text told the\nperson to go look at the status bar for the real answer — an admission that the\ntwo surfaces disagreed, and the status bar was wrong in a different way.\n\n## Decision\n\n**One rule, in one pure function, read by every surface that answers \"what is\nthe hub link doing\".** `apps/studio/sync/presentation.ts` exports\n`syncPresentation(payload, { project })`. The status-bar slot (`app.jsx`) and\nthe cloud rail's connect note (`CloudBar.jsx`) both call it. Agreement between\nthem is now structural rather than something two files have to remember.\n\nAlongside it, the pipeline it reads was made honest:\n\n- The monitor is seeded `'connecting'`, not `'online'`. The grace countdown is\n  now gated on the *timer* rather than on `state === 'online'`, so a link that\n  never reaches a provider still escalates to offline instead of waiting forever.\n- A provider that stops being connected **demotes** its document from\n  `connected` back to `pending` (provider id is the slug, so the monitor already\n  had the mapping). `auth-rejected` is deliberately sticky — the hub gave an\n  answer, and losing the socket does not turn it back into \"still trying\".\n- `remoteGap` is renamed to `pulled` — *what arrived this run*, which is both\n  true and the fact the Synced state tells the user to act on. Names capped at\n  20, `count` carries the true total so the cap can never falsify a number.\n- The connect note stores its **inputs**, not its rendered sentence, and derives\n  the text on every render from the already-wired `sync:status` payload. No\n  polling, no new bus consumer, no new fetch. The transition *Connecting… →\n  Syncing 40 of 75 → Synced · 3 came down* is the feedback that was missing.\n\n**Every state names a next action** (or says explicitly that there is none) —\nthe user's stated requirement. \"Refused\" says *reconnect, the credential may\nhave been rotated*; \"unreachable\" says *nothing to do, it resumes by itself*.\n\nOrdering inside the rule is itself a decision: an unreachable hub outranks every\ncount (a stale 75/75 over a dead socket is the exact lie being removed), and a\nrefusal outranks a partially-healthy count (3 of 75 refused still means 3\ncanvases going nowhere, and only the person can change that).\n\n## Alternatives rejected\n\n**A dedicated \"Connect Run\" sheet** — a persisted job record with a per-document\nledger and a confirmed round trip. The stronger design, and the right model if\nthis recurs. Rejected now because it adds a fourth consumer to a payload whose\nthree existing readers already disagreed; fixing the disagreement is the\nprerequisite, not the sheet. Revisit if ≥2 fresh reports arrive after the honest\nnote ships, or if first-arrival on an empty folder proves to need its own\nsurface.\n\n**Rendering the existing signal faster** (a spinner, a progress bar on the\nmodal). Explicitly rejected: it makes a wrong answer more convincing. This is\nwhy the work opened with a falsifier test rather than with UI.\n\n## Consequences\n\n- The status bar now shows `connecting…` briefly on every healthy boot where it\n  previously showed `synced` immediately. That is the honest reading, and it is\n  the cost of the seed change.\n- `pulled` is a **renamed** snapshot field. It was introduced days earlier in\n  `231ead01` and has no consumer outside `connection-state.ts` and `index.ts`,\n  so no compatibility shim is warranted — but the rename is a payload change and\n  `_sync.json` readers should be checked before assuming otherwise.\n- The presentation rule is imported into the client bundle, so touching it\n  requires a release-minified rebuild of `dist/client.bundle.js`.\n\n## Evidence\n\n- Falsifier: `apps/studio/test/sync-connect-honesty.test.ts` — red before the\n  monitor changed, for the two documented reasons.\n- Surface rule: `apps/studio/test/sync-presentation.test.ts`.\n- The sentence: `apps/studio/test/cloud-connect-note.test.ts`.\n- End to end: `apps/desktop/e2e/scenarios/cloud-attach.e2e.ts` cases 7 and 8.\n- Plan: `.ai/plans/feature-cloud-connect-honest-status.md`.","author":"1aGh","summary":"element general; element DDR-214; link decision:DDR-214-ABOUT-\u003earea:general; element maude; link decision:DDR-214-IN_REPO-\u003erepo:maude; element dev; link decision:DDR-214-IN_DEPT-\u003edept:dev; element DDR-102; link decision:DDR-214-SUPERSEDES-\u003edecision:DDR-102; element DDR-054; link decision:DDR-214-SUPERSEDES-\u003edecision:DDR-054","supersedes":["d_f37d2a7a0e978683552a63c5"],"shapes":["el_3e0fa848fec38ba7fce2","el_4036ee4b369d9a583c92","el_42b070d9886acea59c36","el_b2608542b645791aabc6","el_cdc050b23678a64f9b51","el_ec5ef965b4979595c248"],"targets":["el_4036ee4b369d9a583c92","el_b2608542b645791aabc6"],"mutations":[{"op":"upsert_element","element_id":"el_4036ee4b369d9a583c92","kind":"area","name":"general","props":{"last_ddr":"DDR-214"}},{"op":"upsert_element","element_id":"el_b2608542b645791aabc6","kind":"decision","name":"DDR-214","props":{"date":"2026-08-07","path":".ai/archive/decisions/DDR-214-one-honest-sync-status-rule-for-every-surface.md","title":"DDR-214-one-honest-sync-status-rule-for-every-surface.md"}},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_4036ee4b369d9a583c92","link_kind":"ABOUT"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_3e0fa848fec38ba7fce2","kind":"decision","name":"DDR-102"},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_3e0fa848fec38ba7fce2","link_kind":"SUPERSEDES"},{"op":"upsert_element","element_id":"el_cdc050b23678a64f9b51","kind":"decision","name":"DDR-054"},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_cdc050b23678a64f9b51","link_kind":"SUPERSEDES"}]}}
{"hash":"sha256:cd0b098c4dd172c88cd92907904f31641042c67c2215b13dbd4cf772f50a10e4","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":729,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:1909e861704aab94d3851f13e6d960daa66bfc1111b5b82794dd9c9168dadc97"],"decision":{"id":"d_628ebf739b29079382e24cf8","title":"Closed: feature-acp-turn-notifications","rationale":"Native shell owns the notify decision; two rounds of security fan-out closed a real HIGH finding and then closed two gaps in its own mitigation. Task 1's WKWebView measurement remains an honest open item (no GUI in the execution environment). Spawn-time authentication of _server.json (affecting this feature and the pre-existing has_running_chat) is a named follow-up, not done.","author":"1aGh","summary":"element feature-acp-turn-notifications","supersedes":["d_4452f1a1210446ecbafbb247"],"shapes":["el_edff36aef13890112ad5"],"targets":["el_edff36aef13890112ad5"],"mutations":[{"op":"upsert_element","element_id":"el_edff36aef13890112ad5","kind":"plan","name":"feature-acp-turn-notifications","props":{"path":".ai/plans/archive/feature-acp-turn-notifications.md","status":"done"}}]}}
{"hash":"sha256:33f264a049a492e8b63f9d0379b84e8a1badd91df08e8c8c59881f904554f97f","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":730,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:cd0b098c4dd172c88cd92907904f31641042c67c2215b13dbd4cf772f50a10e4"],"decision":{"id":"d_3150d1e867b2903ef232b664","title":"build.ts output directory is overridable, so tests cannot overwrite the shipped bundle","rationale":"apps/studio/test/bundle-smoke.test.ts shelled out to build.ts in DEV mode and wrote its 14 MB unminified output over the COMMITTED 2 MB release artifacts in apps/studio/dist. What is committed is what ships, so merely running the test suite silently staged a broken release — with no server running and no obvious culprit. CLAUDE.md carried this as \"observed, root cause unconfirmed\"; this test was the cause. build.ts now reads MAUDE_DIST_DIR, defaulting to \u003croot\u003e/dist, and the test builds into a disposable tmpdir. The knob exists ONLY so a test can build somewhere throwaway: unset — every real build, CI and release included — the path is byte-identical to before. Verified: a full studio suite run no longer touches dist/. Alternative rejected: having the test snapshot and restore dist/, which leaves the artifacts clobbered whenever the test crashes. Scope: repo:maude, dept:dev. Files: apps/studio/build.ts, apps/studio/test/bundle-smoke.test.ts.","author":"1aGh","summary":"element build-output-dir-overridable-so-tests-cannot-clobber-the-shipped-bundle","shapes":["el_c29f28e29e85258cb655"],"targets":["el_c29f28e29e85258cb655"],"mutations":[{"op":"upsert_element","element_id":"el_c29f28e29e85258cb655","kind":"decision","name":"build-output-dir-overridable-so-tests-cannot-clobber-the-shipped-bundle","props":{"path":"apps/studio/build.ts","status":"accepted"}}]}}
{"hash":"sha256:9b9e94969ff3dca4418053be9548e14fbdd221a4a1e71f6c158cb71ba9408de9","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":731,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:33f264a049a492e8b63f9d0379b84e8a1badd91df08e8c8c59881f904554f97f"],"decision":{"id":"d_55388320c8fa29c0af75363a","title":"DDR-214-one-honest-sync-status-rule-for-every-surface.md","rationale":"# DDR-214 — One honest sync-status rule, read by every surface\n\n**Date:** 2026-08-07\n**Status:** accepted\n**Scope:** `repo:maude` · `dept:dev`\n**Supersedes:** nothing. **Extends:** DDR-102 (per-document sync state), DDR-054 (hub content is untrusted).\n\n## Context\n\nA user connected a Maude Cloud project from the desktop app, watched the confirm\ndialog close, read one sentence — *\"Syncing with alligators — 75 canvases.\"* —\nand then nothing ever happened or ever changed. Their report: *\"vyskočí modal,\npak vidím jen syncing canvases, ale reálně se nic nestane.\"*\n\nThe obvious reading is that the modal needed a progress bar. It did not. Four\nindependent defects meant the signal a progress bar would have rendered was\noptimistic by construction, and rendering it faster would only have turned a\nstatic wrong sentence into an animated, more credible wrong one:\n\n1. **The monitor was born `online`** (`connection-state.ts`). From the instant a\n   link existed — before a socket, before a token was accepted, before a byte\n   moved — every reader of `state` reported success.\n2. **`aggregate()` returns `'connected'` on the first match.** One connected\n   provider out of seventy-five, with the other seventy-four auth-rejected,\n   still read green.\n3. **`docs.synced` could only rise.** No path demoted a `connected` document, so\n   a hub that died left the last count frozen on screen — the most convincing\n   possible lie, because it was true a moment ago.\n4. **The status bar referenced `docs` zero times.** It keyed on `state` alone,\n   which is hub *reachability*, not whether any document is syncing.\n\nAnd a fifth, ours, shipped days earlier in `231ead01`: `remoteGap` was computed\nbefore the pull and recorded after it, so it enumerated exactly the documents\nthat had just arrived. Observed live — `_sync.json` naming two canvases that\nwere sitting on disk.\n\nCompounding all of it: the connect note was derived from the **attach\nresponse**, which reports an *intention* (`{ syncing: true }` means\n`runtime.start()` did not throw and one local canvas qualified), and was\ncomputed once and stored as a rendered string. Its own hover text told the\nperson to go look at the status bar for the real answer — an admission that the\ntwo surfaces disagreed, and the status bar was wrong in a different way.\n\n## Decision\n\n**One rule, in one pure function, read by every surface that answers \"what is\nthe hub link doing\".** `apps/studio/sync/presentation.ts` exports\n`syncPresentation(payload, { project })`. The status-bar slot (`app.jsx`) and\nthe cloud rail's connect note (`CloudBar.jsx`) both call it. Agreement between\nthem is now structural rather than something two files have to remember.\n\nAlongside it, the pipeline it reads was made honest:\n\n- The monitor is seeded `'connecting'`, not `'online'`. The grace countdown is\n  now gated on the *timer* rather than on `state === 'online'`, so a link that\n  never reaches a provider still escalates to offline instead of waiting forever.\n- A provider that stops being connected **demotes** its document from\n  `connected` back to `pending` (provider id is the slug, so the monitor already\n  had the mapping). `auth-rejected` is deliberately sticky — the hub gave an\n  answer, and losing the socket does not turn it back into \"still trying\".\n- `remoteGap` is renamed to `pulled` — *what arrived this run*, which is both\n  true and the fact the Synced state tells the user to act on. Names capped at\n  20, `count` carries the true total so the cap can never falsify a number.\n- The connect note stores its **inputs**, not its rendered sentence, and derives\n  the text on every render from the already-wired `sync:status` payload. No\n  polling, no new bus consumer, no new fetch. The transition *Connecting… →\n  Syncing 40 of 75 → Synced · 3 came down* is the feedback that was missing.\n\n**Every state names a next action** (or says explicitly that there is none) —\nthe user's stated requirement. \"Refused\" says *reconnect, the credential may\nhave been rotated*; \"unreachable\" says *nothing to do, it resumes by itself*.\n\nOrdering inside the rule is itself a decision: an unreachable hub outranks every\ncount (a stale 75/75 over a dead socket is the exact lie being removed), and a\nrefusal outranks a partially-healthy count (3 of 75 refused still means 3\ncanvases going nowhere, and only the person can change that).\n\n## Alternatives rejected\n\n**A dedicated \"Connect Run\" sheet** — a persisted job record with a per-document\nledger and a confirmed round trip. The stronger design, and the right model if\nthis recurs. Rejected now because it adds a fourth consumer to a payload whose\nthree existing readers already disagreed; fixing the disagreement is the\nprerequisite, not the sheet. Revisit if ≥2 fresh reports arrive after the honest\nnote ships, or if first-arrival on an empty folder proves to need its own\nsurface.\n\n**Rendering the existing signal faster** (a spinner, a progress bar on the\nmodal). Explicitly rejected: it makes a wrong answer more convincing. This is\nwhy the work opened with a falsifier test rather than with UI.\n\n## Consequences\n\n- The status bar now shows `connecting…` briefly on every healthy boot where it\n  previously showed `synced` immediately. That is the honest reading, and it is\n  the cost of the seed change.\n- `pulled` is a **renamed** snapshot field. It was introduced days earlier in\n  `231ead01` and has no consumer outside `connection-state.ts` and `index.ts`,\n  so no compatibility shim is warranted — but the rename is a payload change and\n  `_sync.json` readers should be checked before assuming otherwise.\n- The presentation rule is imported into the client bundle, so touching it\n  requires a release-minified rebuild of `dist/client.bundle.js`.\n\n## Evidence\n\n- Falsifier: `apps/studio/test/sync-connect-honesty.test.ts` — red before the\n  monitor changed, for the two documented reasons.\n- Surface rule: `apps/studio/test/sync-presentation.test.ts`.\n- The sentence: `apps/studio/test/cloud-connect-note.test.ts`.\n- End to end: `apps/desktop/e2e/scenarios/cloud-attach.e2e.ts` cases 7 and 8.\n- Plan: `.ai/plans/feature-cloud-connect-honest-status.md`.\n\n## Amendment — the adversarial pass (2026-08-07)\n\nA defender + attacker review of this change found that an honesty module has a\nfailure mode ordinary features do not: **every defect in it is a lie told\nconfidently.** Six were fixed before landing, and two are worth stating as\nprinciples rather than as patches.\n\n**Fail closed.** `synced` was the fall-through branch, so any payload shape the\nmodule did not recognise — `docs: {}`, a partial write, a newer producer — took\nthe *most reassuring* path and rendered \"all NaN canvases\", green. `/_sync-status`\nis `JSON.parse` of a file with no schema. Counts are now validated as three\nfinite non-negative integers and the unreadable case renders as such. **For this\nmodule the pessimistic branch must be the default; `synced` has to be a positive\nassertion.**\n\n**Ordering is a security property.** The offline branch sat above the refused\nbranch, so a hub could refuse auth on the documents it wanted silenced, drop the\nsockets, and have us say \"your edits are safe and queued — nothing to do\". That\nis the exact bug this DDR exists to remove, re-entering one branch up. A\nrefusal never self-heals, so it outranks everything, including unreachability.\n\n**And the fix's own convenience knob was a supply-chain hole.** `MAUDE_DIST_DIR`\nwas added so a test would stop clobbering `dist/`. Unguarded, one environment\nvariable in a release runner sends every artifact to a scratch directory, exits\n0, and leaves packaging shipping the previously committed bundles while\n`check-runtime-bundles.sh` and `check-client-boots.mjs` validate the stale files\nand pass — a silent downgrade with no diff and no red CI. It is now refused\noutside a dev build. **A test-only override in a release script needs a guard at\nthe moment it is written, not later.**\n\nKnown and NOT fixed, deliberately: a hub that completes every handshake and then\nsilently stops acknowledging updates still reads \"synced\", because the demotion\nkeys on socket status and a ping keeps a socket alive. The obvious fix — gating\non `lastSyncAt` freshness — is wrong: that timestamp advances on sync *activity*,\nso every idle project would start reporting stale. A correct fix needs an\nout-of-band liveness round trip. Full triage, including the pre-existing\n`aggregate()` first-match and the `admitCanvases` bypass on pulled targets, in\n`.ai/logs/security-reviews/feature-cloud-connect-honest-status.md`.","author":"1aGh","summary":"element general; element DDR-214; link decision:DDR-214-ABOUT-\u003earea:general; element maude; link decision:DDR-214-IN_REPO-\u003erepo:maude; element dev; link decision:DDR-214-IN_DEPT-\u003edept:dev; element DDR-102; link decision:DDR-214-SUPERSEDES-\u003edecision:DDR-102; element DDR-054; link decision:DDR-214-SUPERSEDES-\u003edecision:DDR-054","supersedes":["d_730025bdde545150318f9d88"],"shapes":["el_3e0fa848fec38ba7fce2","el_4036ee4b369d9a583c92","el_42b070d9886acea59c36","el_b2608542b645791aabc6","el_cdc050b23678a64f9b51","el_ec5ef965b4979595c248"],"targets":["el_4036ee4b369d9a583c92","el_b2608542b645791aabc6"],"mutations":[{"op":"upsert_element","element_id":"el_4036ee4b369d9a583c92","kind":"area","name":"general","props":{"last_ddr":"DDR-214"}},{"op":"upsert_element","element_id":"el_b2608542b645791aabc6","kind":"decision","name":"DDR-214","props":{"date":"2026-08-07","path":".ai/archive/decisions/DDR-214-one-honest-sync-status-rule-for-every-surface.md","title":"DDR-214-one-honest-sync-status-rule-for-every-surface.md"}},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_4036ee4b369d9a583c92","link_kind":"ABOUT"},{"op":"upsert_element","element_id":"el_42b070d9886acea59c36","kind":"repo","name":"maude"},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_42b070d9886acea59c36","link_kind":"IN_REPO"},{"op":"upsert_element","element_id":"el_ec5ef965b4979595c248","kind":"dept","name":"dev"},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_ec5ef965b4979595c248","link_kind":"IN_DEPT"},{"op":"upsert_element","element_id":"el_3e0fa848fec38ba7fce2","kind":"decision","name":"DDR-102"},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_3e0fa848fec38ba7fce2","link_kind":"SUPERSEDES"},{"op":"upsert_element","element_id":"el_cdc050b23678a64f9b51","kind":"decision","name":"DDR-054"},{"op":"add_link","element_id":"el_b2608542b645791aabc6","from_id":"el_b2608542b645791aabc6","to_id":"el_cdc050b23678a64f9b51","link_kind":"SUPERSEDES"}]}}
{"hash":"sha256:80029f72d2b8eb4203ce512c42b1bb1c864612b2fceccdc4dd2bfa246e9dbd9a","op":"assert","actor":"1aGh","install_id":"id42822428bd846a0","lamport":732,"recorded_at":"2026-08-07T00:00:00Z","parents":["sha256:9b9e94969ff3dca4418053be9548e14fbdd221a4a1e71f6c158cb71ba9408de9"],"decision":{"id":"d_aba46236d3e035a429d8808c","title":"Security review: cloud connect honest status (defender + attacker)","rationale":"# Security review — feature-cloud-connect-honest-status\n\nDate: 2026-08-07 · Defender (`security-auditor`) + attacker (`ethical-hacker`), parallel, same diff.\nSeverity floor: `medium` (no `security` block in config → schema default).\n\n## Fixed in this change\n\n| # | Finding | Fix |\n| --- | --- | --- |\n| D1 / A3 | `safeName` bounded LENGTH only — no control chars, newlines or bidi overrides. A ≤60-char project name like `All 75 canvases synced.\\n\\n\\n` prepends a reassuring clause, and a `title=` tooltip renders the newlines. Two branches also led with the hub-supplied name, contradicting the module's own stated rule. | Strip `\\p{Cc}\\p{Cf}`, collapse whitespace, count/verb first in the offline sentence. |\n| D1b / A3b | `.gi-rail-hint` had **no** truncation while `CloudBar.jsx` documented \"the rail truncates\" — its sibling `.gi-rail-err` has carried the rule all along. A long name wrapped and grew into adjacent chrome. | Added `overflow/text-overflow/white-space` + `overflow-wrap: anywhere`; the wrapping deep-link confirm strip explicitly opts back out. |\n| D2 | The offline branch sat ABOVE the refused branch. A hub can refuse auth then drop sockets → \"your edits are safe and queued — nothing to do\", which for an auth-rejected document is false and never self-heals. The honest-status bug, re-entering one branch up. | Refusals now outrank an unreachable hub; unreachability is still reported alongside. |\n| D3 / A6 | The new demotion made **every** provider status event emit — a synchronous `_sync.json` write plus a WS fan-out, one provider per canvas, at a rate the hub controls. The pre-existing code emitted only on a transition. | Emit only on a real change (status moved, or a document demoted). |\n| A5 | **The module failed OPEN.** `synced` was the fall-through, so `docs: {}` gave `total === NaN` and rendered \"Synced — all NaN canvases\", green. `/_sync-status` is `JSON.parse` of `_sync.json` with no schema. For an honesty module the unreadable case must never be the reassuring one. | `readCounts` validates three finite non-negative integers; anything else renders an explicit \"status unreadable\" with a next action. `pulled.count` validated and clamped to `total`. |\n| A9 | **`MAUDE_DIST_DIR` (added in this change) was unguarded in `--release`.** One env var in a release runner sends every artifact to a scratch dir, exits 0, and packaging ships the previously committed bundles — `check-runtime-bundles.sh` and `check-client-boots.mjs` both validate the stale files and pass. A silent downgrade primitive with no diff and no red CI. | Refused outside `--dev`, with the reason in the error. |\n| A2a | `slugFromDocName` lowercases, so a hub advertising `Foo`, `foo` and `ws/w/main/foo` produced three pull targets for one file — three providers on one path and an inflated `pulled` count. | `pullTargets` dedupes by slug. |\n| A2b | The synced sentence said **\"Open one of the canvases that just arrived.\"** A pulled canvas is hub-authored TSX carrying the DDR-079 residual; an imperative inside our own green success line is the strongest endorsement we can give content we do not vouch for. | Reworded to state the fact — \"They are new to this machine — look them over before editing.\" |\n| A10 | A comment claimed the seed change made a never-answered link escalate to offline. It did not: `enterGrace` is reachable only from `noteProviderStatus`, so a link with zero provider events sat in `connecting` forever. | The grace clock is armed at construction, which makes the claim true. |\n| D-warn | `notePulled` was the only mutator without a `stopped` guard. | Guarded. |\n\n## Accepted, NOT fixed — with reasons\n\n- **A1 (HIGH) — a black-holed hub reads \"synced\" forever.** A hub that completes every\n  handshake, then only pings and never acknowledges another update, holds every provider\n  `connected`: no demotion fires, `state` stays `online`, and the user is told everything\n  is saved indefinitely. Correctly diagnosed and it is the strongest form of the bug this\n  change exists to reduce. **The proposed fix — gate `synced` on `lastSyncAt` freshness —\n  is wrong and was not applied**: `lastSyncAt` advances on sync ACTIVITY, so an idle\n  project legitimately has an old one and every quiet project would start reporting stale.\n  A real fix needs an out-of-band liveness signal (an application-level round trip), which\n  is a design change beyond this plan. **Not a regression** — pre-change this state read\n  green too, and more permanently, since `docs.synced` could not fall at all.\n- **A4 (MEDIUM) — `aggregate()` returns `connected` on the first match**, so one healthy\n  socket pins `state` online for the session, and the `syncing` phase has no timeout. The\n  plan named this and deliberately fixed it at the `docs` layer only; `aggregate` is\n  untouched by this change. Residual, pre-existing.\n- **A8 (MEDIUM) — pulled targets bypass `admitCanvases`.** Hub-supplied targets are\n  appended AFTER the DDR-064 A4 slug-collision check and the A6 pinned-room ceiling, so a\n  hub can create unbounded providers and can deliberately collide a pulled slug with a\n  local canvas. Pre-existing in the pull feature (`231ead01`), real, and the most valuable\n  follow-up here. Out of scope for a status-reporting change; fixing it alters what syncs.\n- **A7 (MEDIUM) — `aria-live=\"polite\"` announces oscillating counts.** Materially reduced\n  by the D3/A6 emit fix (no more per-event storm), but genuine flapping still re-announces.\n  Proper fix is to put only the phase in the live region.\n- **A11 (LOW)** — the `notSyncable` title is a fully-supplied string with no count prefix.\n  Locally generated today, and now passed through `safeName`.\n\n## Verified good (so it is not re-litigated)\n\nNo XSS sink — `presentation.ts` emits no markup and both render sites are React text /\nattribute. No new dependencies, no lockfile change. No token or secret in `_sync.json` or\n`/_sync-status`, and the route is main-origin only (absent from `CANVAS_SAFE_API`). Pulled\nslugs are constrained to `[A-Za-z0-9_-]{1,120}` with a containment re-check, so `names[]`\ncannot carry markup or traversal. State machine: no wedge, leak, or timer spin. Pulled\ncanvases DO reach `writeUntrustedMarkers` via the merged set.\n","author":"1aGh","summary":"element feature-cloud-connect-honest-status-security-review","shapes":["el_aa4b9b07138871b0335d"],"targets":["el_aa4b9b07138871b0335d"],"mutations":[{"op":"upsert_element","element_id":"el_aa4b9b07138871b0335d","kind":"security-review","name":"feature-cloud-connect-honest-status-security-review","props":{"path":".ai/logs/security-reviews/feature-cloud-connect-honest-status.md","status":"complete"}}]}}
